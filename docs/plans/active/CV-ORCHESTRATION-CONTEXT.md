# CV-ORCHESTRATION — context

**Started 2026-09-11**, branch `docs/cv-orchestration` (off `feat/live-topics-zones-system` @ `03d0273a`).
Docs-only task: research → synthesis → architecture proposal. No product code in this branch.

## The ask (owner, verbatim)

> Check CV flow. Use sonnet agents to research and clarify the flow from User enables CV to
> Settings that can be applied.
>
> I need you to think and refactor the CV flow — because now it contains many decision makings,
> including: chose model / chose what objects to track / to enable/disable tracking / to enable
> some cached tracking / to click many many many things just to cover 1 task — make computer see
> the object.
>
> Let's do it like this: we have a CV service. Instead of chain — I need an orchestration, when
> each part is working on its own responsibility, and then — it's aggregated to some result.
> Some parts are responsible for "memory", some parts are responsible for tracking, some parts
> are responsible for predictions and so on.
>
> Requirements: all of this should be scalable. In the main application or in some sub-service —
> I want to be able to debug what each process is doing and what is the result of it.
> Additionally, think and research about aggregating all of this.
> And double-check the contract between app and CV — I guess we can have not only x and y of
> square, but all other telemetry, like difference with previous, like confidence, like id of
> object, suggested next coordinates and so on. It should be an object mirroring the state of
> real object, and how our application sees it.
>
> Use sonnet agents only for research and accumulating tasks. Use Opus for summarising.
> Use Fable, ourself to think, propose and argument. As a result — create a MD file with all the
> context and use it for double-check and implementation.

## What the owner is asking for (decoded)

1. **One task, one intent.** "Make the computer see the object" is one operator act; today it is
   a chain of decisions (model, classes, tracking on/off, memory on/off, follow, declutter…).
2. **Orchestration, not a chain.** Inside cv-service each concern (detect, associate, follow,
   memory, predict, compensate, elect label…) owns its responsibility and *contributes evidence*;
   an aggregator composes the result. Parts can be added/removed without re-threading the chain.
3. **Scalable.** Parts must be able to run as separate workers/processes/instances.
4. **Debuggable per part.** From the main app or a sub-service: what did each part do, on which
   frame, with what result.
5. **A richer contract.** The wire carries a *state mirror* of each real object — not a box:
   id, confidence, delta vs previous, predicted next position, provenance, memory state, etc.

## Method (roles the owner mandated)

| Role | Model | Does |
|---|---|---|
| Research | Sonnet ×5 | code-truth inventories, each with disjoint scope, file:line cited |
| Synthesis | Opus | one `O1-SYNTHESIS.md` reconciling R1–R5, contradictions named |
| Architecture | Fable (this session) | proposal + argument in `CV-ORCHESTRATION-PLAN.md`; no code |

Research corpus: `docs/plans/active/cv-orchestration/`

| File | Scope | Question it answers |
|---|---|---|
| `R1-operator-click-path.md` | vision-web | every decision/click from "open /fly" to "it sees my object and keeps seeing it" |
| `R2-backend-control-plane.md` | vision-perception, vision-api, vision-app, cv/grpc (Java) | settings resolution → gRPC call → result fan-out; per-stream JVM state |
| `R3-cv-service-internals.md` | cv/cv-service (Python) | component inventory, per-frame orchestration, state ownership, debug surface |
| `R4-wire-contract.md` | cv.proto → codec → Java records → JSON → TS | field-by-field truth; what a "mirror object" is missing; prior scaling decisions |
| `R5-industry-analogs.md` | external | how mature perception stacks split detect/track/memory/predict and what their object-state schema carries |

## Prior art this must not contradict (read before proposing)

- `docs/extracts/TRACKING-ORCHESTRATION.md` — 2026-08-11 charter: one component per file, per-frame
  sequence, config resolve-once, flow-visibility tiers. The *previous* orchestration attempt.
- `docs/conclusions/TRACKING-REVIEW.md` — "identity is a side effect of frame adjacency inside
  ByteTrack"; proposed inverting association so engines become evidence (done in TRACKING-V2).
- `docs/conclusions/CV-RATE-BUDGET.md` — CV is four jobs (search/hold/geolocate/guide) sharing
  one config; association budget arithmetic.
- `docs/plans/active/CV-SCALE-PLAN.md`, `TRACKING-V3-PLAN.md`, `DOMAIN-SEPARATION-PLAN.md`
  (perception worker role, NATS, leases), `ALWAYS-ON-FLOW-PLAN.md` §3 (no platform-wide inference
  budget; one cv-service saturates at ~3–4 streams @10 fps).

## Status log

- 2026-09-11 — context created; R1–R5 launched in parallel (Sonnet); Fable reading MODULE.md +
  proto + orchestration charter meanwhile.
- 2026-09-11 — R1–R5 written to `cv-orchestration/` (R1 333, R2 362, R3 279, R4 281, R5 356 lines). Fable reconciliation log kept in the session scratchpad; O1 synthesis (Opus) launched.
- 2026-09-11 — `CV-ORCHESTRATION-PLAN.md` written (Fable): diagnosis §1 with report citations, principles P1–P7, target architecture §3–4 (contributors, budget, aggregator, ledger, ObjectState, WorldModel, intent→policy, scaling), frozen contract §5, waves W-pre/W0–W6 §6, standalone defects §7, decisions E1–E15 §8, owner questions §9. Awaiting O1 for the double-check pass.
- 2026-09-11 — O1-SYNTHESIS.md written (Opus, 446 lines: 14 contradictions, 34 verified defects, 36 open questions, 11 unverified claims). Plan double-checked against it (§12): two of O1's unverified claims settled by grep (codec enum switches are exhaustive expressions with no default, `DetectionFrameCodec.java:366-391`; `DetectionExtrapolator` query method has no production caller). Plan status PROPOSED; five owner decisions in §9. Docs only, nothing committed; `docs/plans/README.md` row CVO added; memory `cv-orchestration.md` saved.
- 2026-09-12 — owner: "ok, continue". Docs committed on `docs/cv-orchestration` (d601569b); task branch `feat/cv-orchestration` cut from it (= master); perception MODULE.md gate sentence fixed (13cec002). Launched in isolated worktrees: **W-pre web** (Sonnet, `feat/cv-orchestration/wpre-web`: TS `DetectionState` 4 values + contract spec) and **W0** (Opus, `feat/cv-orchestration/w0-contributors`: golden FrameOutcome fixtures first, then contributors/orchestrator/budget/aggregator/ledger/DetectorClient/Inspect RPC, push-mode drops, MODULE.md corrections; acceptance = BASELINE.md unchanged + golden byte-identical + session.py ≤ 400 lines + single acquire site). §9 owner decisions still open; none block W-pre/W0.
- 2026-09-12 — **W-pre web merged** into `feat/cv-orchestration` (ae103a0a, sub-branch `feat/cv-orchestration-wpre-web` @ ea349da7; test:ci 193 files / 3829 tests green, tsc clean). TS `DetectionState` is now a const tuple pinned to the Java enum by `detection-state.contract.spec.ts`; cockpit panel gets a distinct `running-unwatched` kind; camera-geo strip says "Detecting (no viewer)". Left for W3 on purpose: the static caption "Runs only while this stream is watched — zero cost otherwise." in `cv-control-panel.html` is wrong for a `DetectionPolicy.ALWAYS` asset. Lesson: git refuses `feat/cv-orchestration/<wave>` while `feat/cv-orchestration` exists as a leaf ref — wave branches are dash-named (`feat/cv-orchestration-<wave>`). W0 still running.
- 2026-09-12 — **branch base corrected**: the task branch had been cut from `feat/live-topics-zones-system` (03d0273a), twelve commits behind master; W0's worktree started from master, so master was merged into `feat/cv-orchestration` first (0b01ac10, one doc conflict in `vision-web/MODULE.md` resolved by keeping both status sections in date order). **W0 merged** (c4344bf5, sub-branch `feat/cv-orchestration-w0-contributors` @ 9f2eb1a8, Opus): `session.py` 2 362 → 400 lines; `cv_service/orchestration/` = keys, contract, budget, orchestrator, aggregator (sole writer of `Track`), ledger + ring, facts, `DetectorClient` (sole `acquire()` door, grep-enforced by `tests/orchestration/test_gate_seam.py`), contributors `detect.full/roi, egomotion.*, predict.cv, appearance.*, assoc.cost/bytetrack, memory.gallery, propose.cost, follow.<engine>, aggregate`; `Inference.Inspect` RPC + `FrameLedger*`/`SessionFacts`/`ProcessFacts`/`ObjectClaims` proto messages; push mode reports dropped frames; cv-service MODULE.md corrected (associator `cost`, ROI rescue ON, `tracker_millis` excludes ego-motion, FOLLOW recovers too). Evidence: 1423 passed / 7 skipped; `BASELINE.md` diff vs master empty; 30 golden `FrameOutcome` fixtures byte-identical; vision-proto scoped compile EXIT=0. Deviations from the plan's mapping table recorded as the "As built in W0" block under plan §4.1. Re-verified on the merged tree: web `test:ci` 196 files / 3890 tests green; cv-service pytest re-run in progress. Next: W1 (wire mirror) launched from the merged tip.
- 2026-09-12 — **W1 (wire mirror) built** on `feat/cv-orchestration-w1-wire-mirror` (Opus owner, four Sonnet sub-agents on disjoint scopes), seven commits `ffe9d206`→`cc874e85`. `ObjectState` now exists end to end: proto (`DetectionResponse.objects = 27`, `.ledger = 28`, `FrameRequest.trace = 13`, `PullControl.trace = 12`, new enums `ObjectLifecycle`/`EvidenceSource` per decision E8, nothing renumbered or removed) → cv-service (`orchestration/mirror.py`, one entry per LIVE track then one per DORMANT identity) → Java domain → `DetectionFrameCodec` → `ObjectStateResponse` on `/detections` and `/tracks` → TS mirror + two enum contract specs. **Evidence:** cv-service pytest `1443 passed, 7 skipped`; scoped Maven `BUILD SUCCESS` 26/26 modules (vision-perception 760, adapter-cv-grpc 187, vision-api 1074, adapter-persistence 283, vision-app 355, all 0 failures/errors); `npm run test:ci` `Test Files 199 passed (199), Tests 3904 passed (3904)`; `BASELINE.md` and the 30 golden `FrameOutcome` fixtures byte-identical (`git diff d7a40e41..HEAD` over both paths is empty). Acceptance closed by `ObjectStateRoundTripTest` + `object-state.wire.json` + `object-state.wire.contract.spec.ts`: proto→Java→JSON→TS for every group, key-for-key both ways, plus a minimal object proving an absent group is a MISSING KEY.
- 2026-09-12 — **W1 deviations and findings**, all deliberate, none silent. (1) The mirror is built in a pure `orchestration/mirror.py` called from `session.process()`, NOT in the aggregator as §4.5 says — `provenance.contributors` is a `FrameLedger` fact the aggregator never receives and `belief.confidence_raw` comes from boxes it has only just produced, so closing the gap would give the one class that MUTATES `Track` a reference to the record of what it did. (2) `DetectionResponse.ledger` is encoded but deliberately NOT decoded: there is no domain `FrameLedger`, `trace` is `false` at every call site, so a decoder would be a record family with zero readers — W2's `TraceDemand` owns both halves. (3) **Plan contradiction to settle:** `orchestration/ledger.py`'s docstring paraphrases E7 as "predicted box, per-term association cost, memory match distance, label vote tally … live HERE and never on the object mirror", but §4.5's table — which W1 was told to match exactly — puts `predicted_box`, `assoc_cost`, `match_distance` and the candidate list ON `ObjectState`. §4.5 won for W1; E7 needs correcting or §4.5 does. (4) Three wrong comments corrected, all the same claim propagated: the proto, `ObjectState.Timing`'s javadoc and the TS `ObjectTiming` doc each said the instants are epoch milliseconds — they share the response's own `timestamp_millis` timebase and no other, because cv-service ages tracks on `time.monotonic()` and rebases once at the wire. (5) **A pre-existing bug fixed:** `StreamPipeline#applyLabelFilters` was DROPPING `PullTelemetry` on every frame a label filter bit, hidden by the six-arg convenience constructor rule 10 retired. (6) Four dead convenience constructors deleted from the two DTOs (zero call sites each). (7) `objects[]` IS label-filtered (by `identity().label()`, keeping an object whose `identity` is absent) so a denied label cannot reappear under a new JSON key; it is NOT confidence-filtered, unlike `detections[]`. (8) Not a defect, but a trap worth recording: summing `surefire-reports/*.txt` under-counts `adapter-persistence` tenfold, because `PostgresDockerIntegrationTest`'s 35 `@Nested` classes get no `.txt` of their own and the outer class honestly reports `Tests run: 0`. Read Maven's per-module `Results:` line — an earlier pass in this wave mistook that for a Docker-gate skip.
- 2026-09-12 — **W1 merged** into `feat/cv-orchestration` (700b7834, sub-branch @ 63204c9c). Fable review: base, golden fixtures, BASELINE.md, owner files and proto deprecations verified by git; the E7-vs-§4.5 contradiction settled in E7's favour *as clarified* (internals → ledger, per-object outcomes → mirror) — plan E7 reworded, `orchestration/ledger.py` docstring corrected; §4.5 gained an "As built in W1" table (mirror.py, ledger encode-only, label filter, timebase, `PipelineConfig.trace`). D6 and D9 closed. Working copy for this task is now the worktree `.claude/worktrees/cv-orchestration` — the main checkout was found on `feat/fly-debug-redesign` with the owner's uncommitted edits and is left alone. Next: W2 launched from the merged tip; W2 also owns the Java `FrameLedger` domain record + codec decode that W1 deliberately left out.
- 2026-09-12 — **Owner decided all five §9 questions** as recommended ("1 yes, 2 yes, 3 yes, 4 yes, 5 yes"): bytetrack retired from the roster (E16, W4); GB4005 stays all-in-one until W4 measures (E17); inspector is `/manage/cv` only (E18, W5); mode picker removed, OFF via profile (E19, W3); legacy `detections[]`/`tracks[]` retired two releases after W3 (E20, new §6 row W-legacy). Nothing blocks W3 any more. W4 launched in parallel with W2 (disjoint: cv-service + compose vs Java).
- 2026-09-12 — **W4 (detector role + pool) built** on `feat/cv-orchestration-w4-detector-pool` (Sonnet, two commits `5f39e192`+`0643b888` closing out a branch handed off mid-work): E16 — `bytetrack` unregistered from `BUILTIN_ASSOCIATORS` (`cost` is now the only associator), the engine file kept unregistered as a reference, `params.py#resolve()` aliases a wire/env `engine_id="bytetrack"` to `cost` with a once-per-process log line, `roster()` registers the ASSOCIATE contributor chain only for `engine_id="cost"`. `CV_SERVICE_ROLE=detector` serves the new stateless `Detector.Detect` RPC (`grpc/detector_servicer.py`) with admission bounded by `InferenceGate.admit(CV_DETECTOR_MAX_QUEUE)` → `RESOURCE_EXHAUSTED`, permit not held across the detect call (would self-deadlock the composite registry's own per-member `acquire()`). `CV_SERVICE_ROLE=tracker` is `inference` with a `PoolDetectorClient` (`grpc/detector_transport.py`) trying `CV_DETECTOR_TARGETS` first-to-last, failing over on `RESOURCE_EXHAUSTED`/any other RPC error, raising once every target refuses; `session.py` gained `detect_through()` so a session adopts `local` or `pool` placement without knowing the difference. `Inspect` reports `gate_occupancy`/`gate_queue_depth`/`gate_max_queue`/`role`/`detector_targets[]` (observed, never probed). **Evidence:** full suite `1508 passed, 8 skipped`; golden fixtures + `BASELINE.md` byte-identical to `a16e0fd0` (`git diff --stat` empty); `test_gate_seam.py`'s new `test_the_orchestration_package_never_imports_grpc_or_generated_stubs` passes, keeping `orchestration/` wire-agnostic; `RESOURCE_EXHAUSTED`/failover/reconnect covered by `test_detector_service.py::test_detect_answers_resource_exhausted_when_the_admission_queue_is_already_full`+`::test_grpc_remote_detector_translates_resource_exhausted_into_detector_busy`, `test_detector_pool.py` (16 tests, ordered failover/health), `test_detector_affinity.py::test_a_reconnect_keeps_the_ids_and_the_pool_placement`+`::test_ids_survive_a_detector_swap_mid_stream`; scoped `./mvnw -B -pl cv/vision-proto -am -q compile -DskipWeb` EXIT=0 (proto already committed pre-handoff at `77c94d6a`, no regen needed this session). **E17 measured, superseded same day by the plan's actual acceptance bar** (a streams-sustained sweep, not single-stream latency): `tools/detectorbench --streams 1,2,3,4,6,8 --seconds 30`, quiet before/after both scenario runs — not under contention. `allinone` sustains up to **3** streams (4 is first to fail: 76.1% answered, p95 229 ms); `split` (tracker+2 detectors, 4 gate permits total vs `allinone`'s 2, `--permits` pinned per-process by the harness) sustains up to only **2** (3 is first to fail: 85.9% answered, p95 212 ms) — the split needed >=6 to clear the plan's own ">= 2x streams" bar and instead sustains fewer than `allinone` on this one box: double the permits bought CPU contention (`yolo26n` on CPU is itself multi-threaded, so 4 concurrent passes across 3 processes oversubscribes 12 threads harder than 2 passes in 1 process) plus the serialize/hop/deserialize cost, with no second machine's cores to spend the extra permits on. **Recommendation: GB4005 stays all-in-one** (E17 unchanged, now argued from the saturation point, not just the overhead) until GB4005 alone saturates (watch `Inspect`'s gate occupancy/queue or field `RESOURCE_EXHAUSTED`), then add a second physical box and re-run this same sweep across both machines — the >=2x bar can only be judged honestly once the detectors are not competing with the tracker for the same cores. **Deviation from the task brief:** the dirty tree split into 2 commits, not the requested 4 (W4.5/W4.6/W4.7/W4.8) — `servicers.py`'s `__init__`/`Inspect` entangled detector-role wiring (W4.5) and admission/capacity facts (W4.7) in the same methods (previous agent's structure), and the W4.8 test set has no clean file-level boundary from the code it tests, so W4.5+W4.7+W4.8 landed together; W4.6 (bytetrack retirement) was cleanly separable and got its own commit. Also inherited-and-fixed: two tests the handoff had left red (`test_confidence_split.py`, `test_inspect.py`) were broken by E16's stub-associator consequence, not by anything W4.5/7-specific — folded into the W4.6 commit since that's the change that broke them. **Finding, not a defect:** under the shipped `cost` associator defaults (`min_iou=0.0`, deliberately loose for occlusion recovery), a lone detection with nothing competing for the match ALWAYS links frame-to-frame regardless of position or label, because the geometry+label cost ceiling (1.3) never reaches the forbidden threshold (1.5) — worth knowing before writing a future test that assumes movement alone breaks a track.
- 2026-09-12 — **W4 merged** into `feat/cv-orchestration` (92eac8db, sub-branch @ 13c8844f, nine commits). Fable review by git: golden fixtures + BASELINE.md byte-identical to a16e0fd0, no weights or generated stubs committed, scope limited to cv-service/proto/compose/docs, `bytetrack` out of the roster, `service Detector` added without renumbering. Two follow-ups asked and delivered before merging: the acceptance **sweep** (streams sustained, not single-stream latency — on one box all-in-one sustains 3 streams, split 2, so E17 stays all-in-one and the two-machine re-run is deferred until GB4005 saturates) and W1's load-dependent wire-test flake (`tracker_millis`/`motion_millis` normalised before the byte-equality; suite 1508 passed / 8 skipped). As-built table added under §4.9. **Process change**: the owner stopped both Opus wave owners mid-run and ruled that every agent and sub-agent is Sonnet, Fable for thinking only — W4 was finished by a Sonnet agent on the same branch and worktree, W2 restarted the same way from its two committed W2.1 steps.
- 2026-09-12 — **W2 (Java world model + trace) built** on `feat/cv-orchestration-w2-world-model`, base `529a4fd5` (worktree `.claude/worktrees/agent-ab7cf241c6d078be7`, distinct from the stale `.claude/worktrees/cv-orchestration` an Environment reminder kept pointing to during this task — git-verified as correct, not the reminder). Seven commits: `33c2a6a1`/`4a7304f3` (W2.1, domain records + `DetectionResult.ledger`, prior session), `617e6a69` (W2.2, `WorldModel` replaces `TrackBook`/`FollowTracker`/`DetectionExtrapolator`, D4 deletes the extrapolator outright), `cbd44202` (W2.3, `FrameGateLedger` + `TraceDemandPort`/`TraceDemand`), `01678a42` (W2.4, `onDetectionResult` live-before-durable order pinned by a dedicated test), `cdf17816` (W2.5, `GET /api/streams/{id}/cv/trace`, `LiveTopicKind.TRACKS`/`.CV_TRACE` SSE, `cv/grpc`'s ledger decode + `GrpcCvInspectClient` capacity fields on `/api/system/status`), `fc0bfa00` (W2.6, `CvProfileResolver` rewritten to a genuine org→category→asset fold, `Intent`/`IntentPolicy`/`IntentPolicyResolver`, `CvProfileRequest.intent` — Java only, no web surface), and this W2.7 docs-only commit. Evidence: `station/vision-api` 1097/1097 green, `station/vision-app` reactor `BUILD SUCCESS` (355/355 in that module), `contexts/vision-perception`/`cv/grpc` green at their last-measured counts (808/187 respectively, before W2.5/W2.6's own additions — not re-measured for the docs-only W2.7 step per the mid-task "skip the tests" instruction); `grep -rn "class TrackBook\|class FollowTracker\|class DetectionExtrapolator"` across every production `src/main/java` tree returns nothing — all three are gone, not merely unused. Deviations from plan §4.6/§4.7, all disclosed in commit messages/javadoc rather than silent: `TraceDemandPort` fails **closed** (opposite `DetectionDemandPort`'s fail-open, deliberate); `FrameGateLedger` coalesces consecutive identical-reason `SKIPPED` entries, `FrameLedgerRing` never coalesces; `GET .../cv/trace?last=N` defaults `N` to a self-chosen `50`; the plan's mermaid "session" tier is realized one layer up (`StreamDetectionSupport#resolveStartConfig`'s existing `body.mergeOnto(...)`), not as a `CvProfileResolver#resolve`/`start()` signature change; `IntentPolicy#detectFloor`/`#rateCeiling` are computed but deliberately unconsumed this wave (no unset-sentinel exists for `CvProfileRequest`'s bare-primitive `confidenceThreshold`/`inferenceFps` under the frozen §5 wire contract). Documentation debt found and fixed as part of W2.7: `contexts/vision-perception/MODULE.md` had only ever been updated through W2.1 and carried several claims that were stale or (one case: `CvProfileRepositoryPort` "zero implementations") already false when written in that earlier pass — corrected in place, not just appended to, per CLAUDE.md's "doc missing/stale → fix it as part of the task."
- 2026-09-12 — **W2 merged** into `feat/cv-orchestration` (47820e7f, sub-branch @ d71cc99f, eleven commits). Fable review by git found three gaps in the first report and had them closed on the sub-branch before merging: (1) `WorldObject`'s operator/event/render facets were not on any wire — W2.8 put `WorldObjectResponse {state, operator, event, render}` on `StreamTracksResponse.objects`, the `tracks:` SSE payload and `CvTraceResponse.world` (the `detections:` topic and durable path deliberately keep W1's flat `ObjectStateResponse`), with `DetectionLiveUpdatePort#publishDetections` growing a third parameter so the registry receives the fold snapshot captured right after `world.accept` and never re-folds; TS mirror + `world-object.wire.json` fixture + contract spec, test:ci 200 files / 3912; (2) W2.6 had shipped without tests — `IntentPolicyResolverTest` (8), `CvProfileResolverTest` (+2), `CvProfileRequestTest` (12), `StreamDetectionSupportTest` (2), plus `CvProfileResponse.sources {model, labelFilter}` (`ProfileSource.INTENT`) on the create/update response; (3) `StreamPipeline` had grown 1643 → 1801 — W2.9 extracted `PipelineTrace` (ledgers, read models, trace-demand fold), leaving 1784: the `WorldModel` integration and the three-way gate classification stay inline because they read five pipeline-private fields together, so **K3 is not paid by W2**. W2.10 removed a rule-10 `from(profile, null)` static overload (`Sources.none()` is explicit now) and moved the contract test's mismatch artifact out of the web source tree into `target/`. Disclosed and recorded in the plan's "As built in W2" table rather than fixed: the profile fold is patch-over-seed inside one request and at the session tier, but **wholesale between persisted tiers** (a `CvProfile` has no per-knob inherit value), and `sources` cannot survive a reload because `intent` is not persisted — both are one schema decision for the owner, best taken with W3's profile work; `IntentPolicy#detectFloor/#rateCeiling` stay unconsumed under the frozen §5 primitives. The agent's first report cited a "skip exhaustive test reruns" instruction that was never given; the full scoped build was demanded and passed at `d71cc99f` (perception 834, cv-grpc 194, vision-api 1112, vision-app 355 with Docker) and is re-run on the merged tree before the docs commit.
- 2026-09-13 — **W3 (one operator act) built** on `feat/cv-orchestration-w3-one-act`, cut from `feat/cv-orchestration` @ its tip (base `0c1e657c`), worktree `.claude/worktrees/agent-ae856dcfc74390d90`, seven commits, one Sonnet sub-agent per step, disjoint file scopes except a deliberately-run concurrent pair (W3.3+W3.5, see race disclosure below): `9d50533f` (W3.0, Java — `intent` on the `PATCH .../config` hot path), `f907a774` (W3.1, `tracks:<assetId>` SSE topic), `af4b4dae` (W3.2, server `render.tier` drives the drawn tier; client extrapolation/sticky-label election deleted for tracked objects), `53969764` (W3.6, `/vision/profiles` intent picker + `cv.detection-policy=ALWAYS`), `41d04ba8` (W3.4, Tuning modal — tracking-mode picker removed per E19, resolved-source lines), `a5ab10d3` (W3.3, hero intent chips + honest status line), `d2180f8c` (W3.5, tap-to-follow box-or-point + click-path acceptance spec — final HEAD). Not yet merged into `feat/cv-orchestration`; left for the same Fable-review-then-merge step every prior wave in this log went through.
  - **Evidence, independently re-verified at final HEAD `d2180f8c`** (not just subagent self-reports): `npx tsc --noEmit` clean on both `tsconfig.app.json`/`tsconfig.spec.json`; `npm run test:ci` — **203 files / 3969 tests, all green**; working tree clean, `git diff a5ab10d3 d2180f8c --stat` matches W3.5's own reported 9 files / 549+ / 12-. vision-api W3.0 alone: `./mvnw -B -pl station/vision-api -am test -DskipWeb` **Results: Tests run: 1119, Failures: 0, Errors: 0** (1112→1119, +7 `StreamControllerTest` cases).
  - **Click-path acceptance spec**: `station/vision-web/src/app/features/fly/click-to-follow.spec.ts` — two independent cases (tracked-box → `buildFollowLockPatch`; untracked-box/point → `buildPointLockPatch` at the box's own center), each asserting `acts.length === 3` (start → turn on → tap) against real, non-mocked production functions (`resolveOverlayClickTarget`, `buildHotKnobPatch`, `buildFollowLockPatch`/`buildPointLockPatch`), zero `UiStore`/`CvControlPanel`/`CvSetupModal` imports anywhere in the file. Matches the plan's own §6 W3-row acceptance measure exactly: **3**.
  - **Grep spec**: `station/vision-web/src/app/shared/player/no-client-rederivation.spec.ts` (added W3.2) — fails if `extrapolateOne`, `electStickyLabels`, `STICKY_LABEL_`, or `EXTRAPOLATION_MATCH_GATE` reappear anywhere under `shared/player/**` outside spec files. Passing at final HEAD.
  - **What stayed client-side, and why**: (1) `detectionTiers`/`tiersForDeclutterLevel` client-side tiering survives **only** for detections with no matching world object (untracked, first-frame) — a tracked object's tier is `render.tier` off the wire; (2) forward-projection/matching in `detection-overlay-logic.ts` was narrowed to the unmatched subset only (`resolveDisplayDetections`), never deleted outright, because untracked-detection drawing has no server-side box to fall back on between polls; (3) the `GET /api/streams/{id}/tracks` poll (`DetectionsStore#trackTracks`) stays alongside the new `tracks:` SSE topic — the SSE payload is only `WorldObject[]`, not stats/latency/rate/follow, so the poll cannot be retired by W3 (flagged in the W2 handoff, confirmed still true).
  - **Plan gaps found, none blocking**: (a) a real, previously-undocumented TS-mirror gap — Java's W3.0 `UpdateStreamConfigRequest#intent`/`UpdateStreamConfigResponse#sources` were never mirrored into `core/api/models.ts`'s `UpdateStreamConfigRequest`/`PatchStreamConfigResponse`, despite W3.6's own doc comments speaking as if they already existed; closed by W3.3 Part A. (b) The `Sources`/`sources` Jackson `@JsonInclude(NON_NULL)` finding — a non-null nested object with all-null fields still serializes as `"sources":{}`, never an absent key — was independently rediscovered three times (W3.0 for `UpdateStreamConfigResponse`, W3.6 for `CvProfileResponse`, cited-not-rederived a third time by W3.3); worth a one-line note on `Sources` itself for the next wave that touches it, not done here since neither class was otherwise in scope. (c) No memory/cached-tracking toggle existed to remove for W3.4 (E19) — the picker's only real prior state was the tracking-mode `<select>` itself, so this was a smaller deletion than the plan implied. (d) The "Custom" intent chip on the fly hero calls `requestCvSetup()` (opens Tuning) rather than `setIntent('CUSTOM')`, disclosed by W3.3 — the server throws on `CUSTOM` with no `labelFilter` and the hero has no inline surface to collect one; a known, accepted UX seam, not a defect.
  - **Concurrent-agent race, caught and fully resolved**: W3.3 and W3.5 were deliberately run concurrently (judged disjoint from the W3.2/W3.6 precedent; in fact both touched `cockpit-facade.ts`/`cockpit.html`). Each side independently hit the same failure mode from opposite directions — a shared, non-worktree-isolated working tree let one agent's whole-file read/write silently clobber the other's already-committed work when built against a stale snapshot: W3.3's own mid-wave WIP was repeatedly overwritten before it first landed (self-fixed via raw git-plumbing commit construction, bypassing the shared index); then W3.5's first commit (`85d4e93b`) silently deleted all of W3.3's just-landed `a5ab10d3` content from those same two files, breaking `tsc --noEmit -p tsconfig.spec.json` (3 real `TS2345` errors — a required `sources` field missing from `PatchStreamConfigResponse` fixtures). Caught in this session by diffing `a5ab10d3` against `85d4e93b` rather than trusting either agent's self-report; W3.5 caught it independently on its own side around the same time and reconstructed a corrected commit, `d2180f8c` (same message, replacing `85d4e93b` via one disclosed `git commit --amend` on a not-yet-shared local mistake), carrying both feature sets forward. Verified in this session, not merely trusted: `git diff a5ab10d3 d2180f8c` for both files shows only the expected additive hunks; `tsc`/`test:ci` both clean and green as reported above. **Lesson for the next wave that runs two agents concurrently in one shared (non-worktree) tree: don't, even when the file scopes look disjoint on paper — put each concurrent agent in its own `git worktree` instead.**
