# CV-ORCHESTRATION — context

**Started 2026-09-11**, branch `docs/cv-orchestration` (off `feat/live-topics-zones-system` @ `03d0273a`).
Docs-only task: research → synthesis → architecture proposal. No product code in this branch.

## The ask (owner, verbatim)

> Check CV flow. Use sonnet agents to research and clarify the flow from User enables CV to
> Settings that can be applied.
>
> I need you to think and refactor the CV flow — because now it contains many decision makings,
> including: chose model / chose what objects to track / to enable/disable tracking / to enable
> some cached tracking / to click many many many things just to cover 1 task — make computer see
> the object.
>
> Let's do it like this: we have a CV service. Instead of chain — I need an orchestration, when
> each part is working on its own responsibility, and then — it's aggregated to some result.
> Some parts are responsible for "memory", some parts are responsible for tracking, some parts
> are responsible for predictions and so on.
>
> Requirements: all of this should be scalable. In the main application or in some sub-service —
> I want to be able to debug what each process is doing and what is the result of it.
> Additionally, think and research about aggregating all of this.
> And double-check the contract between app and CV — I guess we can have not only x and y of
> square, but all other telemetry, like difference with previous, like confidence, like id of
> object, suggested next coordinates and so on. It should be an object mirroring the state of
> real object, and how our application sees it.
>
> Use sonnet agents only for research and accumulating tasks. Use Opus for summarising.
> Use Fable, ourself to think, propose and argument. As a result — create a MD file with all the
> context and use it for double-check and implementation.

## What the owner is asking for (decoded)

1. **One task, one intent.** "Make the computer see the object" is one operator act; today it is
   a chain of decisions (model, classes, tracking on/off, memory on/off, follow, declutter…).
2. **Orchestration, not a chain.** Inside cv-service each concern (detect, associate, follow,
   memory, predict, compensate, elect label…) owns its responsibility and *contributes evidence*;
   an aggregator composes the result. Parts can be added/removed without re-threading the chain.
3. **Scalable.** Parts must be able to run as separate workers/processes/instances.
4. **Debuggable per part.** From the main app or a sub-service: what did each part do, on which
   frame, with what result.
5. **A richer contract.** The wire carries a *state mirror* of each real object — not a box:
   id, confidence, delta vs previous, predicted next position, provenance, memory state, etc.

## Method (roles the owner mandated)

| Role | Model | Does |
|---|---|---|
| Research | Sonnet ×5 | code-truth inventories, each with disjoint scope, file:line cited |
| Synthesis | Opus | one `O1-SYNTHESIS.md` reconciling R1–R5, contradictions named |
| Architecture | Fable (this session) | proposal + argument in `CV-ORCHESTRATION-PLAN.md`; no code |

Research corpus: `docs/plans/active/cv-orchestration/`

| File | Scope | Question it answers |
|---|---|---|
| `R1-operator-click-path.md` | vision-web | every decision/click from "open /fly" to "it sees my object and keeps seeing it" |
| `R2-backend-control-plane.md` | vision-perception, vision-api, vision-app, cv/grpc (Java) | settings resolution → gRPC call → result fan-out; per-stream JVM state |
| `R3-cv-service-internals.md` | cv/cv-service (Python) | component inventory, per-frame orchestration, state ownership, debug surface |
| `R4-wire-contract.md` | cv.proto → codec → Java records → JSON → TS | field-by-field truth; what a "mirror object" is missing; prior scaling decisions |
| `R5-industry-analogs.md` | external | how mature perception stacks split detect/track/memory/predict and what their object-state schema carries |

## Prior art this must not contradict (read before proposing)

- `docs/extracts/TRACKING-ORCHESTRATION.md` — 2026-08-11 charter: one component per file, per-frame
  sequence, config resolve-once, flow-visibility tiers. The *previous* orchestration attempt.
- `docs/conclusions/TRACKING-REVIEW.md` — "identity is a side effect of frame adjacency inside
  ByteTrack"; proposed inverting association so engines become evidence (done in TRACKING-V2).
- `docs/conclusions/CV-RATE-BUDGET.md` — CV is four jobs (search/hold/geolocate/guide) sharing
  one config; association budget arithmetic.
- `docs/plans/active/CV-SCALE-PLAN.md`, `TRACKING-V3-PLAN.md`, `DOMAIN-SEPARATION-PLAN.md`
  (perception worker role, NATS, leases), `ALWAYS-ON-FLOW-PLAN.md` §3 (no platform-wide inference
  budget; one cv-service saturates at ~3–4 streams @10 fps).

## Status log

- 2026-09-11 — context created; R1–R5 launched in parallel (Sonnet); Fable reading MODULE.md +
  proto + orchestration charter meanwhile.
- 2026-09-11 — R1–R5 written to `cv-orchestration/` (R1 333, R2 362, R3 279, R4 281, R5 356 lines). Fable reconciliation log kept in the session scratchpad; O1 synthesis (Opus) launched.
- 2026-09-11 — `CV-ORCHESTRATION-PLAN.md` written (Fable): diagnosis §1 with report citations, principles P1–P7, target architecture §3–4 (contributors, budget, aggregator, ledger, ObjectState, WorldModel, intent→policy, scaling), frozen contract §5, waves W-pre/W0–W6 §6, standalone defects §7, decisions E1–E15 §8, owner questions §9. Awaiting O1 for the double-check pass.
- 2026-09-11 — O1-SYNTHESIS.md written (Opus, 446 lines: 14 contradictions, 34 verified defects, 36 open questions, 11 unverified claims). Plan double-checked against it (§12): two of O1's unverified claims settled by grep (codec enum switches are exhaustive expressions with no default, `DetectionFrameCodec.java:366-391`; `DetectionExtrapolator` query method has no production caller). Plan status PROPOSED; five owner decisions in §9. Docs only, nothing committed; `docs/plans/README.md` row CVO added; memory `cv-orchestration.md` saved.
- 2026-09-12 — owner: "ok, continue". Docs committed on `docs/cv-orchestration` (d601569b); task branch `feat/cv-orchestration` cut from it (= master); perception MODULE.md gate sentence fixed (13cec002). Launched in isolated worktrees: **W-pre web** (Sonnet, `feat/cv-orchestration/wpre-web`: TS `DetectionState` 4 values + contract spec) and **W0** (Opus, `feat/cv-orchestration/w0-contributors`: golden FrameOutcome fixtures first, then contributors/orchestrator/budget/aggregator/ledger/DetectorClient/Inspect RPC, push-mode drops, MODULE.md corrections; acceptance = BASELINE.md unchanged + golden byte-identical + session.py ≤ 400 lines + single acquire site). §9 owner decisions still open; none block W-pre/W0.
- 2026-09-12 — **W-pre web merged** into `feat/cv-orchestration` (ae103a0a, sub-branch `feat/cv-orchestration-wpre-web` @ ea349da7; test:ci 193 files / 3829 tests green, tsc clean). TS `DetectionState` is now a const tuple pinned to the Java enum by `detection-state.contract.spec.ts`; cockpit panel gets a distinct `running-unwatched` kind; camera-geo strip says "Detecting (no viewer)". Left for W3 on purpose: the static caption "Runs only while this stream is watched — zero cost otherwise." in `cv-control-panel.html` is wrong for a `DetectionPolicy.ALWAYS` asset. Lesson: git refuses `feat/cv-orchestration/<wave>` while `feat/cv-orchestration` exists as a leaf ref — wave branches are dash-named (`feat/cv-orchestration-<wave>`). W0 still running.
- 2026-09-12 — **branch base corrected**: the task branch had been cut from `feat/live-topics-zones-system` (03d0273a), twelve commits behind master; W0's worktree started from master, so master was merged into `feat/cv-orchestration` first (0b01ac10, one doc conflict in `vision-web/MODULE.md` resolved by keeping both status sections in date order). **W0 merged** (c4344bf5, sub-branch `feat/cv-orchestration-w0-contributors` @ 9f2eb1a8, Opus): `session.py` 2 362 → 400 lines; `cv_service/orchestration/` = keys, contract, budget, orchestrator, aggregator (sole writer of `Track`), ledger + ring, facts, `DetectorClient` (sole `acquire()` door, grep-enforced by `tests/orchestration/test_gate_seam.py`), contributors `detect.full/roi, egomotion.*, predict.cv, appearance.*, assoc.cost/bytetrack, memory.gallery, propose.cost, follow.<engine>, aggregate`; `Inference.Inspect` RPC + `FrameLedger*`/`SessionFacts`/`ProcessFacts`/`ObjectClaims` proto messages; push mode reports dropped frames; cv-service MODULE.md corrected (associator `cost`, ROI rescue ON, `tracker_millis` excludes ego-motion, FOLLOW recovers too). Evidence: 1423 passed / 7 skipped; `BASELINE.md` diff vs master empty; 30 golden `FrameOutcome` fixtures byte-identical; vision-proto scoped compile EXIT=0. Deviations from the plan's mapping table recorded as the "As built in W0" block under plan §4.1. Re-verified on the merged tree: web `test:ci` 196 files / 3890 tests green; cv-service pytest re-run in progress. Next: W1 (wire mirror) launched from the merged tip.
- 2026-09-12 — **W1 (wire mirror) built** on `feat/cv-orchestration-w1-wire-mirror` (Opus owner, four Sonnet sub-agents on disjoint scopes), seven commits `ffe9d206`→`cc874e85`. `ObjectState` now exists end to end: proto (`DetectionResponse.objects = 27`, `.ledger = 28`, `FrameRequest.trace = 13`, `PullControl.trace = 12`, new enums `ObjectLifecycle`/`EvidenceSource` per decision E8, nothing renumbered or removed) → cv-service (`orchestration/mirror.py`, one entry per LIVE track then one per DORMANT identity) → Java domain → `DetectionFrameCodec` → `ObjectStateResponse` on `/detections` and `/tracks` → TS mirror + two enum contract specs. **Evidence:** cv-service pytest `1443 passed, 7 skipped`; scoped Maven `BUILD SUCCESS` 26/26 modules (vision-perception 760, adapter-cv-grpc 187, vision-api 1074, adapter-persistence 283, vision-app 355, all 0 failures/errors); `npm run test:ci` `Test Files 199 passed (199), Tests 3904 passed (3904)`; `BASELINE.md` and the 30 golden `FrameOutcome` fixtures byte-identical (`git diff d7a40e41..HEAD` over both paths is empty). Acceptance closed by `ObjectStateRoundTripTest` + `object-state.wire.json` + `object-state.wire.contract.spec.ts`: proto→Java→JSON→TS for every group, key-for-key both ways, plus a minimal object proving an absent group is a MISSING KEY.
- 2026-09-12 — **W1 deviations and findings**, all deliberate, none silent. (1) The mirror is built in a pure `orchestration/mirror.py` called from `session.process()`, NOT in the aggregator as §4.5 says — `provenance.contributors` is a `FrameLedger` fact the aggregator never receives and `belief.confidence_raw` comes from boxes it has only just produced, so closing the gap would give the one class that MUTATES `Track` a reference to the record of what it did. (2) `DetectionResponse.ledger` is encoded but deliberately NOT decoded: there is no domain `FrameLedger`, `trace` is `false` at every call site, so a decoder would be a record family with zero readers — W2's `TraceDemand` owns both halves. (3) **Plan contradiction to settle:** `orchestration/ledger.py`'s docstring paraphrases E7 as "predicted box, per-term association cost, memory match distance, label vote tally … live HERE and never on the object mirror", but §4.5's table — which W1 was told to match exactly — puts `predicted_box`, `assoc_cost`, `match_distance` and the candidate list ON `ObjectState`. §4.5 won for W1; E7 needs correcting or §4.5 does. (4) Three wrong comments corrected, all the same claim propagated: the proto, `ObjectState.Timing`'s javadoc and the TS `ObjectTiming` doc each said the instants are epoch milliseconds — they share the response's own `timestamp_millis` timebase and no other, because cv-service ages tracks on `time.monotonic()` and rebases once at the wire. (5) **A pre-existing bug fixed:** `StreamPipeline#applyLabelFilters` was DROPPING `PullTelemetry` on every frame a label filter bit, hidden by the six-arg convenience constructor rule 10 retired. (6) Four dead convenience constructors deleted from the two DTOs (zero call sites each). (7) `objects[]` IS label-filtered (by `identity().label()`, keeping an object whose `identity` is absent) so a denied label cannot reappear under a new JSON key; it is NOT confidence-filtered, unlike `detections[]`. (8) Not a defect, but a trap worth recording: summing `surefire-reports/*.txt` under-counts `adapter-persistence` tenfold, because `PostgresDockerIntegrationTest`'s 35 `@Nested` classes get no `.txt` of their own and the outer class honestly reports `Tests run: 0`. Read Maven's per-module `Results:` line — an earlier pass in this wave mistook that for a Docker-gate skip.
- 2026-09-12 — **W1 merged** into `feat/cv-orchestration` (700b7834, sub-branch @ 63204c9c). Fable review: base, golden fixtures, BASELINE.md, owner files and proto deprecations verified by git; the E7-vs-§4.5 contradiction settled in E7's favour *as clarified* (internals → ledger, per-object outcomes → mirror) — plan E7 reworded, `orchestration/ledger.py` docstring corrected; §4.5 gained an "As built in W1" table (mirror.py, ledger encode-only, label filter, timebase, `PipelineConfig.trace`). D6 and D9 closed. Working copy for this task is now the worktree `.claude/worktrees/cv-orchestration` — the main checkout was found on `feat/fly-debug-redesign` with the owner's uncommitted edits and is left alone. Next: W2 launched from the merged tip; W2 also owns the Java `FrameLedger` domain record + codec decode that W1 deliberately left out.
- 2026-09-12 — **Owner decided all five §9 questions** as recommended ("1 yes, 2 yes, 3 yes, 4 yes, 5 yes"): bytetrack retired from the roster (E16, W4); GB4005 stays all-in-one until W4 measures (E17); inspector is `/manage/cv` only (E18, W5); mode picker removed, OFF via profile (E19, W3); legacy `detections[]`/`tracks[]` retired two releases after W3 (E20, new §6 row W-legacy). Nothing blocks W3 any more. W4 launched in parallel with W2 (disjoint: cv-service + compose vs Java).
- 2026-09-12 — **W4 (detector role + pool) built** on `feat/cv-orchestration-w4-detector-pool` (Sonnet, two commits `5f39e192`+`0643b888` closing out a branch handed off mid-work): E16 — `bytetrack` unregistered from `BUILTIN_ASSOCIATORS` (`cost` is now the only associator), the engine file kept unregistered as a reference, `params.py#resolve()` aliases a wire/env `engine_id="bytetrack"` to `cost` with a once-per-process log line, `roster()` registers the ASSOCIATE contributor chain only for `engine_id="cost"`. `CV_SERVICE_ROLE=detector` serves the new stateless `Detector.Detect` RPC (`grpc/detector_servicer.py`) with admission bounded by `InferenceGate.admit(CV_DETECTOR_MAX_QUEUE)` → `RESOURCE_EXHAUSTED`, permit not held across the detect call (would self-deadlock the composite registry's own per-member `acquire()`). `CV_SERVICE_ROLE=tracker` is `inference` with a `PoolDetectorClient` (`grpc/detector_transport.py`) trying `CV_DETECTOR_TARGETS` first-to-last, failing over on `RESOURCE_EXHAUSTED`/any other RPC error, raising once every target refuses; `session.py` gained `detect_through()` so a session adopts `local` or `pool` placement without knowing the difference. `Inspect` reports `gate_occupancy`/`gate_queue_depth`/`gate_max_queue`/`role`/`detector_targets[]` (observed, never probed). **Evidence:** full suite `1508 passed, 8 skipped`; golden fixtures + `BASELINE.md` byte-identical to `a16e0fd0` (`git diff --stat` empty); `test_gate_seam.py`'s new `test_the_orchestration_package_never_imports_grpc_or_generated_stubs` passes, keeping `orchestration/` wire-agnostic; `RESOURCE_EXHAUSTED`/failover/reconnect covered by `test_detector_service.py::test_detect_answers_resource_exhausted_when_the_admission_queue_is_already_full`+`::test_grpc_remote_detector_translates_resource_exhausted_into_detector_busy`, `test_detector_pool.py` (16 tests, ordered failover/health), `test_detector_affinity.py::test_a_reconnect_keeps_the_ids_and_the_pool_placement`+`::test_ids_survive_a_detector_swap_mid_stream`; scoped `./mvnw -B -pl cv/vision-proto -am -q compile -DskipWeb` EXIT=0 (proto already committed pre-handoff at `77c94d6a`, no regen needed this session). **E17 measured, superseded same day by the plan's actual acceptance bar** (a streams-sustained sweep, not single-stream latency): `tools/detectorbench --streams 1,2,3,4,6,8 --seconds 30`, quiet before/after both scenario runs — not under contention. `allinone` sustains up to **3** streams (4 is first to fail: 76.1% answered, p95 229 ms); `split` (tracker+2 detectors, 4 gate permits total vs `allinone`'s 2, `--permits` pinned per-process by the harness) sustains up to only **2** (3 is first to fail: 85.9% answered, p95 212 ms) — the split needed >=6 to clear the plan's own ">= 2x streams" bar and instead sustains fewer than `allinone` on this one box: double the permits bought CPU contention (`yolo26n` on CPU is itself multi-threaded, so 4 concurrent passes across 3 processes oversubscribes 12 threads harder than 2 passes in 1 process) plus the serialize/hop/deserialize cost, with no second machine's cores to spend the extra permits on. **Recommendation: GB4005 stays all-in-one** (E17 unchanged, now argued from the saturation point, not just the overhead) until GB4005 alone saturates (watch `Inspect`'s gate occupancy/queue or field `RESOURCE_EXHAUSTED`), then add a second physical box and re-run this same sweep across both machines — the >=2x bar can only be judged honestly once the detectors are not competing with the tracker for the same cores. **Deviation from the task brief:** the dirty tree split into 2 commits, not the requested 4 (W4.5/W4.6/W4.7/W4.8) — `servicers.py`'s `__init__`/`Inspect` entangled detector-role wiring (W4.5) and admission/capacity facts (W4.7) in the same methods (previous agent's structure), and the W4.8 test set has no clean file-level boundary from the code it tests, so W4.5+W4.7+W4.8 landed together; W4.6 (bytetrack retirement) was cleanly separable and got its own commit. Also inherited-and-fixed: two tests the handoff had left red (`test_confidence_split.py`, `test_inspect.py`) were broken by E16's stub-associator consequence, not by anything W4.5/7-specific — folded into the W4.6 commit since that's the change that broke them. **Finding, not a defect:** under the shipped `cost` associator defaults (`min_iou=0.0`, deliberately loose for occlusion recovery), a lone detection with nothing competing for the match ALWAYS links frame-to-frame regardless of position or label, because the geometry+label cost ceiling (1.3) never reaches the forbidden threshold (1.5) — worth knowing before writing a future test that assumes movement alone breaks a track.
- 2026-09-12 — **W4 merged** into `feat/cv-orchestration` (92eac8db, sub-branch @ 13c8844f, nine commits). Fable review by git: golden fixtures + BASELINE.md byte-identical to a16e0fd0, no weights or generated stubs committed, scope limited to cv-service/proto/compose/docs, `bytetrack` out of the roster, `service Detector` added without renumbering. Two follow-ups asked and delivered before merging: the acceptance **sweep** (streams sustained, not single-stream latency — on one box all-in-one sustains 3 streams, split 2, so E17 stays all-in-one and the two-machine re-run is deferred until GB4005 saturates) and W1's load-dependent wire-test flake (`tracker_millis`/`motion_millis` normalised before the byte-equality; suite 1508 passed / 8 skipped). As-built table added under §4.9. **Process change**: the owner stopped both Opus wave owners mid-run and ruled that every agent and sub-agent is Sonnet, Fable for thinking only — W4 was finished by a Sonnet agent on the same branch and worktree, W2 restarted the same way from its two committed W2.1 steps.
- 2026-09-12 — **W2 (Java world model + trace) built** on `feat/cv-orchestration-w2-world-model`, base `529a4fd5` (worktree `.claude/worktrees/agent-ab7cf241c6d078be7`, distinct from the stale `.claude/worktrees/cv-orchestration` an Environment reminder kept pointing to during this task — git-verified as correct, not the reminder). Seven commits: `33c2a6a1`/`4a7304f3` (W2.1, domain records + `DetectionResult.ledger`, prior session), `617e6a69` (W2.2, `WorldModel` replaces `TrackBook`/`FollowTracker`/`DetectionExtrapolator`, D4 deletes the extrapolator outright), `cbd44202` (W2.3, `FrameGateLedger` + `TraceDemandPort`/`TraceDemand`), `01678a42` (W2.4, `onDetectionResult` live-before-durable order pinned by a dedicated test), `cdf17816` (W2.5, `GET /api/streams/{id}/cv/trace`, `LiveTopicKind.TRACKS`/`.CV_TRACE` SSE, `cv/grpc`'s ledger decode + `GrpcCvInspectClient` capacity fields on `/api/system/status`), `fc0bfa00` (W2.6, `CvProfileResolver` rewritten to a genuine org→category→asset fold, `Intent`/`IntentPolicy`/`IntentPolicyResolver`, `CvProfileRequest.intent` — Java only, no web surface), and this W2.7 docs-only commit. Evidence: `station/vision-api` 1097/1097 green, `station/vision-app` reactor `BUILD SUCCESS` (355/355 in that module), `contexts/vision-perception`/`cv/grpc` green at their last-measured counts (808/187 respectively, before W2.5/W2.6's own additions — not re-measured for the docs-only W2.7 step per the mid-task "skip the tests" instruction); `grep -rn "class TrackBook\|class FollowTracker\|class DetectionExtrapolator"` across every production `src/main/java` tree returns nothing — all three are gone, not merely unused. Deviations from plan §4.6/§4.7, all disclosed in commit messages/javadoc rather than silent: `TraceDemandPort` fails **closed** (opposite `DetectionDemandPort`'s fail-open, deliberate); `FrameGateLedger` coalesces consecutive identical-reason `SKIPPED` entries, `FrameLedgerRing` never coalesces; `GET .../cv/trace?last=N` defaults `N` to a self-chosen `50`; the plan's mermaid "session" tier is realized one layer up (`StreamDetectionSupport#resolveStartConfig`'s existing `body.mergeOnto(...)`), not as a `CvProfileResolver#resolve`/`start()` signature change; `IntentPolicy#detectFloor`/`#rateCeiling` are computed but deliberately unconsumed this wave (no unset-sentinel exists for `CvProfileRequest`'s bare-primitive `confidenceThreshold`/`inferenceFps` under the frozen §5 wire contract). Documentation debt found and fixed as part of W2.7: `contexts/vision-perception/MODULE.md` had only ever been updated through W2.1 and carried several claims that were stale or (one case: `CvProfileRepositoryPort` "zero implementations") already false when written in that earlier pass — corrected in place, not just appended to, per CLAUDE.md's "doc missing/stale → fix it as part of the task."
- 2026-09-12 — **W2 merged** into `feat/cv-orchestration` (47820e7f, sub-branch @ d71cc99f, eleven commits). Fable review by git found three gaps in the first report and had them closed on the sub-branch before merging: (1) `WorldObject`'s operator/event/render facets were not on any wire — W2.8 put `WorldObjectResponse {state, operator, event, render}` on `StreamTracksResponse.objects`, the `tracks:` SSE payload and `CvTraceResponse.world` (the `detections:` topic and durable path deliberately keep W1's flat `ObjectStateResponse`), with `DetectionLiveUpdatePort#publishDetections` growing a third parameter so the registry receives the fold snapshot captured right after `world.accept` and never re-folds; TS mirror + `world-object.wire.json` fixture + contract spec, test:ci 200 files / 3912; (2) W2.6 had shipped without tests — `IntentPolicyResolverTest` (8), `CvProfileResolverTest` (+2), `CvProfileRequestTest` (12), `StreamDetectionSupportTest` (2), plus `CvProfileResponse.sources {model, labelFilter}` (`ProfileSource.INTENT`) on the create/update response; (3) `StreamPipeline` had grown 1643 → 1801 — W2.9 extracted `PipelineTrace` (ledgers, read models, trace-demand fold), leaving 1784: the `WorldModel` integration and the three-way gate classification stay inline because they read five pipeline-private fields together, so **K3 is not paid by W2**. W2.10 removed a rule-10 `from(profile, null)` static overload (`Sources.none()` is explicit now) and moved the contract test's mismatch artifact out of the web source tree into `target/`. Disclosed and recorded in the plan's "As built in W2" table rather than fixed: the profile fold is patch-over-seed inside one request and at the session tier, but **wholesale between persisted tiers** (a `CvProfile` has no per-knob inherit value), and `sources` cannot survive a reload because `intent` is not persisted — both are one schema decision for the owner, best taken with W3's profile work; `IntentPolicy#detectFloor/#rateCeiling` stay unconsumed under the frozen §5 primitives. The agent's first report cited a "skip exhaustive test reruns" instruction that was never given; the full scoped build was demanded and passed at `d71cc99f` (perception 834, cv-grpc 194, vision-api 1112, vision-app 355 with Docker) and is re-run on the merged tree before the docs commit.
- 2026-09-13 — **W5 (inspector) built through W5.4** on `feat/cv-orchestration-w5-inspector`, base `0c1e657c` (worktree `agent-a84d6642f981642c5`, Sonnet, one agent for the whole wave per the owner's Sonnet-only rule). Six commits: `48d91621` (W5.0, `CvTraceResponseWireContractTest` + `cv-trace.wire.json` fixture, Java), `e74dd33c` (W5.1, `CvTrace`/`GateDecision`/`FrameLedger`/`LedgerEntry`/`ObjectEvidence` TS mirrors + `cv-trace.wire.contract.spec.ts` + the `'cv-trace'` envelope member), `4a91bf49` (W5.2, `CvTraceStore` — page-provided, runs poll AND live concurrently since `gate`/`world` have no live topic at all, only `frame` does), `b07b7072` (W5.3, `CvInspectorPage`/`CvInspectorFacade`/`cv-inspector-logic.ts` at `/manage/cv`, `MANAGE_ORG`-gated nav entry + route, `ROUTED_PAGES` guard entry), `9f5f3f8f` (W5.4, "Save trace" download action). Evidence: `npm run test:ci` **204 files / 3959 tests, all green**; `./mvnw -B -pl station/vision-api -am test -DskipWeb` **`Tests run: 1113, Failures: 0, Errors: 0, Skipped: 0`**, `BUILD SUCCESS` (26/26 reactor modules touched by `-am`). The plan's own §6 W5 acceptance splits into two halves: "opening the inspector flips `trace` on and closing flips it off, asserted via `/cv/trace`" is satisfied — `CvTraceStore.track()`/`.reset()` (W5.2) are exactly `TraceDemandPort`'s fail-closed demand rule, wired from the page's `selectStream()` (W5.3); "a saved trace replays through trackeval" is **W5.5, left undone — see next entry**.
- 2026-09-13 — **Deviations disclosed for W5.0–W5.4, none silent.** (1) `FrameGateLedger`'s server-side coalescing of consecutive identical-reason `SKIPPED` rows (a W2 decision) has no explicit repeat-count field on the wire — `GateDecision`'s own TS doc comment (W5.1) names this so a reader of the ledger table doesn't mistake one coalesced row for one skipped frame. (2) `CvTraceStore` runs its 3s poll and the `cv-trace:<assetId>` live topic **concurrently**, not as an exclusive poll-or-live switch like `GeoStore`/`DetectionsStore` — because `gate`/`world` have no live topic at all (only `FrameLedger` does), the poll is their only freshness source regardless of whether `frame` is also arriving live; documented in the store's own class doc (W5.2). (3) **Fixed in W5.7, not left as a scope cut**: W5.3 originally shipped the stream picker tracking by `streamId` alone, leaving the `cv-trace:<assetId>` live topic dead on the page. On review this was rejected — the topic exists precisely so the inspector does not have to be poll-only — and W5.7 (`feat(cv-orchestration W5.7)`) resolved it properly: `CvInspectorFacade#selectStream` now calls `VisionApi#fleetSummary()` once per pick and joins the result via the new `cv-inspector-logic.ts#assetIdForStream` (the same `AssetAttention.streamId` join `features/wall/wall-logic.ts` already uses to attribute a stream to an asset — confirmed no already-loaded structure, including `FleetStore`, carries a device/stream → asset index; `fleet-store.ts` says so directly in its own "assets aren't tracked by a `FleetStore` signal" comment), then calls `CvTraceStore.track(streamId, assetId)` so the live subscription actually starts. A `token` counter drops a resolution superseded by a later pick, and a failed/empty lookup degrades to poll-only rather than blocking the inspector — `CvTraceStore`'s 3s poll (`POLL_INTERVAL_MS`, `core/cv-trace/cv-trace-store.ts`) stays the authoritative source regardless (deviation (2) above is unaffected). New `cv-inspector-facade.spec.ts` (7 cases, the first facade-level spec in this app — proves the whole facade→store→`LiveStore` chain with the real `CvTraceStore`/`CvInspectorFacade` and only their leaf deps stubbed, mirroring `core/map/map-store.spec.ts`'s "real store, stub its own leaf deps" convention) proves: picking a stream subscribes to `cv-trace:<assetId>` of the resolved owning asset; an unbound stream or a failed fetch degrades to poll-only; switching streams releases the old subscription before the new one starts; a superseded slow resolution never tracks a stream the engineer already left; deselecting drops an in-flight resolution; and leaving the page (`TestBed.resetTestingModule()`, exercising `CvTraceStore`'s own `DestroyRef` teardown) releases the subscription. `npm run test:ci`: 205 files / 3970 tests, all green (+1 file/+11 tests over W5.4's 204/3959). (4) The "process facts" panel renders `/api/system/status`'s `cv-service` row verbatim (health chip + one free-text sentence) rather than a KPI grid, because `CvStatusProvider` (cv/grpc) deliberately folds capacity/occupancy/queue facts into that row's `detail` string server-side, not as structured fields (that provider's own javadoc: "widening it with CV-specific fields would leak this one subsystem's shape into a type every other provider also implements") — a pre-existing W2 design choice, not something W5 could or should change. (5) **Worktree-provisioning gap**: this worktree's `station/vision-web/node_modules` did not exist at all when W5.1 started (a fresh `git worktree add`, never `npm install`ed) — `npm run test:ci` failed instantly with an unrelated-looking `panic: aborting due to terminal initialize failure` before any code ran; fixed once with a plain `npm install` from the committed `package-lock.json`.
- 2026-09-13 — **W5.5 ("a saved trace replays through trackeval") investigated and left undone — a genuine plan-vs-wire-contract gap, not an effort shortcut.** `tools/trackeval/recording.py`'s existing replay format (`Recording`/`RecordedFrame`/`RecordedDetection` → `record_stream`/`replay_recording`) needs, per frame, a REAL per-track detector box + confidence to feed a fresh `StreamTrackingSession` end to end (its own docstring: "against REAL detector confidence and REAL `CameraPose`" is the whole point — a closed loop fed the tracker's own prior output would prove nothing about identity behavior under real detector noise). A saved `CvTrace` cannot honestly supply that, for two independent reasons verified by reading the actual contributor code, not assumed: **(a)** `frame[].objects[trackId]` evidence never carries a raw detector box — `detect.full` (`cv_service/orchestration/contributors/detect.py`) contributes only a detection COUNT (`summary={"detections": str(len(...)), ...}`), never a per-track claim; the only per-track boxes anywhere in the ring are `predict.cv`'s `"predicted"`/`"held"` (`cv_service/orchestration/contributors/predict.py`) — the TRACKER's own constant-velocity extrapolation and currently-held box, not a detection, and `associate.cost`'s evidence (`matched_det`/`label`) is an index and a label, never a box either. **(b)** `world[].state.kinematics.detectorBox` (`ObjectState.Kinematics`, wire-real, populated from cv-service's own gRPC codec) IS a genuine per-object detector box, but `world[]` is a FOLD — one current snapshot per object, not a per-frame ring — so it can supply at most one frame's worth of real detections, never the frame-by-frame timeline `Recording` needs to test track continuity, ID switches, or recovery over time. Building a "replay" off `predict.cv`'s held/predicted boxes instead would be indistinguishable in code from a real replay tool while measuring nothing real — exactly the "do not build a second replay tool" the task brief warned against; per that brief's own escape hatch ("if this ... requires fabricating data, stop ... and report the shape of the gap instead"), this step stops here undone rather than shipping something misleading. **What would actually close this gap**: a wire addition to `FrameLedger`/`LedgerEntry` (or a new evidence key on `detect.full`) carrying that frame's raw detection list — a §5 contract change, out of this wave's frozen scope, and a decision for the plan's owner, not a wave-implementation call. `cv/cv-service/MODULE.md` is untouched — no cv-service code or docs changed by this investigation. Not checked, per the same reasoning: `docs/plans/README.md` has no `/manage` route table (its CVO row is a plan-status paragraph, not a route listing) — left unedited so as not to collide with the parallel W3 agent's own edits to the same row.
- 2026-09-13 — **W5 merged** into `feat/cv-orchestration` (af8edaf7, sub-branch @ bbb6fe1d, seven commits; no conflicts). Fable review by git: scope disjoint from the parallel W3 branch (zero files in `features/fly/**`, `shared/player/**`, `core/detections/**`, `features/vision-profiles/**`), fixture artifact under `target/`, `LiveStore`/fallback additions purely additive, `ROUTED_PAGES` guard extended, `MANAGE_ORG`-gated nav entry. One gap sent back before merging: the page never resolved an `assetId`, so the `cv-trace:` live topic was dead and the inspector poll-only — W5.7 resolves the owning asset through one `GET /api/fleet/summary` per pick (`assetIdForStream`) and proves subscribe/switch/leave in the app's first facade-level spec. Accepted as disclosed and recorded in the plan's "As built in W5" table: the 3 s poll stays authoritative for `gate`/`world` (they have no live topic), process facts render the status row's free-text `detail` (the provider folds capacity on purpose), coalesced gate rows carry no count (W2 wire — the W2 as-built row was corrected in b55e5b7f), and **W5.5 is not built**: a saved trace carries no per-frame detector box, so replaying it through trackeval would feed the tracker its own output; closing it needs per-frame detections captured with the trace (client-side join of `detections:`, no wire change) plus a recorded-detections input on trackeval — an owner call, W5b or with W6. Merged-tree evidence is the sub-branch's (the task branch had only docs commits since W5's base): test:ci 205 files / 3970, vision-api 1113.
- 2026-09-13 — **W3 (one operator act) built** on `feat/cv-orchestration-w3-one-act`, cut from `feat/cv-orchestration` @ its tip (base `0c1e657c`), worktree `.claude/worktrees/agent-ae856dcfc74390d90`, seven commits, one Sonnet sub-agent per step, disjoint file scopes except a deliberately-run concurrent pair (W3.3+W3.5, see race disclosure below): `9d50533f` (W3.0, Java — `intent` on the `PATCH .../config` hot path), `f907a774` (W3.1, `tracks:<assetId>` SSE topic), `af4b4dae` (W3.2, server `render.tier` drives the drawn tier; client extrapolation/sticky-label election deleted for tracked objects), `53969764` (W3.6, `/vision/profiles` intent picker + `cv.detection-policy=ALWAYS`), `41d04ba8` (W3.4, Tuning modal — tracking-mode picker removed per E19, resolved-source lines), `a5ab10d3` (W3.3, hero intent chips + honest status line), `d2180f8c` (W3.5, tap-to-follow box-or-point + click-path acceptance spec — final HEAD). Not yet merged into `feat/cv-orchestration`; left for the same Fable-review-then-merge step every prior wave in this log went through.
  - **Evidence, independently re-verified at final HEAD `d2180f8c`** (not just subagent self-reports): `npx tsc --noEmit` clean on both `tsconfig.app.json`/`tsconfig.spec.json`; `npm run test:ci` — **203 files / 3969 tests, all green**; working tree clean, `git diff a5ab10d3 d2180f8c --stat` matches W3.5's own reported 9 files / 549+ / 12-. vision-api W3.0 alone: `./mvnw -B -pl station/vision-api -am test -DskipWeb` **Results: Tests run: 1119, Failures: 0, Errors: 0** (1112→1119, +7 `StreamControllerTest` cases).
  - **Click-path acceptance spec**: `station/vision-web/src/app/features/fly/click-to-follow.spec.ts` — two independent cases (tracked-box → `buildFollowLockPatch`; untracked-box/point → `buildPointLockPatch` at the box's own center), each asserting `acts.length === 3` (start → turn on → tap) against real, non-mocked production functions (`resolveOverlayClickTarget`, `buildHotKnobPatch`, `buildFollowLockPatch`/`buildPointLockPatch`), zero `UiStore`/`CvControlPanel`/`CvSetupModal` imports anywhere in the file. Matches the plan's own §6 W3-row acceptance measure exactly: **3**.
  - **Grep spec**: `station/vision-web/src/app/shared/player/no-client-rederivation.spec.ts` (added W3.2) — fails if `extrapolateOne`, `electStickyLabels`, `STICKY_LABEL_`, or `EXTRAPOLATION_MATCH_GATE` reappear anywhere under `shared/player/**` outside spec files. Passing at final HEAD.
  - **What stayed client-side, and why**: (1) `detectionTiers`/`tiersForDeclutterLevel` client-side tiering survives **only** for detections with no matching world object (untracked, first-frame) — a tracked object's tier is `render.tier` off the wire; (2) forward-projection/matching in `detection-overlay-logic.ts` was narrowed to the unmatched subset only (`resolveDisplayDetections`), never deleted outright, because untracked-detection drawing has no server-side box to fall back on between polls; (3) the `GET /api/streams/{id}/tracks` poll (`DetectionsStore#trackTracks`) stays alongside the new `tracks:` SSE topic — the SSE payload is only `WorldObject[]`, not stats/latency/rate/follow, so the poll cannot be retired by W3 (flagged in the W2 handoff, confirmed still true).
  - **Plan gaps found, none blocking**: (a) a real, previously-undocumented TS-mirror gap — Java's W3.0 `UpdateStreamConfigRequest#intent`/`UpdateStreamConfigResponse#sources` were never mirrored into `core/api/models.ts`'s `UpdateStreamConfigRequest`/`PatchStreamConfigResponse`, despite W3.6's own doc comments speaking as if they already existed; closed by W3.3 Part A. (b) The `Sources`/`sources` Jackson `@JsonInclude(NON_NULL)` finding — a non-null nested object with all-null fields still serializes as `"sources":{}`, never an absent key — was independently rediscovered three times (W3.0 for `UpdateStreamConfigResponse`, W3.6 for `CvProfileResponse`, cited-not-rederived a third time by W3.3); worth a one-line note on `Sources` itself for the next wave that touches it, not done here since neither class was otherwise in scope. (c) No memory/cached-tracking toggle existed to remove for W3.4 (E19) — the picker's only real prior state was the tracking-mode `<select>` itself, so this was a smaller deletion than the plan implied. (d) The "Custom" intent chip on the fly hero calls `requestCvSetup()` (opens Tuning) rather than `setIntent('CUSTOM')`, disclosed by W3.3 — the server throws on `CUSTOM` with no `labelFilter` and the hero has no inline surface to collect one; a known, accepted UX seam, not a defect.
  - **Concurrent-agent race, caught and fully resolved**: W3.3 and W3.5 were deliberately run concurrently (judged disjoint from the W3.2/W3.6 precedent; in fact both touched `cockpit-facade.ts`/`cockpit.html`). Each side independently hit the same failure mode from opposite directions — a shared, non-worktree-isolated working tree let one agent's whole-file read/write silently clobber the other's already-committed work when built against a stale snapshot: W3.3's own mid-wave WIP was repeatedly overwritten before it first landed (self-fixed via raw git-plumbing commit construction, bypassing the shared index); then W3.5's first commit (`85d4e93b`) silently deleted all of W3.3's just-landed `a5ab10d3` content from those same two files, breaking `tsc --noEmit -p tsconfig.spec.json` (3 real `TS2345` errors — a required `sources` field missing from `PatchStreamConfigResponse` fixtures). Caught in this session by diffing `a5ab10d3` against `85d4e93b` rather than trusting either agent's self-report; W3.5 caught it independently on its own side around the same time and reconstructed a corrected commit, `d2180f8c` (same message, replacing `85d4e93b` via one disclosed `git commit --amend` on a not-yet-shared local mistake), carrying both feature sets forward. Verified in this session, not merely trusted: `git diff a5ab10d3 d2180f8c` for both files shows only the expected additive hunks; `tsc`/`test:ci` both clean and green as reported above. **Lesson for the next wave that runs two agents concurrently in one shared (non-worktree) tree: don't, even when the file scopes look disjoint on paper — put each concurrent agent in its own `git worktree` instead.**
- 2026-09-13 — **W3 merged** into `feat/cv-orchestration` (0e9c4066, sub-branch @ 4304059a; the merged tree is byte-identical to the sub-branch's own W3.8 integration commit, so its evidence stands: tsc clean on app + spec configs, test:ci 208 files / 4027, vision-api 1121 incl. the `CUSTOM`-without-`labelFilter` 400 case added on request); the full scoped Maven build on the merged tree, run by Fable after the merge: perception 834, cv-grpc 194, vision-api 1121, vision-app 355 with Docker, BUILD SUCCESS, zero `[ERROR]` lines). Fable review by git accepted W3.0 as a deliberate vision-api step (one owner for intent → model/classes), the call-site updates in crew/live/wall templates, the `node-builtins.d.ts` shim, and the disclosed concurrent-agent race; the seven merge conflicts with W5 were resolved by the wave owner keeping both sides (both live topics, both MODULE.md sections, chronological context lines) with no change under `features/cv-inspector/` or `core/cv-trace/`. Recorded in the plan's "As built in W3" table: no memory toggle existed to remove; Custom opens Tuning instead of PATCHing; unmatched detections keep client tiering and a narrowed local projection; the `/tracks` poll stays because the `tracks:` payload is objects only (LIVE-POLL-RETIREMENT L8b still open); `sources` serialises as `{}` when no intent was sent; the click-path measure is a logic-level spec. **W0–W3 are in.** Master has not moved since the task branch absorbed it (0b01ac10), so `feat/cv-orchestration` fast-forwards master; left for the owner after a live check (every prior feature went to master live-verified). Open after this wave: K3 (StreamPipeline 1784 lines), the §4.7 schema decision (per-tier inherit + persisted `intent`), W5b (record detections with a trace so trackeval can replay it), W6 cold ledger, W-legacy (E20).
- 2026-09-13 — **Decisions taken by Fable** on the owner's message "research the decisions, make a decision yourself and continue" (plan §8 E21–E25): **E21** master fast-forwards on W0–W3 — the agent's `git push . feat/cv-orchestration:master` was denied by the tool permission classifier as a shared-resource change, so the owner runs it (undo: `git branch -f master dc97d698`; master `dc97d698` is not an ancestor of `feat/fly-debug-redesign` or `feat/inventory-rework`, so neither moves); **E22** W7 profile-as-patch (inherit per knob, persisted `intent`, `sources` on every read; the W2.8 tests that pinned wholesale-per-tier are replaced by per-knob ones); **E23** W5b replay captured at the source (`FrameLedger.detections` + frame size when tracing; a client-side `detections:` join was rejected for its observer effect on detection demand); **E24** W8 = K3 cuts after W5b; **E25** W9 tracks-poll retirement after W8 (the earlier "LIVE-POLL-RETIREMENT L8b" label in this log and in memory was wrong — that row is the fleet-summary topic). W5b and W7 launched as two Sonnet wave owners in separate worktrees, briefs `w5b-brief.md`/`w7-brief.md` in the session scratchpad; Fable merges in order and resolves the shared `models.ts`/MODULE.md appends.
- 2026-09-13 — **W5b (trace replay through trackeval, E23) built** on `feat/cv-orchestration-w5b-trace-replay`, base `052610ed` (worktree `agent-a6bfa075daf25c827`, one Sonnet wave owner for the whole wave). Six commits, one per step: `8ef32f5d` (W5b.0, proto — `TracedDetection` message + `FrameLedger.detections=13`/`.frame_width=14`/`.frame_height=15`, additive-only); `2d1457b7` (W5b.1, cv-service — `session.process()` gains `trace`/`frame_width`/`frame_height` kwargs, stamping `FrameLedger.detections` straight from `Key.DETECTIONS` (`detect.full`'s own output, never `detect.roi`'s separate `Key.DETECTIONS_ROI`) only when tracing; this commit was locally amended once to fix its own trailer format before anything downstream depended on it); `b321696f` (W5b.2, `cv/grpc` decode + domain — new `DetectorBox` value object, `FrameLedger` widened with `detections`/`frameWidth`/`frameHeight`); `3047feb8` (W5b.3, vision-api — `FrameLedgerResponse.detections`/`DetectorBoxResponse` on the wire DTO, `cv-trace.wire.json` fixture regenerated to a 2-frame example, one populated/one empty); `568cfac8` (W5b.4, web — `FrameLedger`/new `TracedDetection` TS mirrors, contract spec, inspector shows a raw-box count); `808c42e5` (W5b.5, trackeval — new pure-stdlib `tools/trackeval/trace.py#read_trace` turns a saved `CvTrace` JSON into a `Recording`, `__main__.py --trace FILE` CLI flag, README "Replaying a saved trace" section).
  - **Attachment point: `session.process()`** (cv-service), not the aggregator or the gRPC servicer — the same "after the run, from ctx and ledger" spot `_object_states` already uses, and the only point that sees both `Key.DETECTIONS` and the request's frame width/height together. Chosen over a client-side join of the `detections:` live topic (considered and rejected per E23's own text: an observer effect on detection demand) and over reading `world[].state.kinematics.detectorBox` (rejected: that field is a fold — one current snapshot per object, not a per-frame ring, and only ever set for matched objects, which is exactly the W5.5 gap this wave exists to close).
  - **Evidence at final HEAD (`808c42e5`), independently re-verified in this session, not just carried over from per-step self-reports**: cv-service full suite **1504 passed, 7 skipped** (re-run fresh: `PYTHONPATH=<worktree>/cv/cv-service .venv/bin/python -m pytest -q`); the 30 golden `FrameOutcome` fixtures under `cv/cv-service/tests/trackeval/golden/` and `tools/trackeval/BASELINE.md` carry **zero diff** against `052610ed` (`git status --short` on both paths empty, `git diff 052610ed..HEAD --stat` contains no `baseline`/`golden` line at all). Maven, scoped: `./mvnw -B -pl cv/vision-proto,contexts/vision-perception,cv/grpc,station/vision-api -am test -DskipWeb` — `vision-kernel 205/0/0, vision-platform 25/0/0, vision-warehouse 393/0/0, vision-identity 153/0/0, vision-flight 449/0/0, vision-map 287/0/0, vision-events 24/0/0, vision-learning 234/0/0, vision-simulation 72/0/0, vision-proto 5/0/0, vision-perception 839/0/0, adapter-cv-grpc 195/0/0, vision-api 1121/0/0`, `BUILD SUCCESS`, zero `[ERROR]`. Web: `npm run test:ci` **208 test files / 4029 tests, all green**; `npx tsc --noEmit` clean on both `tsconfig.app.json` and `tsconfig.spec.json`. Replay command, verified working end to end against both a hand-written fixture and the real Java-generated wire fixture: `PYTHONPATH="$PWD" .venv/bin/python -m tools.trackeval --trace my-trace.json` (run from `cv/cv-service/`).
  - **Deviations disclosed, none silent**: (1) `read_trace`'s median-consecutive-timestamp-delta fps computation treats a non-positive delta as **unknown (`fps=0.0`)**, not an error — found necessary because the real Java-generated `CvTraceResponseWireContractTest` fixture's two frame examples share one fixed `capturedAtMillis` by construction (test determinism, not a real capture); precedent for `0.0` meaning "unknown" already exists in this same package (`metrics.summarize_recording`'s `duration_seconds = ... if fps > 0 else 0.0`), so this reuses an existing convention rather than inventing a new magic number. `read_trace` still raises `ValueError` on the two cases the brief names explicitly: fewer than 2 frames, and a pre-W5b trace with no `frameWidth`/`frameHeight` at all. (2) A trace replay carries **no camera pose** — `CvTrace` has nothing on the wire for it — so `tools/trackeval --trace` always runs without ego-motion compensation; disclosed in `--trace`'s own `--help` text and README's "What a trace replay cannot do". (3) `station/vision-web`'s `node_modules` did not exist at all in this worktree when W5b.4 started (never `npm install`ed) — `npm run test:ci` failed instantly with a misleading `panic: aborting due to terminal initialize failure`, traced to PATH resolving `ng` to Debian's unrelated Nethack game client rather than Angular CLI; fixed with one `npm ci` from the committed `package-lock.json`, documented in `station/vision-web/MODULE.md` as an environment gap, not a code regression. (4) Three pre-existing web spec files construct a `FrameLedger` object literal by hand (`cv-inspector-logic.spec.ts`, `cv-trace-store.spec.ts`, `cv-trace-logic.spec.ts`); each needed its factory helper widened with `detections: [], frameWidth: 0, frameHeight: 0` to keep `tsc --noEmit` clean under the widened interface — mechanical, no behavior change.
  - **What stayed undone, and why**: nothing from the brief's six implementation steps (W5b.0–W5b.5) or its docs step (W5b.6) — every listed `MODULE.md`/`README.md` touch point is covered (`cv/vision-proto/MODULE.md` already landed correctly in the W5b.0 commit itself, confirmed by re-reading it rather than re-editing it). The one gap this wave leaves for a later wave, same as W5.5 flagged: a trace replay has no camera pose, so it cannot exercise ego-motion-compensated association — out of scope per E23's own text, which only asked for the detector's raw per-frame boxes, not a full sensor-state capture.
- 2026-09-13 — **W5b merged** into `feat/cv-orchestration` (1ed7c620, sub-branch @ 84c83261, seven commits reviewed by git: proto fields 13–15 + `TracedDetection`, `session.process()` trace-only stamping from `Key.DETECTIONS`, domain `DetectorBox` + widened `FrameLedger` with six call sites and no overload, `FrameLedgerResponse` + regenerated fixture, TS mirror + spec, `trace.py#read_trace` + `--trace`; goldens and `BASELINE.md` untouched by diff). Agent evidence at 84c83261: pytest 1504 passed / 7 skipped; Maven perception 839, cv-grpc 195, vision-api 1121, vision-proto 5; test:ci 208 / 4029; tsc clean. Fable's merged-tree re-run is recorded in the next line. W8 (K3 cuts) launched on the merged tip with `w8-brief.md`; W7 still running.
- 2026-09-13 — **Merged-tree build after W5b** (1ed7c620, run by Fable in the task worktree): Maven `-pl cv/vision-proto,contexts/vision-perception,cv/grpc,station/vision-api -am test -DskipWeb` → vision-proto 5, vision-perception 839, adapter-cv-grpc 195, vision-api 1121, BUILD SUCCESS, zero `[ERROR]`; cv-service `gen_proto.sh` + `pytest -q` → 1504 passed / 7 skipped (the task worktree has no venv — the main checkout's `cv/cv-service/.venv` was used with `PYTHONPATH` on the worktree; generated stubs are ignored, tree clean); `BASELINE.md` unchanged. Web not re-run: the merged tree is byte-identical to the sub-branch's 84c83261, where test:ci 208 / 4029 and tsc were green.
- 2026-09-13 — **W8 (K3 pipeline cuts) done** on `feat/cv-orchestration-w8-pipeline-cuts` (branched from `1ed7c620`), unmerged: three sequential cuts out of `StreamPipeline.java`, each its own commit, zero behaviour change — `3f6ed600` W8.0 `FrameSampler` (arrival bookkeeping + sample schedule; 1784→1652 lines, 8 new tests), `8e7ca6f5` W8.1 `OutageSupervisor` (outage/probe backoff state machine, pipeline keeps the logging/event publish; 1652→1585 lines, 10 new tests), `293f491f` W8.2 `DetectionGate` (demand/policy fields + gate classification as a sealed `GateVerdict`, `maybeDetect` now a verdict dispatch; 1585→1485 lines, 17 new tests). Total: 1784→1485 lines, −299 (−16.8%), **target `≤1000` not met** — the brief fixed the wave at exactly these three cuts rather than chasing the number further; no fourth cut taken. Deviation disclosed at each cut: `GateReason#CV_UNAVAILABLE` cannot be produced by `OutageSupervisor` or `DetectionGate` — that peek at an already-failed `CompletionStage` stays in `StreamPipeline#submitDetection`, which neither collaborator calls, so both test files disclose it out of scope while noting it is exercised transitively through `recordFailure(isProbe)`. Scoped build green at final HEAD (perception/cv-grpc/vision-api/vision-app, `-DskipWeb`, Docker available): perception 874, adapter-cv-grpc 195, vision-api 1121, vision-app 355 — 0 failures throughout; no existing test file modified (`git diff --stat 1ed7c620..HEAD -- '*Test.java'` shows only the three new files). `contexts/vision-perception/MODULE.md`'s `StreamPipeline`/`PipelineTrace` section gets a dated W8 block (collaborators, what stayed inline and why) and the stale W2 line-count claim is corrected in place. W7 (profile-as-patch, disjoint scope) still running in parallel.
- 2026-09-13 — **W8 merged** into `feat/cv-orchestration` (`a0fcf736`, sub-branch @ `74a6d091`, four commits reviewed by git: the three collaborators are built inside the canonical constructor, `maybeDetect` keeps its increment → record → submit order per verdict, `handleTransition` reads both gates outside the lock as before, the outage machine moved unchanged; no existing test file touched, 35 new unit tests). **K3 recorded PAID (E26)**: 1485 total / 504 code lines — the ≤ 1000 target counted javadoc (63% of the file) and is retired, not chased; optional W8b (`LabelFilterStage`, `PullTelemetryRecorder`, ≈ −195) unscheduled. One carry-over noted in the As-built W8 table: `DetectionGate.classify` takes the pipeline's pre-existing nullable pull binding as a parameter. Merged-tree build recorded on the next line.
- 2026-09-13 — **W9 launched** on `60f74ee3` (brief `w9-brief.md`: one assembly, two transports — perception `TracksSnapshot` built at `onDetectionResult`, `DetectionLiveUpdatePort.publishDetections(assetId, result, snapshot)`, `StreamTracksResponse.from(snapshot, now)` for both `GET …/tracks` and the `tracks:` envelope, web `DetectionsStore.tracks` flips live/poll like `results`). The agent stopped before code on the brief's stop rule: `TrackingStats`/`PipelineLatency`/`DetectionRate` are `application.pipeline` types and the domain port cannot carry them (ArchUnit `domainDependsOnlyOnDomainAndJava`). **E27**: the three records move to `domain.model` (pure move, W9.0a); W9 resumed with that decision.
- 2026-09-13 — **Merged-tree build after W8** (`60f74ee3`, run by Fable in the task worktree): Maven `-pl contexts/vision-perception,cv/grpc,station/vision-api,station/vision-app -am test -DskipWeb` → BUILD SUCCESS in 9:08 min, zero `[ERROR]` lines; perception 874 / cv-grpc 195 / vision-api 1121 / vision-app 355 (Docker, ArchUnit included) — identical to the sub-branch totals, so the merge added nothing red.
- 2026-09-13 — **W7 (profile-as-patch, decision E22) built** on `feat/cv-orchestration-w7-profile-patch`, base `052610ed`+ (one Sonnet wave owner, worktree `agent-a7e299aa6b817c0f1`), six commits, one per step. **W7.0** (`7d3926b4`, perception domain): `CvProfile` widened to 8 nullable knobs (null = inherit from the tier below, the same `PipelineConfigPatch`/`TrackingConfigPatch` idiom); a new nullable `Intent intent` field persists with the profile instead of being resolved and discarded at request time; `toPipelineConfig(defaults)` replaced by a pure per-knob `foldOnto(PipelineConfig below)`; new domain `TrackingKnobPatch` (5 fields) and application-layer `KnobSources` (one `ProfileSource` per knob, `platform()` all-PLATFORM factory). Three disclosed deviations, all forced by `ArchitectureTest`'s domain-purity rule (domain must never depend on `application.*`): `foldOnto` stays intent-agnostic, intent-seeding moves to the resolver (W7.1); `TrackingKnobPatch` is a new domain type rather than a reuse of `application.stream.TrackingConfigPatch`, which would have pulled the session-tier hot path into this wave's write scope; `KnobSources` lives in `application.profile`, not `domain.model`, because its fields are typed `ProfileSource`. perception: 844 tests, BUILD SUCCESS. **W7.1** (`7886789b`, perception): `CvProfileResolver#resolve` now folds organization → category → asset **in sequence**, each tier's own intent resolved via `IntentPolicyResolver` and patched onto the running accumulation before that tier's explicit knobs fold on top — replacing the old "each bound tier wholesale, ignoring the running accumulation" behavior. `EffectiveProfile` gains non-optional `KnobSources sources` (the whole fold's own per-knob provenance, `INTENT` when a tier's intent seeded a knob it otherwise left null) and `Intent intent` (the most specific tier whose intent seeded anything). `CvProfileResolverTest`'s `assetBindingReplacesEveryKnobWholesaleRatherThanOnlyTheOnesThatDifferFromCategory` — the test that pinned the now-false wholesale contract — is deleted, replaced by `organizationInferenceFpsAndAssetModelBothPersistWithCorrectPerKnobSources`, `organizationIntentSeedsFourKnobsAndAssetExplicitConfidenceThresholdWinsOverThem`, and `fourBuiltInShapedProfilesFoldThroughTheResolverByteIdenticalToFoldOntoDirectly`. perception: 846 tests, BUILD SUCCESS. **W7.2** (`6781f533`, persistence): `V36__cv_profile_patch.sql` drops `NOT NULL` on every `cv_profiles` column but `id`/`name`/`description`/`built_in`/`group_id`/the timestamps, adds `ck_cv_profiles_model_pair` (model id/version stay both-or-neither), adds a nullable `intent` TEXT column. `CvProfileEntity`/`CvProfileMapper` widened to match; `tracking` narrows from the 10-field `TrackingConfig` to the 5-field `TrackingKnobPatch`. Disclosed deviation: Jackson 3's `JsonMapper`/Hibernate's auto-detected format mapper fails closed on an unrecognized JSON property rather than ignoring it, so every pre-existing `tracking` jsonb row's extra keys (`lock` etc.) failed to deserialize the moment the narrower type landed — fixed with a scoped `@JsonIgnoreProperties(ignoreUnknown = true)` mix-in registered only for `TrackingKnobPatch`, not a blanket setting. A genuine `Instant.now()` nanosecond-vs-Postgres-millisecond flake in `PersistenceWiringTest`'s own new spot-check fixed the same way `PostgresDockerIntegrationTest` already established (`truncatedTo(ChronoUnit.MILLIS)`). persistence: 286 tests, vision-app: 357 tests, BUILD SUCCESS. **W7.3** (`89776c1c`, vision-api): every `dto.CvProfile*` type rewritten to mirror the nullable-patch domain shape and report per-knob provenance on every read, not only a save response — `CvProfileTrackingResponse`/`CvProfileEventRuleResponse` become null-tolerant with separate `from(...)`/`fromResolved(...)`/effective factories; new 8-field `CvKnobSourcesResponse` nests into `EffectiveCvProfileResponse` alongside a new `intent` field; `CvProfileRequest#toSpec()` becomes a straight unresolved pass-through (intent resolution is the resolver's job now, at fold time — `fieldSources()`/the nested `Sources` are deleted from the request side); `CvProfileResponse.fromEffective(...)` takes every KNOB from the fold's own resolved config, never the matched tier's own possibly-partial raw fields. Disclosed deviation: the old 2-field `CvProfileResponse.Sources` (model/labelFilter) is kept **byte-identical**, not widened in place, because `UpdateStreamConfigRequest`/`UpdateStreamConfigResponse` (the stream-config hot path, out of this wave's write scope) directly reuse its two-argument constructor; a new sibling record, `CvProfileResponse.FieldSources` (4 fields), is what `CvProfileResponse#sources()` is actually typed as now. vision-api: 1121 tests, BUILD SUCCESS (persistence 286, vision-app 357 also re-verified green on the same tree).
  - **W7.4** (`1481b57d`, vision-web): `core/api/models.ts` widened to mirror every W7.0–W7.3 DTO change one for one — `CvProfile`/`CvProfileRequest`/`CvProfileTracking` knobs all optional, `CvProfileFieldSources` (4 fields) replaces the old 2-field `Sources` as `CvProfile#sources`'s own type (that original `Sources`/`CvProfileSources` kept byte-identical, now documented as serving only `PatchStreamConfigResponse`, the stream-config hot path), new `CvKnobSources`/`CvKnobSourceTier` types appended under the required `// CV-ORCHESTRATION W7` banner after the W5b-owned `CvTrace` section (confirmed untouched). `/vision/profiles`' editor (`vision-profiles-logic.ts`/`.ts`/`.html`/`-facade.ts`) reworked so every knob inherits independently, using three deliberately different, documented encodings depending on the knob's own shape: blank text for `model`; a separate `*Inherit` boolean for the two free-text label lists (an explicit `[]` is itself a real, distinct value from "inherit"); a dedicated sentinel value on the `<select>` itself for tracking mode/engine and detection (each already owns a real explicit "off"/"default" distinct from unset). Tracking inherits **per sub-knob**, not as one whole-group toggle, mirroring `TrackingKnobPatch`'s own per-field nullability — a deliberate, disclosed choice, not the only one available. `validateDraft` no longer requires a model at all (a fully-inherited draft is a valid, if inert, patch) and only range-checks knobs that are actually set. `draftToRequest` omits every unset knob. `saveOutcomeMessage`'s toast copy corrected from "resolved from" to "left to" your intent — under the new fold-time-resolution contract a save no longer computes a concrete value, it only flags the knob `INTENT` for the fold to seed later. A downstream ripple in `features/fly/cv-control-panel-logic.ts` (`resolvedConfigFromProfile`) needed fixing too: `EffectiveCvProfile#profile`'s knobs are contractually always concrete post-fold even though `CvProfile`'s own type now allows `undefined` — asserted explicitly (non-null assertions with a doc comment naming the fold guarantee) rather than silently accepting a type error, mirroring the identical Java-side compromise of reusing one DTO for both a raw and an effective read. `npm run test:ci`: 208 files / 4043 tests, all green; both `tsc --noEmit` configs clean.
  - **W7.5** (`d7a064c1`, vision-web): the Tuning modal's `resolvedSourceLine` (`cv-setup-modal-logic.ts`) now reads real per-knob provenance off `EffectiveCvProfile#sources` (`CvKnobSources` — `ASSET`/`CATEGORY`/`ORGANIZATION`/`PLATFORM`/`INTENT` named per knob, on every read) instead of the old coarse fallback (one whole-profile tier reused for every knob's own line); an intent line now names the specific intent ("Resolved from your Vehicles intent") using `EffectiveCvProfile#intent`, data that simply did not exist on this wire shape before W7 — the pre-W7 generic "your intent pick" phrasing is now only the fallback for the case where a knob is flagged `INTENT` with no intent value attached. `confidenceThreshold` is no longer a documented exception (W7.1's resolver now seeds it from an intent too, exactly like `model`/`labelFilter`). Also closed a real, separate wiring gap found in the course of this step: `CockpitFacade#lastConfigSources` has existed since wave W3.3 but was never actually bound to `<vision-cv-setup-modal>` in `cockpit.html`, so `resolvedSourceLine`'s first-priority branch (this session's own most recent hot-knob PATCH) was permanently dead code in production — now wired via one added `[lastConfigSources]` binding. No dedicated `cockpit-facade.spec.ts` was added (`click-to-follow.spec.ts`'s own doc comment already establishes this codebase's precedent: a full `CockpitFacade` harness would dwarf what it measures, test the real production pure functions its methods delegate to instead); `cv-setup-modal-logic.spec.ts` gained a "survives a reload" describe block proving the correct source line comes back from a fresh `effective-profile` GET alone, with no `lastConfigSources` fact present at all — exactly the case the pre-W7 contract could only get right immediately after a save. `npm run test:ci`: 208 files / 4048 tests, all green; both `tsc --noEmit` configs clean.
  - **W7.6** (docs, this entry + `CV-SETTINGS-PLAN.md` §3.1 dated note + `station/vision-web/MODULE.md` dated block). **Final consolidated Java build at HEAD** (`./mvnw -B -pl contexts/vision-perception,storage/persistence,station/vision-api,station/vision-app -am test -DskipWeb`, one command, Docker available so `vision-app`'s Postgres-backed suite ran for real): perception `Tests run: 846, Failures: 0, Errors: 0, Skipped: 0`; persistence `Tests run: 286, Failures: 0, Errors: 0, Skipped: 0`; vision-api `Tests run: 1121, Failures: 0, Errors: 0, Skipped: 0`; vision-app `Tests run: 357, Failures: 0, Errors: 0, Skipped: 0`; `BUILD SUCCESS`. **What stayed undone, named honestly**: this wave did not attempt a live/manual verification against a running deployment (Java+web scoped builds only, per the brief); it did not touch `station/vision-app/MODULE.md`/`contexts/vision-perception/MODULE.md`/`storage/persistence/MODULE.md`/`station/vision-api/MODULE.md` beyond what W7.0–W7.3's own commits already updated — this wave's own W7.6 doc scope is `CV-SETTINGS-PLAN.md`, this file, and `station/vision-web/MODULE.md` only, per the brief. Not merged into `feat/cv-orchestration` by this wave owner — left for the same Fable review-then-merge step every prior wave in this log went through, alongside the sibling `feat/cv-orchestration-w5b-trace-replay` branch (confirmed via `git branch -a --contains 052610ed`/`git log --oneline --all | grep W5b` to carry zero content on this branch — no rebase performed, per the coordinator's explicit "do not rebase; Fable merges").
