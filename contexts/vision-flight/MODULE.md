# vision-flight

Flight sessions, telemetry transport, geofencing, and guarded flight command/manual control.

**What it deliberately does not do**: hold the actual `AssetUsage`/flight-session record (that's `vision-warehouse`'s — this context only *produces* the telemetry samples a usage accumulates, via `TelemetryService`, see below). It does not carry `Telemetry`/`FlightState` as its own types either — both are **`vision-kernel`** records (pure, no ports, read by five contexts). It does not run the video/telemetry ingest pipeline itself (perception's `UsageTracker`/`StreamPipeline` do, subscribing to this context's `TelemetrySourcePort`) and does not resolve who may command what (`VisibilityScope`, checked here as a parameter, lives in `vision-platform`).

**Depends on:** `vision-kernel` (`Telemetry`, `FlightState`, every id, `GeoPosition`…, `VisualFix`/`VisualFixEvidence` — the input `DefaultTrackCorrectionService` gates) · `vision-platform` (`Event`/`EventPublisherPort`/`EventLiveUpdatePort` — `GeofenceMonitor` raises `GEOFENCE_BREACH`, `DefaultTrackCorrectionService` raises `POSITION_DIVERGENCE` via `EventPublisherPort` only, not `EventLiveUpdatePort`, `BatteryMonitor` raises `BATTERY_LOW`, `LinkLossNotifier` raises `LINK_LOST`; `AuditTrailPort`/`AuditEntry`; `VisibilityScope`/`Authority`/`AccessDeniedException` — `Authority` since **AUTH-ROLES-PLAN wave B6**, `VehicleProfileService#probe`/`#captureSnapshot` and `RemediationService#requestMessageInterval`/`#writeParameter`) · **`vision-warehouse`** — a flight command resolves the asset it acts on: `DefaultFlightCommandService` and `DefaultManualControlService` both resolve the target via warehouse's `AssetService#details` before touching a `Device`; `DefaultRemediationService` reads warehouse's `AssetLiveStatePort#latestTelemetry(AssetId)` for the disarmed-only interlock; `DefaultVehicleProfileService` reads warehouse's `AssetService#details` plus **`UsageSessionService#usageBelongsToAsset(UsageId, AssetId)`** — an uncapped boolean membership check, not a foreign repository port; `DefaultReadinessService` reads warehouse's **`MaintenanceQuery#openBlockers(AssetId)`** (WAREHOUSE-UX-CONTEXT.md D6/OQ1 — see Gotchas). No other context.
**Used by:** `vision-perception` (the `UsageTracker`/`GeofenceMonitor` wiring seam, see Gotchas), `vision-api`, `vision-app`, `adapter-mavlink` (`MavlinkVehicleConfigurator` is the `VehicleConfigPort` implementation).
**Build/test:** `./mvnw -B -pl contexts/vision-flight test`

## Section index
| Topic | Heading |
|---|---|
| RC/command domain model (`ChannelMap`, `ControlProfile`, `RcChannels`, geofence zones…) | API surface → `domain.model` — core RC/command |
| Vehicle onboarding domain model (probe/readiness/remediation, `VehicleProfile`, `FeatureRequirement`…) | API surface → `domain.model` — vehicle onboarding |
| Visual-geolocation gating domain model (`TrackCorrection`, `DivergenceRule`) | API surface → `domain.model` — visual-geolocation gating |
| Crew seats domain model (`SeatKind`, `Seat`) | API surface → `domain.model` — crew seats |
| Driven ports (`FlightCommandPort`, `ManualControlPort`, `VehicleConfigPort`…) | API surface → `domain.port` |
| Flight commands + RC relay application services | API surface → `application` (root package) |
| Battery/link-loss alerting monitors | API surface → `application.alerting` |
| Crew seat registry service (`SeatService`) | `application.seat` — the crew seat registry |
| Validation/DI/audit idioms | Conventions |
| Hard-won facts, quirks, deliberate gaps | Gotchas |
| What's real vs. deferred | Status |
| Wave-by-wave narrative, dates, test counts | [`MODULE-HISTORY.md`](MODULE-HISTORY.md) |

## API surface

### `com.drones.vision.flight.domain.model` — core RC/command
- `record ChannelMap(List<ControlBinding> bindings)` — a full axis/button → RC-channel map; `bindings` defensively copied; `RcChannels apply(List<Double> axes, List<Double> buttons)` — a missing/out-of-range/`null` index reads as `0.0` (never throws), assembles channels 1..N (N = highest `rcChannel` any binding targets, or 1 if empty); an unbound channel *below* N is `RcChannels#IGNORE`, channels above N are simply absent. Maps come from `ControlProfile.forKind(...)` — there is no airframe-blind default.
- `enum CommandResult` — `ACCEPTED`/`NO_ACK`, the outcome of a `FlightCommandPort` call; an explicit refusal is deliberately not a value here — see `FlightCommandPort`'s own javadoc (that case is a thrown `IllegalStateException` instead).
- `record ControlBinding(Source source, ControlInputKind kind, ControlFunction function, int sourceIndex, int rcChannel, int minMicros, int centerMicros, int maxMicros, double deadband, boolean reversed)` — one physical control (`enum Source { AXIS, BUTTON }` — *which array the value is read from*) mapped to one RC channel with linear calibration, labelled by what it does (`function`, non-null). Compact ctor rejects a `kind`/`source` pair the hardware cannot produce (`kind.allows(source)`); `rcChannel` must be within `[1,16]` (**FLEET-RADIO R3, F17** — narrowed from `[1,18]`; ArduPilot reads no channel past 16, so a binding on 17/18 is now rejected here, at construction, rather than accepted and silently truncated at the wire). `int toMicros(double normalized)` — `AXIS` maps `[-1,0]`/`[0,1]` piecewise-linearly through `centerMicros`; `BUTTON` maps `[0,1]` straight onto `[minMicros,maxMicros]` (`centerMicros` plays no part in a button's own mapping); a `SWITCH_2`/`SWITCH_3` **quantizes** to its own detents — `LOW → minMicros`, `MIDDLE → centerMicros`, `HIGH → maxMicros` — honouring `reversed` by flipping the *position*, not the number.
  - `enum Travel { CENTERED, UNIDIRECTIONAL }` + `Travel travel()` — **derived, never stored**: `centerMicros == minMicros` *is* a unidirectional control. Rest is `centerMicros`, so a centred control rests mid-travel (a rover's throttle: stop) and a unidirectional one rests at `minMicros` (a copter's throttle: idle).
  - Factories: `centeredAxis(fn, sourceIndex, rcChannel)`, `unidirectionalAxis(...)`, `button(...)`, `switched(fn, source, kind, sourceIndex, rcChannel)` (2-/3-position only); `CENTER_MICROS = 1500`.
- `enum ControlFunction` — `ROLL | PITCH | THROTTLE | YAW | STEERING | AUX_1..AUX_4`, each with `String label()`. `STEERING` is separate from `ROLL` — a ground vehicle steers, it does not bank.
- `record ControlProfile(ControlProfileId id, VehicleKind kind, String code, String displayName, ChannelMap channelMap, ActionMap actionMap)` — the stick-and-switch layout, the only source of a `ChannelMap`. Two compact-ctor invariants: one physical control appears in at most one of the two maps, and no RC channel is driven twice. `withBindings(ChannelMap, ActionMap)`/`copyAs(ControlProfileId, String)` return edited copies; `boolean isBuiltIn()` delegates to the id.
  - `static forKind(VehicleKind)` — the **built-in** profiles, each with `ControlProfileId.builtIn(kind)` and an **empty** `ActionMap`: **COPTER/PLANE** → `AETR`, centred roll/pitch/yaw + **unidirectional throttle on CH3**; **ROVER** → `S-T-`, centred steering (CH1) + centred throttle (CH3), nothing else bound; **UNKNOWN** → code `----`, displayName `"Unidentified vehicle"`, an **empty `ChannelMap`** (**FLEET-RADIO R2** — was four centred axes before this wave; see Gotchas for why empty, not a guess). No built-in binds an aux channel or an action. `forKind` stays **total and non-throwing for every `VehicleKind`** — including `UNKNOWN` — that invariant is unchanged; what changed is that its `UNKNOWN` output is no longer flyable. The real refusal gate for `UNKNOWN` is `DefaultManualControlService#engage` (below), not this factory — this method's own emptiness is defense in depth, not the mechanism.
- `enum ControlInputKind` — `AXIS | BUTTON | SWITCH_2 | SWITCH_3`, each with `label()`, `List<SwitchPosition> positions()` (empty for `AXIS`), `boolean isSwitched()`, `boolean allows(ControlBinding.Source)`. Deliberately separate from `Source`: an EdgeTX 3-position switch arrives on the *axes* array, a latching toggle on the *buttons* array — "what shape is it" and "where is it read from" are two facts. `SWITCH_3` allows only an `AXIS` source.
- `enum SwitchPosition` — `LOW | MIDDLE | HIGH`, ArduPilot's own three, with `label()`, `SwitchPosition reversed()` (`MIDDLE` its own mirror), `int auxFunctionLevel()` = `ordinal()` (`MAV_CMD_DO_AUX_FUNCTION`'s own encoding). `static of(Source, ControlInputKind, double raw)` — the single quantizer every caller shares: a button is `HIGH` at `raw >= 0.5`; a `SWITCH_3` axis is `LOW <= -0.5`, `HIGH >= 0.5`, `MIDDLE` between; anything else splits at `0.0`. `LOW_MICROS = 1200` / `HIGH_MICROS = 1800`.
- `enum ControlAction` — `ARM`, `DISARM`, `TOGGLE_ARM`, `EMERGENCY_STOP`, `RETURN_TO_HOME`, `SET_MODE`, `AUX_FUNCTION`. Each carries `Parameter parameter()` (`NONE | MODE_NAME | AUX_FUNCTION`), `String label()`, `boolean dangerous()` (a domain fact served to clients, not re-invented by them). `void requireValidParameter(String)` rejects a parameter on a `NONE` action, a missing one on the others, a non-numeric/out-of-`[0,400]` `RCx_OPTION` number.
- `record PositionAction(SwitchPosition position, ControlAction action, String parameter)` — one detent's worth of behaviour; blank `parameter` normalizes to `null`, then `action.requireValidParameter` runs. `static of(position, action)` for the parameterless case; `int auxFunctionNumber()` throws unless the action is `AUX_FUNCTION`.
- `record ActionBinding(ControlBinding.Source source, ControlInputKind kind, int sourceIndex, List<PositionAction> positions)` — one physical control and everything it fires. Refuses an `AXIS` kind, an impossible `kind`/`source` pair, an empty list, a position the kind does not have, two actions on the same position. `static pressButton(sourceIndex, action)`; `Optional<PositionAction> actionAt(SwitchPosition)`; `boolean isControl(Source, int)`.
- `record ActionMap(List<ActionBinding> bindings)` — defensively copied; `static empty()`, `Optional<ActionBinding> find(Source, int sourceIndex)`, `boolean isEmpty()`.
- `record ControlProfileId(UUID value)` — `random()`, `of(String)`; `static builtIn(VehicleKind)` — a **derived, stable** id (UUIDv3 over `"vision:control-profile:built-in:<KIND>"`) so a built-in can be named on the wire without being stored; `boolean isBuiltIn()`.
- `record OwnedControlProfile(UserId owner, ControlProfile profile, boolean active, Instant updatedAt, TransmitterView view)` (docs/plans/active/CONTROLLER-SETUP-CONTEXT.md C6, fifth component added by C15) — a saved profile, who owns it, and how their radio is arranged. **Refuses to wrap a built-in** (`copyAs` it first — built-ins are the floor everyone falls back to). `id()`/`kind()` delegate. A **4-argument convenience constructor** delegates to `TransmitterView.DEFAULT` — the view is a display fact, and every construction site that predates C15 had no opinion to state.
- `record TransmitterView(int stickMode, boolean forwardIsUp)` (C15) — how the operator's transmitter is *laid out*: which stick carries throttle (mode 1-4, rejected outside that range in the compact constructor) and whether pushing a stick forward reads positive. `DEFAULT` is mode 2, forward-up. It lives on `OwnedControlProfile` and **deliberately not on `ControlProfile`**: it changes a picture, never a channel, so the record a live session engages must not carry it. It is on the profile rather than in browser storage because it describes the operator's own radio, which follows them between machines.
- `enum VehicleKind` — `COPTER | PLANE | ROVER | UNKNOWN`; a *control* taxonomy, not a catalogue — `ROVER` covers boats, `UNKNOWN` is never resolved to a guess. **Deliberately not widened for FLEET-RADIO R2** — see `UnidentifiedReason` immediately below for why the operator-facing "why is it UNKNOWN" distinction lives in a separate, orthogonal type instead of a wider `VehicleKind`.
- `enum UnidentifiedReason` — `UNSUPPORTED_VEHICLE | NOT_A_VEHICLE | NEVER_IDENTIFIED` (**FLEET-RADIO R2**) — *why* a link's `vehicleKind()` came back `UNKNOWN`, kept orthogonal to `VehicleKind` itself so `ControlProfile`/`ChannelMap` resolution stays a pure 4-way control-shape switch while the operator still gets a true reason. `UNSUPPORTED_VEHICLE`: a recognized-but-unflyable airframe (e.g. a submarine, or any future `VehicleClass` this platform doesn't fly/drive) — the remedy is "this platform cannot do this, not now, maybe never". `NOT_A_VEHICLE`: the heartbeating device is an instrument sharing the link (a gimbal, a ground station, an ADS-B transponder) — the remedy is "you commanded the wrong thing, not a vehicle at all". `NEVER_IDENTIFIED`: a genuinely unseen `MAV_TYPE` — the remedy is "an operator who can see the aircraft may proceed by choosing its kind explicitly" (the only one of the three where a manual override is a sane next step). See `ManualControlLink#unidentifiedReason()` and `DefaultManualControlService#engage` below for where this is produced and consumed.
- `record FlightCapability(boolean commandable, boolean armSupported, boolean modeSelectSupported, List<String> selectableModes, VehicleKind vehicleKind)` — a best-effort snapshot of what flight commands a device's aircraft accepts; `armSupported`/`modeSelectSupported` never `true` when `commandable` is `false`; `static notCommandable()` = all-false + empty list + `UNKNOWN`.
- `record GeofenceZone(ZoneId id, String name, ZoneKind kind, List<GeoPosition> polygon, Double maxAltitudeMeters, boolean enabled)` — polygon ≥3 vertices, implicitly closed; `maxAltitudeMeters` nullable, non-negative if present; zones are global (no per-asset/per-group scoping). `boolean contains(GeoPosition)` — ray-casting point-in-polygon treating lat/lon as planar Cartesian (see Gotchas). `withEnabled(boolean)`, `withDetails(...)` return copies.
- `record GeofenceZoneEvent(Action action, GeofenceZone zone)` (LIVE-POLL-RETIREMENT-PLAN.md §3 D2, wave L3) — the payload `GeofenceLiveUpdatePort#publishZoneEvent` carries; nested `enum Action { CREATED, UPDATED, DELETED }`. Compact ctor rejects a null `action`/`zone`. For `DELETED`, `zone` is the **last-known** zone (the caller — `DefaultGeofenceService#delete` — captures it before removal), never a stub carrying only the id, so a subscriber can render "zone X was deleted" without a follow-up lookup.
- `record RcChannels(List<Integer> microsByChannel)` — one 1-based microsecond value per RC channel, the payload `ManualControlPort` relays as a MAVLink `RC_CHANNELS_OVERRIDE` (#70) frame; constants `RELEASE=0`, `IGNORE=0xFFFF`, `MIN_MICROS=1000`, `MAX_MICROS=2000`, `MAX_CHANNELS=16`; `static released(int channelCount)`. **FLEET-RADIO R3 (F17)**: accepts at most `MAX_CHANNELS` (16) entries — ArduPilot's own RC-override handling reads no channel past 16, even though the wire message has 18. The domain's `RELEASE`/`IGNORE` sentinels are uniform across every channel here; the *wire* re-encoding that channels 9+ actually need (F4) is `mavlink-core`'s concern, not this module's — see that module's `RcChannels#wireValue` and its own Gotchas for why, and this module's Gotchas below for why no equivalent method was added here.
- `record ZoneId(UUID value)` — `static random()`, `static of(String)`.
- `enum ZoneKind` — `KEEP_IN | KEEP_OUT`, read by `GeofenceMonitor` to decide what "inside"/"outside" a zone's polygon means for breach purposes.

### `com.drones.vision.flight.domain.model` — vehicle onboarding (probe/readiness/remediation, docs/plans/active/DRONE-ONBOARDING-PLAN.md)
- `enum FlightPhase` — `PREFLIGHT | IN_FLIGHT | LINK_LOST | POSTFLIGHT | ABANDONED | CLOSED`, the frozen §8.1 wire spelling (enum name **is** the wire string); `IDLE` deliberately absent — that is "no `AssetUsage` exists yet", not a phase to stamp on one.
- `final class FlightPhaseRule(Duration silenceWindow, Duration abandonWindow)` — the pure phase-transition function; ctor validates both positive and `abandonWindow >= silenceWindow`. `FlightPhase nextPhase(FlightPhase currentPhase, FlightState state, Duration linkAge, int streamCount)` — the sample-driven half; reads `state.armed()` via `Boolean.TRUE/FALSE.equals(...)`, never unboxes (`armed == null` is never "false"); `streamCount` only gates the never-armed `PREFLIGHT`/`POSTFLIGHT` → `CLOSED` transition. `FlightPhase onSessionClosed(FlightPhase currentPhase)` — the explicit-close half (`IN_FLIGHT`/`LINK_LOST` → `ABANDONED`, everything else → `CLOSED`); **two entry points, not one function**, because "session closed while armed" fires when the platform stops observing, not on a telemetry sample — see Gotchas.
- `record VehicleProfile(String linkKey, Instant observedAt, Integer sysid, String firmware, String firmwareVersion, String vehicleKind, Long capabilityBitmask, List<String> capabilityFlags, List<MessageObservation> messages, List<ParameterReading> parameters, Long linkBytesPerSecond, boolean complete, String incompleteReason)` — the PROBE stage's observed snapshot, keyed by `linkKey` (pre-registration identity, e.g. `"udp://0.0.0.0:14550#7"`) rather than `DeviceId`; `complete == false` requires a non-blank `incompleteReason` and vice versa ("probably fine, didn't check" cannot be represented); `capabilityBitmask` deliberately **not** validated non-negative (a `uint64` high bit is a legitimate negative `long`); null lists normalize to empty.
- `record MessageObservation(int messageId, String name, double hz, long count)` — one passively-inventoried message rate; `name` nullable (unresolved dictionary entry, still counted).
- `record ParameterReading(String name, double value, String type)` — one parameter actually read; only what was actually read — an unanswered request has no entry, never a fabricated zero.
- `final class ParameterAliases` (static) — the same vehicle parameter under the names successive firmware generations give it. `static String canonical(String name)` (first spelling of its group, else `name` itself), `static boolean sameParameter(String a, String b)` (`false` if either is null), `static Set<String> spellingsOf(String name)` (every spelling incl. itself; a singleton for an unaliased name). Groups today: `SYSID_THISMAV`/`MAV_SYSID` and `SYSID_MYGCS`/`MAV_GCS_SYSID` (both renamed by ArduPilot 4.7). Compared at the *comparison*, never by rewriting a `ParameterReading` — see Gotchas.
- `enum ParameterTier { A, B, C }` — `static Optional<ParameterTier> classify(String parameterName)` canonicalises through `ParameterAliases` first, then matches a seeded regex allowlist (representative, not verified-complete — see Gotchas); `boolean everWritable()` — `false` only for `C`. The allowlist is domain code, not a UI convention.
- `record FeatureRequirement(String featureKey, String label, String firmware, Integer requiredMessageId, String requiredMessageName, Double minimumHz, String requiredParameterName, Double requiredParameterValue, Long forbiddenParameterBits)` — one seeded requirement row; `public static final Set<String> FEATURE_KEYS` is the frozen §8.1 eleven-key set, enforced in the compact constructor; `requiredMessageId` present ⇒ `requiredMessageName` non-blank and `minimumHz` non-negative, both required together; a row with neither message nor parameter requirement is valid (e.g. `command-tx`). **FLEET-RADIO R6**: `requiredParameterValue` (must equal exactly, small float tolerance) and `forbiddenParameterBits` (value, rounded to a `long`, must have none of these bits set) generalize `requiredParameterName` from presence-only to value-aware; both are independently nullable but neither may be set without `requiredParameterName`, and `forbiddenParameterBits` must not be negative — enforced in the compact constructor.
- `enum FeatureStatus { READY, DEGRADED, MISSING, UNKNOWN }`, `enum RemedyKind { MESSAGE_INTERVAL, PARAM_WRITE, CLI_SCRIPT, MANUAL }`, `enum ReadinessVerdict { GO, NO_GO, UNKNOWN }` — frozen §8.1 wire spellings, enum name = wire string.
- `record FeatureReadiness(String featureKey, String label, FeatureStatus status, String detail, RemedyKind remedy)` — one `ReadinessReport` row; `featureKey` validated against `FeatureRequirement.FEATURE_KEYS`; `detail` never blank; `remedy` nullable.
- `record ReadinessReport(AssetId assetId, ReadinessVerdict verdict, Instant evaluatedAt, Instant profileObservedAt, List<FeatureReadiness> features, List<String> blockers)` — `profileObservedAt` is `null` when never probed (an honest, renderable `UNKNOWN`, not a 404).
- `enum RemediationResultCode { ACCEPTED, DENIED, NO_ACK, UNSUPPORTED }` — frozen §8.1 `RemediationResult.actions[].outcome` spelling, gives `VehicleConfigPort`'s write/message-interval methods an honest return type.
- `record MessageIntervalOutcome(int messageId, Duration interval, RemediationResultCode outcome, String detail)` — Mechanism A's outcome; not a write, nothing persists past the session.
- `record ParameterWriteOutcome(String parameterName, RemediationResultCode outcome, Double previousValue, Double newValue, String detail)` — Mechanism B's outcome; a read-back that differs from the requested value is `DENIED` with `previousValue` set for the caller to restore to, never `ACCEPTED`.
- `record ParameterDrift(String parameterName, double previousValue, double currentValue, Instant previousObservedAt, Instant currentObservedAt)` — one changed parameter between two `VehicleProfile` snapshots; ctor rejects a blank name, a null timestamp, `currentObservedAt` before `previousObservedAt`, and `previousValue == currentValue` — cannot represent a non-change.
- `final class ConfigDriftCalculator` (pure, private ctor) — `static List<ParameterDrift> diff(VehicleProfile earlier, VehicleProfile later)`: throws `IllegalArgumentException` if `later.observedAt()` precedes `earlier.observedAt()`. Compares parameters present **in both** snapshots by name; present in only one is never reported as drift (absence of evidence is not evidence of a change); unchanged value produces no row. Sorted by `parameterName`.
- `record FlightPassport(UsageId usageId, AssetId assetId, VehicleProfile preflightProfile, VehicleProfile postflightProfile)` — read model, not persisted as its own row; both profiles nullable — a passport for a flight whose snapshot was never captured is still a valid, renderable passport with a `null` half.

### `com.drones.vision.flight.domain.model` — visual-geolocation gating (docs/plans/done/VISUAL-GEO-V2-PLAN.md §3.5/§4.3/§4.5)
- `enum CorrectionStatus { CONFIRMED, PROBABLE, NO_FIX }` — the Java-side §4.3 gate's verdict on one fix.
- `enum CorrectionSource { VISUAL_HEAVY }` — one value today (the HEAVY-A pipeline); a placeholder for a future lighter-weight source.
- `record TrackCorrection(AssetId assetId, UsageId usageId, Instant frameAt, Instant computedAt, CorrectionStatus status, CorrectionSource source, GeoPosition position, Double yawDegrees, Double radiusMeters, Double impliedAglMeters, GeoPosition rawPosition, Double separationMeters, Double sigmaMeters, boolean divergent, Instant divergentSince, String regionId, String tileId, String refusal, VisualFixEvidence evidence)` — one gated, persisted, published correction; `position`/`refusal` mutually exclusive exactly like `VisualFix`; `position.altitudeMeters()` must be `null` (2-D only); `divergent == (divergentSince != null)`, enforced in the compact ctor; `radiusMeters`/`separationMeters`/`sigmaMeters` non-negative if present.
- `final class DivergenceRule` (public, no interface) — the §4.5 divergence alarm state machine, standalone from `DefaultTrackCorrectionService` so its numeric semantics are unit-testable in isolation.
  - `DivergenceRule(double sigma, int consecutiveFixes, Duration clearAfter)` — `sigma` positive finite, `consecutiveFixes` ≥1, `clearAfter` positive.
  - `Outcome evaluate(AssetId, CorrectionStatus, Double separationMeters, Double sigmaMeters, Instant now)` — only a `CONFIRMED` status with both separation/sigma present participates; "qualifying" means `separationMeters > sigma * sigmaMeters`; a non-participating call reports the asset's *current* latched state back unchanged (`risingEdge=false`). State machine: Quiet→(qualifying)→Arming→…→Alarm at the Nth consecutive qualifying fix (rising edge, reported once)→(non-qualifying, `clearAfter` elapses)→Quiet; a non-qualifying fix while merely arming resets the count. Clear-after is lazily evaluated, only re-checked on the next `CONFIRMED` correction for that asset. State is in-heap only, resets on restart — the same honest limitation `GeofenceMonitor`'s breach state carries.
  - `record Outcome(boolean divergent, Instant divergentSince, boolean risingEdge)` (nested) — `divergentSince` non-null iff `divergent`; `risingEdge` requires `divergent`.

### `com.drones.vision.flight.domain.model` — crew seats (docs/plans/active/CREW-CONTROL-PLAN.md §3.1, wave W1)
- `enum SeatKind { FLIGHT, CAMERA }` — which of an asset's two seats. **No ordinal semantics** — nothing may switch/compare/rank on declaration order, each value's meaning comes only from its name. `FLIGHT` guards the verbs that command the aircraft; `CAMERA` guards the verbs that command the camera/CV surface.
- `record Seat(AssetId assetId, SeatKind kind, UserId holder, Instant acquiredAt, Instant expiresAt)` — one in-heap, time-limited hold; compact ctor rejects any null field and an `expiresAt` before `acquiredAt` (equal is allowed). Deliberately not a database row and not a field on `AssetUsage` — see `application.seat.DefaultSeatService`'s own javadoc and the plan's "three rejected placements" for why a durable holder is the wrong, dangerous shape. `UserId` is `vision-kernel`'s own type — already reachable from this module's existing `AssetId`/`Telemetry`/every-id dependency, no new dependency needed.

### `com.drones.vision.flight.domain.model` — link pairing (LINK-PAIRING-PLAN.md §3.4 frozen contract)
Local mirror types, not a direct import of `mavlink-core`'s `com.drones.mavlink.transport`/
`com.drones.mavlink.session` types — **mandatory, not a deviation from the frozen contract's literal
code snippets**: ArchUnit's `domainDependsOnlyOnDomainAndJava`/
`applicationDependsOnlyOnApplicationDomainAndJava` rules (`station/vision-app`'s `ArchitectureTest`)
forbid any class in this package (or `application`) from depending on `com.drones.mavlink..` at all —
same precedent as `RcChannels`'s own local mirror (see `mavlink-core`'s MODULE.md Gotchas). The
one-way translation between the two type systems happens only in `drone-link/mavlink`'s
`MavlinkVehicleLinkPort` (see that module's own MODULE.md).
- `record LinkId(String value)` — a link's stable identity; blank rejected.
- `enum CarrierKind { UDP, SERIAL }` — which physical/logical technology a link rides on.
- `enum SerialRole { NONE, GROUND_RADIO, BENCH }` — what a `SERIAL` link is for; `NONE` for a `UDP` link. `GROUND_RADIO` is eligible for automatic election; `BENCH` (a bench/USB debug cable) never is, reachable only via an operator pin.
- `record LinkQuality(Instant lastRadioStatusAt, Integer rssi, Integer remoteRssi, Integer noise, Integer rxErrors, Boolean fixed)` — last known `RADIO_STATUS`-derived reading for one link; every field nullable (no reading has ever arrived, or the field itself is unsupported by that radio).
- `record LinkView(LinkId id, CarrierKind carrier, SerialRole serialRole, String label, boolean active, boolean receiving, Duration heartbeatAge, LinkQuality quality, DeviceId deviceId)` — one link's election-relevant state. `active` — this is the group's current ACTIVE link; `receiving` — heard the vehicle within the configured soft timeout. `deviceId` is **additive beyond the frozen contract** (this wave's own task brief): the web contract assumed exactly one paired telemetry device per asset; tagging each link with its own `DeviceId` lets a multi-device asset's links be told apart without renaming or removing anything the web already reads.
- `record LinkGroupView(AssetId assetId, List<LinkView> links, LinkId activeLinkId, boolean pinned, Instant lastFailoverAt)` — a point-in-time view of one asset's paired links and which (if any) is ACTIVE. `activeLinkId` is `null` if none (hard timeout with no healthy alternative, or never heard from at all); `lastFailoverAt` moves only on an **automatic** election, never on an operator pin/release.
- `record CarrierView(LinkId id, CarrierKind carrier, SerialRole serialRole, String label, int priority)` — one entry of the **station-wide** carrier directory (`GET /api/carriers`, §7 ruling 5) — a carrier belongs to no single asset, so this carries no `AssetId`/election state at all, unlike `LinkView`.

### `com.drones.vision.flight.domain.port` (driven — implemented by adapters)
- `FlightCommandPort` (**the one deliberate place the RX-only doctrine, `docs/main/CYCLES-PLAN.md` §0, is broken**): `boolean supports(Device)`; six blocking commands, each sending one MAVLink command and waiting briefly for the aircraft's own ack — `CommandResult setMode(Device, String modeName)`, `CommandResult arm(Device, boolean force)`, `CommandResult disarm(Device, boolean force)`, `CommandResult returnToHome(Device)`, `CommandResult emergencyStop(Device)` (**vehicle-kind-dependent as of FLEET-RADIO R4b** — force-disarm on `COPTER`/`PLANE`/an unidentified kind, *an airborne vehicle will fall*; `MAV_CMD_DO_SET_MODE` into ArduRover's `Hold` on `ROVER`, never a disarm — see Gotchas and `drone-link/mavlink`'s own MODULE.md for the full rationale), `CommandResult auxFunction(Device, int function, int level)` (`MAV_CMD_DO_AUX_FUNCTION` 218; `level` 0/1/2, IAE outside) — plus one non-blocking, never-throwing `FlightCapability capabilities(Device)`. `arm`/`disarm`'s `force` uses the MAVLink `21196` magic value. Throws `IllegalArgumentException` for an unsupported device/firmware or unknown mode; `IllegalStateException` when the aircraft explicitly refuses; a lost/missing ack is neither (`CommandResult#NO_ACK`). One real implementation: `drone-link/mavlink`'s `MavlinkFlightCommander`.
- `ControlProfileRepositoryPort`: `List<OwnedControlProfile> findAllByOwner(UserId)`; `Optional<OwnedControlProfile> findById(ControlProfileId)`; `Optional<OwnedControlProfile> findActive(UserId, VehicleKind)`; `void save(OwnedControlProfile)` upsert; `void activate(UserId, ControlProfileId)` — exactly one active profile per (owner, kind), implementation clears siblings in the same transaction; `void delete(ControlProfileId)` idempotent.
- `GeofenceRepositoryPort`: `GeofenceZone save(GeofenceZone)` upsert; `Optional<GeofenceZone> findById(ZoneId)`; `List<GeofenceZone> findAll()` snapshot, global; `void deleteById(ZoneId)` idempotent.
- `GeofenceLiveUpdatePort` (LIVE-POLL-RETIREMENT-PLAN.md §3 D2, wave L3) — one method, `void publishZoneEvent(GeofenceZoneEvent event)`; must not throw, must return quickly (same contract idiom as `TelemetryLiveUpdatePort`/`TrackCorrectionLiveUpdatePort`). Follows the one-port-per-context idiom exactly like `MapLiveUpdatePort`/`MapEvent` in `contexts/vision-map` — zones deliberately do **not** ride the `map` topic, since no `map -> flight` architecture edge exists (`ContextArchitectureTest`, vision-app) and zones are this context's own concept. One real implementation: `station/vision-api`'s `LiveUpdateRegistry` (the same class implementing every other `*LiveUpdatePort`); `station/vision-app`'s `NoopLiveUpdatePublisher` is the disabled-live-updates fallback.
- `VehicleLinkPort` (LINK-PAIRING-PLAN.md §3.4 frozen contract) — `LinkGroupView linksFor(AssetId)`; `void pin(AssetId, LinkId)` — operator override, `IllegalArgumentException` if `linkId` is not one of this asset's known links; `void release(AssetId)` — idempotent, hands control back to automatic election. **(LINK-PAIRING-PLAN.md §8 defect #5, new)** `void onGroupChanged(Consumer<AssetId> listener)` — subscribes (not one-shot) to spontaneous election changes the adapter discovers off its own thread (a sighting or a forget that actually moved the active link), so a live-transport client sees a failover without anyone having to GET the route first; not fired for an operator `pin`/`release` (those are already synchronously re-observed by their own call chain) or for a no-op re-sighting. Implementations must never let a listener's exception escape onto the calling thread. One real implementation: `drone-link/mavlink`'s `MavlinkVehicleLinkPort` (see that module's own MODULE.md).
- `CarrierDirectoryPort` (LINK-PAIRING-PLAN.md §3.4/§7 ruling 5) — `List<CarrierView> carriers()`, **station-wide**, not per-asset: "what carriers exist on this station at all", independent of any sysid's election state — the domain-side half of `GET /api/carriers`. One real implementation: `drone-link/mavlink`'s `MavlinkVehicleLinkPort` (the same class as `VehicleLinkPort` above — one adapter class, two related ports, mirroring `LiveUpdateRegistry`'s own precedent).
- `LinkStateLiveUpdatePort` (LINK-PAIRING-PLAN.md §3.4/§4 row L3) — one method, `void publishLinks(AssetId, LinkGroupView)`; must not throw, must return quickly — same contract idiom as `GeofenceLiveUpdatePort` above, backing the `links:<assetId>` live topic. One real implementation: `station/vision-api`'s `LiveUpdateRegistry` (the eighth `*LiveUpdatePort` it implements); `station/vision-app`'s `NoopLiveUpdatePublisher` is the disabled-live-updates fallback.
- `ManualControlLink` — opaque, adapter-owned handle to one engaged `ManualControlPort` relay link; `boolean active()` · `VehicleKind vehicleKind()` — what the vehicle behind the link most recently reported itself to be, read live at `engage` and never stored; an implementation that cannot tell must return `UNKNOWN` · `int rateHz()` — the link's own keepalive cadence, resolved at `engage` and fixed for the link's life; a **floor, not a ceiling**; exists so `vision-api` can report the real rate without depending on `adapter-mavlink`. Not a SAM — a lambda no longer compiles. `default Optional<UnidentifiedReason> unidentifiedReason()` (**FLEET-RADIO R2**) — empty unless `vehicleKind() == UNKNOWN`; the **default implementation** returns `Optional.of(NEVER_IDENTIFIED)` whenever it is, so every pre-R2 implementer (test doubles included) keeps compiling and keeps refusing correctly without an override. Only `drone-link/mavlink`'s real adapter overrides it, to report `UNSUPPORTED_VEHICLE`/`NOT_A_VEHICLE` where its own `VehicleClass` classification can tell the finer story.
- `ManualControlPort` — a streaming, ack-less RC-channel-override relay, the fire-and-forget opposite of `FlightCommandPort`'s request→ack one-shots: `boolean supports(Device)`; `ManualControlLink engage(Device)` — opens a link + starts a fixed-rate sender thread, throws `IllegalArgumentException` if unsupported/unreachable; `void send(ManualControlLink, RcChannels)` — latest-wins mailbox, non-blocking; `void release(ManualControlLink)` — release-sentinel burst then stops the sender. Real implementation: `drone-link/mavlink`'s `MavlinkManualControlSender`.
- `TelemetryLiveUpdatePort`: `void publishTelemetryAppended(AssetId, Telemetry)` — announces one sample appended to an asset's open usage; must not throw, must return quickly.
- `TelemetryRepositoryPort`: `void save(UsageId, Telemetry)` append-only; `List<Telemetry> findByUsage(UsageId, int limit)` — returns the **earliest** samples, not latest, unchanged (see `vision-events`' Gotchas for the consequence — `DefaultReplayService` genuinely wants this window); `List<Telemetry> findLatestByUsage(UsageId, int limit)` (COMMAND-MAP-FLOW-PLAN.md B1/D1) — the **most recent** `limit` samples, always returned ascending by `at` like every other read from this port. `AssetController`'s `GET /api/usages/{usageId}/telemetry` now serves from this method, fixing the `/command` fleet map's freeze-after-N-samples defect; the wire contract is unchanged.
- `TelemetrySourcePort`: `boolean supports(Device)`; `Flow.Publisher<Telemetry> open(Device)` — per-open, hot/live, latest-wins backpressure; `void close(DeviceId)` idempotent.
- `VehicleConfigPort` — talks to a vehicle over its own protocol for onboarding; every method takes a `String linkKey` (`"udp://host:port#sysid"`), **not** a `Device` — the pre-registration probe runs before any `DeviceId` exists. `boolean supports(Device)`; `VehicleProfile probe(String linkKey, Duration window)` — never throws for an incomplete answer; `MessageIntervalOutcome requestMessageInterval(String linkKey, int messageId, Duration interval)` — Mechanism A, not a write; `List<ParameterReading> readParams(String linkKey, List<String> parameterNames)`; `ParameterWriteOutcome writeParam(String linkKey, String parameterName, double value)` — Mechanism B, snapshot-before/read-back-after is the implementation's job. One expected implementation: `adapter-mavlink`'s `MavlinkVehicleConfigurator`, riding the same shared socket `MavlinkHeartbeatScanner` already borrows.
- `VehicleProfileRepositoryPort`: `void save(DeviceId, VehicleProfile)` append-only, never overwrites; `Optional<VehicleProfile> findLatest(DeviceId)` — keyed by `DeviceId`; `void save(DeviceId, UsageId, FlightPhase, VehicleProfile)` — the tagged variant `captureSnapshot` uses, still append-only; `Optional<VehicleProfile> findByUsageAndPhase(UsageId, FlightPhase)` — "the newest tagged row for this usage+phase".
- `FeatureRequirementRepositoryPort`: `List<FeatureRequirement> findByFirmware(String firmware)` — empty for a never-seen firmware, read as "every feature `UNKNOWN`", not an error; `List<FeatureRequirement> findAll()`.
- `TrackCorrectionRepositoryPort`: `void save(TrackCorrection)` append-only, never updated in place (the `track_corrections` table is excluded from the audit trigger, telemetry-character, ~1Hz per flying asset); `List<TrackCorrection> findByUsage(UsageId, int limit)` oldest→newest, bounded; `Optional<TrackCorrection> findLatest(AssetId)` freshest across every usage; `int deleteOlderThan(Instant)` / `int trimUsageToMostRecent(UsageId, int maxRows)` — the composition-layer runner's own retention mechanisms; neither is called by `DefaultTrackCorrectionService` itself, only `deleteOlderThan` via `prune`.
- `TrackCorrectionLiveUpdatePort`: `void publishCorrection(AssetId, TrackCorrection)` — mirrors `TelemetryLiveUpdatePort`'s contract exactly; a driving adapter pushes this to the `geo:<assetId>` live topic.

### `application` (root package — flight commands + RC relay)
- `FlightCommandService` (interface) → `DefaultFlightCommandService` — guarded flight command TX, behind `vision-api`'s `FlightCommandController`
  - `DefaultFlightCommandService(AssetService, FlightCommandPort, AuditTrailPort, ReadinessService)` — `AssetService` is warehouse's. **`ReadinessService` is a new required 4th collaborator (ASSET-FLOWS-PLAN S1)** — every call site must pass one; no default.
  - `CommandResult returnToHome(AssetId, UserId actor, VisibilityScope scope)` — `!scope.includes(asset)` → 403 (not the 404 a scoped *read* returns) **and the denial is audited**; resolves the asset via `AssetService#details` (404 unknown); picks the first **active** device `FlightCommandPort#supports` claims (zero matches → 409 naming the asset); the port's own `IllegalArgumentException` is caught and rethrown as `IllegalStateException` with the identical message so `vision-api`'s existing 409 mapping applies without a special case; the port's own `IllegalStateException` propagates unchanged. One `AuditEntry` per actual command attempt — resolving *no* commandable device is not an attempt and is not audited.
  - `CommandResult setMode(AssetId, String modeName, UserId, VisibilityScope)` / `disarm(..., boolean force, ...)` — each mirrors `returnToHome` via shared private helpers `resolveForCommand`/`sendAndAudit`. **Unknown-mode 400 vs not-commandable 409, the deliberate split**: `setMode` validates the mode against `capabilities(device).selectableModes()` **before** the port is called — unknown mode for a `modeSelectSupported()` vehicle → plain `IllegalArgumentException` (400), thrown outside `sendAndAudit`'s try/catch and **not audited**; a not-commandable vehicle skips the mode check entirely and stays 409.
  - **`CommandResult arm(AssetId, boolean force, UserId, VisibilityScope)` — ASSET-FLOWS-PLAN S1, maintenance-grounding gate.** Goes through `resolveScopedAsset` (scope check, audited `DENIED:out of scope` on denial) → `requireNotMaintenanceGrounded` → `commandableDevice`, in that order, so a grounded asset's port is never touched — `resolveForCommand` itself is now just `commandableDevice(resolveScopedAsset(...), assetId)` and every other command method still calls it unchanged. `requireNotMaintenanceGrounded` calls `readinessService.evaluate(assetId, scope)` and filters `report.blockers()` for the `DefaultReadinessService.MAINTENANCE_BLOCKER_PREFIX` idiom **`DefaultManualControlService#engage` already uses** (same collaborators, same prefix constant); any survivor → audited `REFUSED:maintenance-grounded`, `IllegalStateException` (409) naming every blocking record's own summary. **Never gated**: `disarm`/`emergencyStop`/`returnToHome`/`setMode` — energy-reducing/recovery verbs must stay available on a vehicle that may already be airborne (plan §2 rationale); `DefaultFlightCommandServiceTest` asserts all four still work on a grounded asset.
  - `FlightCapability capabilities(AssetId, VisibilityScope scope)` — a **READ**: uses the scoped `AssetService#details(scope, assetId)` so unknown/out-of-scope both surface as 404; an in-scope asset with no commandable device returns `FlightCapability.notCommandable()`; never audited.
  - `CommandResult emergencyStop(AssetId, UserId, VisibilityScope)` / `CommandResult auxFunction(AssetId, int function, int level, UserId, VisibilityScope)` — same scope gate, same audit shape. `emergencyStop` is *not* folded into `disarm(force=true)`: a trail that cannot tell a considered disarm from a panic stop cannot answer the question anyone asks afterwards. **FLEET-RADIO R4b:** this method itself needed no change — it stays a thin `resolveForCommand`/`sendAndAudit` pass-through; the vehicle-kind decision (force-disarm vs. ArduRover `Hold`) is made entirely inside `drone-link/mavlink`'s `MavlinkFlightCommander`, which already has the `mavType` this decision needs and this service does not.
- `ControlProfileService` (interface) → `DefaultControlProfileService(ControlProfileRepositoryPort[, Clock])` (docs/plans/active/CONTROLLER-SETUP-CONTEXT.md C6) — the operator's own saved layouts. `List<ControlProfile> builtIns()`; `List<OwnedControlProfile> saved(UserId)`; `ControlProfile activeFor(UserId, VehicleKind)` — **never empty**: the active saved profile if there is one, else `ControlProfile.forKind(kind)`; `create(UserId, VehicleKind, String name)` copies the built-in for that kind (a saved profile is an edited copy, never a blank slate); `update(UserId, ControlProfileId, name, ChannelMap, ActionMap, TransmitterView)` (C15 — a `null` view reads as `TransmitterView.DEFAULT`); `activate`; `delete`. Editing/deleting a built-in throws `IllegalArgumentException`; **activating one does not** — it means "stop using my saved layout for this kind", clears the owner's active flag and returns, which is precisely the state `activeFor` already falls back from; touching another operator's profile throws `AccessDeniedException`. **No `VisibilityScope` parameter anywhere** — a control layout is personal equipment configuration, ownership *is* the whole authority model.
- `ControlProfileSelector` — a one-method `@FunctionalInterface` (`ControlProfile forSession(UserId actor, VehicleKind kind)`) with `static builtInOnly()`. Exists so `DefaultManualControlService` can resolve a saved layout **without depending on `ControlProfileService`**: engage needs one profile, not a CRUD surface, and the app wires the real service in as a lambda.
- `ManualControlService` (interface) → `DefaultManualControlService`, plus `ManualControlSession` (interface, one private `Default*`-style implementation) and `WatchdogListener` (interface) — a guarded, stateful, watchdog-supervised RC-channel-override relay session.
  - `ManualControlSession engage(AssetId, UserId actor, VisibilityScope scope, WatchdogListener onWatchdog)` — same scope-gate/device-resolution/audit idiom as `DefaultFlightCommandService`; resolves the first active device `ManualControlPort#supports` claims; the port rejecting an unreachable device is rethrown as `IllegalStateException`, unaudited. **FLEET-RADIO R2 — refuses an unidentified vehicle, after the link is already open**: once `manualControlPort.engage(device)` succeeds, if the resulting link's `vehicleKind() == VehicleKind.UNKNOWN`, the service releases that link immediately (`manualControlPort.release(link)`), audits `REFUSED:unidentified-vehicle:<reason>`, and throws `VehicleUnidentifiedException` (a `RuntimeException` in `application`, `reason()` one of `UnidentifiedReason`'s three values) carrying a reason-specific, operator-facing message — never a session. The link must be opened first because `vehicleKind()`/`unidentifiedReason()` are only known live, from the vehicle's own heartbeat; there is no way to ask "what kind is this" without engaging. A release that itself throws cannot displace the refusal: it is attached to the `VehicleUnidentifiedException` as a **suppressed** cause, so the operator still gets "unidentified vehicle" rather than the port's transport error, and the refusal is still audited — which it would not be if the release propagated.
  - **FLEET-RADIO R6 — refuses a silently misconfigured vehicle too, before any device is resolved or any link opened**: right after the scope gate, `engage` calls `readinessService.evaluate(assetId, scope)` **once** and reuses that single `ReadinessReport` for two independent gates (below) — no live link needed for either, both re-derived from the vehicle's already-probed `VehicleProfile`/warehouse state, never trusted from an earlier preflight read since either can change in between:
    - **WAREHOUSE-UX W5 — `requireNotMaintenanceGrounded`, checked first.** If `report.blockers()` carries any `DefaultReadinessService.MAINTENANCE_BLOCKER_PREFIX`-prefixed entry, `engage` audits `REFUSED:maintenance-grounded` and throws `IllegalStateException` naming every blocking record's own summary. This is the "may this asset fly / engage" predicate WAREHOUSE-UX-CONTEXT.md's OQ1 asks for; a manager's `AssetCustodyService#release` (warehouse) clears it in one click. **Does not gate** `DefaultFlightCommandService#arm`/`disarm` (the MAVLink command path) or perception's `UsageTracker` (which opens the underlying `AssetUsage`) — out of this wave's scope, see Gotchas.
    - **FLEET-RADIO R6 — `requireRcRelayReady`, checked second.** Re-evaluates the `rc-relay` `FeatureReadiness` row from the same report. Only `FeatureStatus.MISSING` refuses (audited `REFUSED:not-ready:rc-relay`, plain `IllegalStateException` carrying the row's own detail sentence); `UNKNOWN`/`DEGRADED`/`READY` all let `engage` proceed — a never-probed vehicle must still be able to engage, matching `ReadinessService`'s own "absence of evidence is not evidence of readiness" rule.
  - `VehicleUnidentifiedException` (`application`, `final`, extends `IllegalStateException`) — chosen as an `IllegalStateException` subtype specifically so it satisfies `engage`'s pre-existing throws contract with no signature change, and so `vision-api`'s WS handler can catch it ahead of the generic `IllegalStateException` clause and map it to its own wire code instead of sniffing the message. Carries `UnidentifiedReason reason()`.
  - **One session per _asset_ (E2E-FLOW-AUDIT S1)**: a second `engage()` on an asset that already has a live session throws `IllegalStateException` without touching the port; an `engage()` on any *other* asset proceeds normally. Backed by a `Map<AssetId, DefaultManualControlSession>` guarded by the existing global `sessionLock` — engage is a once-per-flight event and the ~30 Hz `onChannels` hot path never takes that lock, so one coarse lock buys atomicity across the whole check→resolve→open→register sequence for free. **Before S1 this was a single `activeSession` field**, which — since `vision-app` wires exactly one singleton bean — serialized manual-control relaying to *one connection across the entire fleet*: a second operator engaging a different drone was refused "already active". The refusal message **must keep the literal substring `already active`** (see Gotchas).
  - `ManualControlSession#onChannels(axes, buttons, seq, tSent)` — maps through the session's `ControlProfile`'s `ChannelMap` and forwards to `ManualControlPort#send` (latest-wins), resets the watchdog deadline. `#release()` — idempotent, cancels the watchdog, audits `RELEASE`. `#controlProfile()` — `ControlProfile.forKind(link.vehicleKind())`, resolved once at engage and fixed for the session; `#active()`; `#rateHz()` — delegates to the engaged `ManualControlLink`.
  - **Watchdog**: a single self-rescheduling `ScheduledFuture` per session, not cancel-and-reschedule-per-`onChannels` — `armWatchdog()` schedules once; each firing re-reads `lastInput` and either releases (audits `WATCHDOG`, calls `WatchdogListener#watchdogTripped()`) or reschedules for the remaining time.
  - **Concurrency**: `onChannels`/`release()`/a scheduled watchdog check can each run on a different thread; one `AtomicBoolean released` (`compareAndSet(false,true)`) gates the single `doRelease` exit path; `lastInput` is `volatile` for single-writer/single-reader visibility only.
  - **Profile resolution on engage**: the session resolves `(actor, live vehicleKind)` through its `ControlProfileSelector` and falls back to `ControlProfile.forKind(...)` if `null`. Resolved **once per engage**, from what the vehicle is reporting right now — never stored per asset, which would go stale the moment an operator re-flashes the flight controller.
  - **Constructors (FLEET-RADIO R6 — `ReadinessService` is now a required 4th parameter on every one, no default exists for it)**: 4-arg production convenience (`assetService, manualControlPort, auditTrail, readinessService` — `Clock.systemUTC()` + a dedicated `"rc-watchdog"` daemon scheduler + `DEFAULT_WATCHDOG_TIMEOUT_MS=300`); 6-arg test seam (adds explicit `Clock`+`ScheduledExecutorService`); 7-arg additionally takes `long watchdogTimeoutMs`; 8-arg canonical additionally takes `ControlProfileSelector` — `vision-app`'s wiring calls this one directly with `vision.rc.watchdog-timeout-ms` and the real profile selector. `readinessService` has no safe no-op default (unlike `profileSelector`'s `builtInOnly()`) — skipping the gate would silently reintroduce the exact failure mode it exists to close, so every constructor requires it explicitly (java-clean-code §3: a new required collaborator means updating call sites, never a null-means-off overload).
  - **Audit**: mirrors `DefaultFlightCommandService`'s shape — `attrs = {assetId, command:"MANUAL_CONTROL", result}`, `result ∈ {ENGAGE, RELEASE, WATCHDOG, DENIED:out of scope, REFUSED:unidentified-vehicle:<reason>, REFUSED:not-ready:<featureKey>, REFUSED:maintenance-grounded}` (the last, WAREHOUSE-UX W5). No `REFUSED` variant for a port-rejected engage — "no attempt was actually relayed".
- `VehicleProfileService` (interface) → `DefaultVehicleProfileService(AssetService, VehicleConfigPort, VehicleProfileRepositoryPort, UsageSessionService, AuditTrailPort)` — the PROBE stage, 5 parameters, at the java-clean-code §3 ceiling.
  - `VehicleProfile probe(AssetId, Duration window, UserId actor, Authority scope)` — a **command**, not a read: active probe traffic on the aircraft's own link, gated on `scope.mayManageFleet(asset.ownership())` (**AUTH-ROLES-PLAN wave B6** — widened from a bare `VisibilityScope` parameter calling the now-`@Deprecated` `canManage(Ownership)`; same authority-not-visibility semantics, additionally requiring `Capability.MANAGE_FLEET`); a denial is `AccessDeniedException` (403) **and audited**. Resolves the asset unscoped first (`AssetService#details(AssetId)` → 404 unknown), then the first active device `VehicleConfigPort#supports` claims (zero matches → `IllegalStateException`, 409, not audited). On success, persists via `VehicleProfileRepositoryPort#save` and audits `result = "COMPLETE"` or `"INCOMPLETE:<reason>"`.
  - `VehicleProfile latestProfile(AssetId, VisibilityScope scope)` — a **read**: scoped `AssetService#details`; unknown/out-of-scope/never-probed all collapse to `NoSuchElementException` (404); never audited. **Stays `VisibilityScope`-typed** — it never called a deprecated authority predicate, so wave B6 left it untouched.
  - `VehicleProfile probeCandidate(String linkKey, Duration window, UserId actor)` — the pre-registration probe: calls `VehicleConfigPort#probe` directly, no `Asset`/`Device`/scope/audit involved (none exists yet — see Gotchas).
  - `VehicleProfile captureSnapshot(AssetId, UsageId, FlightPhase, Duration window, UserId actor, Authority scope)` — same authority/device-resolution/persist/audit shape as `probe` (**AUTH-ROLES-PLAN wave B6**, same `Authority`/`mayManageFleet` widening), plus: rejects any `phase` other than `PREFLIGHT`/`POSTFLIGHT` with `IllegalArgumentException` (400, unaudited, checked before authority) and persists via the tagged `save(DeviceId, UsageId, FlightPhase, VehicleProfile)`. Audited via a dedicated `auditCapture` helper (`command="CAPTURE_SNAPSHOT"`, attrs add `usageId`/`phase`).
  - `FlightPassport passport(AssetId, UsageId, VisibilityScope scope)` — a **read**: scoped `AssetService#details` (404 unknown/out-of-scope) then `requireUsageBelongsToAsset` (`void`, 404 if `usageId` doesn't belong to this asset) — delegates to **`UsageSessionService#usageBelongsToAsset(usageId, assetId)`**, an uncapped boolean membership check (not `AssetDetails#recentUsages()`'s capped 20-item list — a passport must resolve a flight of any age, see Gotchas). Then looks up `PREFLIGHT`/`POSTFLIGHT` independently via `findByUsageAndPhase`, either or both `null` if not yet captured; never audited.
  - `List<ParameterDrift> driftFromPreviousFlight(AssetId, UsageId, VisibilityScope scope)` — a **read**, same scope+membership gate as `passport`. Compares the **previous flight's `POSTFLIGHT`** snapshot against the **current flight's `PREFLIGHT`** snapshot (see Gotchas for why that pairing). The "current" flight is read out of the already-scoped, capped `details.recentUsages()` (safe by construction — see Gotchas). "Previous flight" is the nearest `AssetUsage` in that same capped list with `startedAt()` before the current one's. No previous flight, or either endpoint snapshot missing, both return an empty list. `ConfigDriftCalculator.diff` does the comparison once both are in hand.
- `RemediationService` (interface) → `DefaultRemediationService(AssetService, AssetLiveStatePort, VehicleConfigPort, AuditTrailPort)` — the CONFIGURE stage's vehicle-side half (Mechanism A + Tier-A/B `PARAM_SET`).
  - `MessageIntervalOutcome requestMessageInterval(AssetId, int messageId, Duration interval, UserId actor, Authority scope)` — gate order: resolve asset (404) → `scope.mayManageFleet(asset.ownership())` (403, audited) → disarmed-only → resolve device (409, not audited) → call port, audit `outcome.outcome().name()`. **AUTH-ROLES-PLAN wave B6**: `scope` widened from `VisibilityScope` (was `scope.canManage(...)`, now `@Deprecated`) to `Authority`, same parameter name, additionally requiring `Capability.MANAGE_FLEET`.
  - `ParameterWriteOutcome writeParameter(AssetId, String parameterName, double value, boolean explicitConsent, UserId actor, Authority scope)` — gate order, **Tier first, before the asset is even looked up**: `ParameterTier.classify(parameterName)` empty or `C` → `IllegalArgumentException` (400), unaudited, no asset resolution at all. Then resolve asset (404) → Tier B needs `scope.mayAdminister()` (403, audited) **and** `explicitConsent == true` (else 400, unaudited); Tier A needs `scope.mayManageFleet(...)` (403, audited) → disarmed-only → resolve device (409) → call port, audit. **AUTH-ROLES-PLAN wave B6**: same `Authority` widening as `requestMessageInterval` — Tier B additionally requires `Capability.MANAGE_ORG`, Tier A additionally requires `Capability.MANAGE_FLEET`.
  - **Disarmed-only, both methods**: reads `AssetLiveStatePort#latestTelemetry(assetId).map(Telemetry::flightState).map(FlightState::armed)`; `armed == null` is refused **exactly like** `armed == true` — audited as `"REFUSED:arming state unknown"` vs `"REFUSED:aircraft is armed"`, both `IllegalStateException` (409).
  - **Authority divergence from `DefaultFlightCommandService`, flagged**: these services (and `VehicleProfileService#probe`/`#captureSnapshot` above) gate on `Authority#mayManageFleet(Ownership)`/`Authority#mayAdminister()` (the authority axis, **AUTH-ROLES-PLAN wave B6**) where pre-onboarding flight-command/manual-control gates still take a bare `VisibilityScope` and use `scope.includes(asset.ownership())` (visibility) — a deliberate, plan-mandated split within the same module, not an inconsistency.
- `ReadinessService` (interface) → `DefaultReadinessService(AssetService, VehicleProfileRepositoryPort, FeatureRequirementRepositoryPort, MaintenanceQuery)` (+ package-private 5-arg test seam additionally taking `Supplier<Instant> clock`) — the NEGOTIATE stage. **`MaintenanceQuery` (WAREHOUSE-UX-CONTEXT.md D6/OQ1, WAREHOUSE-UX W5) is a new required 4th collaborator, no default** — every call site must pass warehouse's `MaintenanceQuery` bean.
  - `ReadinessReport evaluate(AssetId, VisibilityScope scope)` — a **read**: `AssetService#details(scope, assetId)` (404), never audited. Verdict rule ("absence of evidence is not evidence of readiness"): `UNKNOWN` whenever never-probed or the latest `VehicleProfile.complete() == false`; otherwise `NO_GO` iff any feature evaluates `MISSING` (collected into `blockers`), else `GO` — a `DEGRADED` or per-feature `UNKNOWN` never forces `NO_GO` alone. Evaluates **only the configuration-derived half** (profile vs. requirement table) — the telemetry-derived half (video/telemetry/GPS/battery/armable) is an intentional gap, see Gotchas.
  - **WAREHOUSE-UX W5 — a third, independent `NO_GO` source: open warehouse maintenance blockers.** After computing the configuration-derived verdict above, `evaluate` also calls `maintenanceQuery.openBlockers(assetId)`, defensively re-filters to `MaintenanceRecord#isOpen()` and `MaintenanceKind#blocksFlight()` (true only for `GROUNDING`/`INSPECTION_DUE` — `REPAIR`/`NOTE` never block), and if any survive: forces `NO_GO` (**overriding even `UNKNOWN`** — a manager's grounding is a harder fact than "never probed"), and appends one `blockers` entry per record, `"MAINTENANCE_GROUNDED:<kind>:<summary>"` (constant `DefaultReadinessService.MAINTENANCE_BLOCKER_PREFIX = "MAINTENANCE_GROUNDED:"`). Deliberately **not** a new `FeatureReadiness` row — `featureKey` is validated against the frozen `FeatureRequirement.FEATURE_KEYS` eleven-key set and a maintenance record isn't one of those keys — so this rides `ReadinessReport#blockers(): List<String>`, which carries no such constraint; the wire (`ReadinessReportResponse`, `vision-api`) already passes `blockers` through verbatim, and `station/vision-web`'s `readiness-logic.ts#featureLabel` already falls back to rendering an unrecognized key/string as-is — **zero wire or web changes needed**. `DefaultManualControlService#engage` checks for this exact prefix to refuse manual control on a grounded asset (see below).
  - **FLEET-RADIO R6 — a feature key may now carry more than one requirement row, folded into one `FeatureReadiness`**: `evaluateFeature` evaluates every `FeatureRequirement` row sharing a `featureKey` independently, then `combine` folds them into the single row the wire contract allows per key (worst status wins; a non-`READY` result's `detail` joins every failing row's own sentence; `remedy` is the first non-null remedy among the rows at the worst status). `rc-relay` (seeded by V27) is the first feature with two rows: a GCS-sysid value check (`SYSID_MYGCS`/`MAV_GCS_SYSID`, alias-aware, must equal `255` — the sysid this platform transmits as) and an `RC_OPTIONS` forbidden-bits check (bit 1, `IGNORE_OVERRIDES`, must be clear). Fixed in the same wave: a parameter-only `MISSING` row now correctly carries `remedy = PARAM_WRITE` — before R6 this branch always fell through to `null`, untested (no prior row's `MISSING` case had ever asserted its remedy).
- `TrackCorrectionSettings(GateSettings gate, DivergenceSettings divergence)` — every threshold `DefaultTrackCorrectionService` needs, sourced from `vision.geo.visual.gate.*`/`vision.geo.visual.divergence.*` (no magic numbers, CLAUDE.md rule 1). `GateSettings(double minRadiusMeters, double maxRadiusMeters, int confirmConsecutive, double confirmAgreementMeters, Duration confirmWindow)` — `maxRadiusMeters` must exceed `minRadiusMeters`, `confirmConsecutive` ≥1, both other fields positive. `DivergenceSettings(double sigma, int consecutiveFixes, Duration clearAfter, double defaultRawRadiusMeters)` — passed straight through to an internally-built `DivergenceRule`; `defaultRawRadiusMeters` is the raw fix's assumed 1-sigma (see Gotchas).
- `TrackCorrectionService` (interface) → `DefaultTrackCorrectionService(TrackCorrectionRepositoryPort, TrackCorrectionLiveUpdatePort, EventPublisherPort, TrackCorrectionSettings)` (+ package-private 5-arg test seam adding `Supplier<Instant> clock`).
  - `TrackCorrection submit(AssetId, UsageId, VisualFix, Telemetry rawAtFrameTime)` — applies the §4.3 Java-side gate in order: **radius floor** (a Python-accepted fix whose `radiusMeters` is absent or below `gate.minRadiusMeters()` downgrades to `NO_FIX`, Java-authored refusal) → **consecutive agreement** (every radius-floor-passing fix joins a per-asset rolling window capped at `confirmConsecutive`; `CONFIRMED` needs a *full* window spanning ≤`confirmWindow` with every pairwise distance within `confirmAgreementMeters`; any `NO_FIX` resets the window) → **radius ceiling** (above `maxRadiusMeters`, at most `PROBABLE`) → **Python evidence** (`CONFIRMED` additionally requires `evidence.sequenceConverged() && evidence.cellCalibrated()`). `separationMeters`/`sigmaMeters` (`sigmaMeters = sqrt(defaultRawRadiusMeters² + fix.radiusMeters()²)`, via `GeoProjection.bearingDistance`) computed whenever both a corrected and a raw position exist, regardless of status; `rawAtFrameTime == null` or missing lat/lon leaves both `null`. Feeds an internally-built `DivergenceRule`; a rising edge publishes exactly one `POSITION_DIVERGENCE` `Event` via `EventPublisherPort`. Persists and publishes always — `NO_FIX` included.
  - `List<TrackCorrection> forUsage(UsageId, int limit, VisibilityScope)` / `Optional<TrackCorrection> latest(AssetId, VisibilityScope)` — scoped reads; `Kind.GROUPS` is pass-through, unfiltered — see Gotchas.
  - `int prune(Instant before)` — delegates to `repository.deleteOlderThan`; called by the runner's own cadence, not user-facing.
- `GeofenceService` (interface) → `DefaultGeofenceService(GeofenceRepositoryPort, GeofenceMonitor, GeofenceLiveUpdatePort)` (3rd param added LIVE-POLL-RETIREMENT-PLAN wave L3, CLAUDE.md rule 10 — the old 2-arg constructor was replaced outright, not kept as a convenience overload; both call sites, the test and `vision-app`'s `ApplicationServiceWiring`, updated) — CRUD/list over `GeofenceZone`s; zones are global reference data (no ownership, no audit trail), a thin pass-through plus keeping `GeofenceMonitor`'s cache current and publishing to the `zones` SSE topic.
  - `List<GeofenceZone> zones()` sorted by name; `Optional<GeofenceZone> find(ZoneId)`; `create`/`update`/`delete` each call `geofenceMonitor.refresh()` **then** `geofenceLiveUpdatePort.publishZoneEvent(new GeofenceZoneEvent(Action.X, zone))` (`update`/`delete` 404 for an unknown id). **`delete` captures `require(id)`'s return value** (`GeofenceZone existing = require(id);`) rather than discarding it, specifically so the `DELETED` event carries the last-known zone in full — see `GeofenceZoneEvent`'s own entry above.
- `GeofenceMonitor` (public, final, no interface) — evaluates live telemetry against the enabled `GeofenceZone` set and raises `GEOFENCE_BREACH` events on edge transitions; reached from perception's `UsageTracker#applySample` **indirectly** — `vision-app` wires `geofenceMonitor::evaluate` into that tracker's `BiConsumer<AssetId, Telemetry>` seam, so perception never names this flight-context type (see Gotchas).
  - `GeofenceMonitor(GeofenceRepositoryPort, EventPublisherPort, EventLiveUpdatePort)` — both publisher params nullable.
  - `void refresh()` — reloads the enabled-zone cache from `findAll()` filtered to `enabled()`.
  - `void evaluate(AssetId, Telemetry)` — ignores a sample with no lat/lon entirely; self-populates the cache lazily on first use. Breach rules (frozen): `KEEP_OUT` breaches iff inside the polygon at any altitude; `KEEP_IN` breaches iff at least one enabled `KEEP_IN` zone exists and the position is outside **every** enabled `KEEP_IN` zone, or inside this zone but above its own `maxAltitudeMeters`. Per-`(assetId,zoneId)` breach state is in-heap only; only an edge transition raises one `Event(GEOFENCE_BREACH, ...)` via both ports.
- `GeofenceZoneSpec(name, kind, polygon, maxAltitudeMeters, enabled)` — one shape for both create and update; duplicates `GeofenceZone`'s own polygon-size/altitude validation for fail-fast.
- `LinkStateService` (interface) → `application.link.DefaultLinkStateService(VehicleLinkPort, LinkStateLiveUpdatePort, EventPublisherPort)` (LINK-PAIRING-PLAN.md §3.4 frozen contract) — the one implementation, matching `DefaultGeofenceService`'s own constructor idiom. The constructor's last act is `vehicleLinkPort.onGroupChanged(this::onAutomaticGroupChange)` — exactly one subscription, for the instance's lifetime.
  - `LinkGroupView linksFor(AssetId)` / `LinkGroupView pin(AssetId, LinkId, UserId actor)` / `LinkGroupView release(AssetId, UserId actor)` — each delegates to `VehicleLinkPort` then routes the fresh snapshot through a shared private `observe` helper: always republishes on `LinkStateLiveUpdatePort#publishLinks` ("one topic, one full-state payload", same rule `tracks`/`geo` follow), and keeps this instance's own last-observed `activeLinkId` per asset (`Map<AssetId, Optional<LinkId>>`) so it can tell a genuine change from a repeat read.
  - **(LINK-PAIRING-PLAN.md §8 defect #5, new)** `onAutomaticGroupChange(AssetId)` — the `VehicleLinkPort#onGroupChanged` listener; simply re-runs `linksFor(assetId)` (reusing the exact same `observe` machinery, so a spontaneous adapter-side election change now republishes on the live topic and fires `LINK_FAILOVER` on its own, not only when a client happens to GET the route) inside a `try/catch (RuntimeException)` that logs (`System.Logger`, WARNING) and swallows — this runs off the adapter's own thread, so nothing here may ever propagate back into it.
  - **Exactly one `LINK_FAILOVER` per active-link change**: automatic elections happen continuously inside the adapter's own `LinkGroup`, entirely outside this service's control — this service only ever *discovers* such a change the next time something calls one of the three methods above. The very first observation of a given asset only establishes the baseline (a cold cache catching up is not itself a "failover"); every observation after that fires exactly one `EventType.LINK_FAILOVER` (`vision-platform`) iff `activeLinkId` actually changed, attributes `{assetId, fromLinkId, toLinkId, reason}` (`reason` `"auto"` for `linksFor`, `"operator"` for `pin`/`release` — either side of `from`/`to` omitted, not `null`-on-the-wire, when there was no active link).
- `application.telemetry.TelemetryService` (interface) → `DefaultTelemetryService(TelemetryRepositoryPort)` — this context's telemetry-*persistence* ownership seam, deliberately minimal (one method only — a second telemetry concern should prompt reconsidering the name/shape).
  - `void record(UsageId, Telemetry)` — delegates to `telemetryRepository.save(usageId, telemetry)`; perception's `UsageTracker` calls this once per applied sample, after warehouse's `UsageSessionService#fold` has folded the sample into the `AssetUsage` in memory.

### `application.alerting` — "aircraft is in trouble" notifications (docs/plans/active/ASSET-FLOWS-PLAN.md §2, wave S4/BK2)
Two more edge-triggered telemetry monitors, filling in the `EventType.LINK_LOST`/`BATTERY_LOW` bell
kinds (`vision-platform`) alongside `GeofenceMonitor`'s `GEOFENCE_BREACH` — same nullable-publisher-port
convention, same "raise on the edge only" discipline, deliberately **not** merged into `GeofenceMonitor`
itself (unrelated triggers, unrelated state shape — one class per monitored condition, matching this
module's existing one-monitor-per-concern precedent).
- `record BatteryAlertSettings(double criticalPercent, double warningPercent)` — the D6 threshold pair (`vision.ops.battery.critical-percent`/`warning-percent`, bound by `vision-app`, this module never reads a property directly). Compact constructor rejects either value outside `[0,100]`, NaN, or `warningPercent <= criticalPercent`. `static defaults()` → `(10.0, 25.0)`, the frozen D6 defaults, used only if `vision-app`'s `@Value` binding is itself absent.
- `BatteryMonitor` (public, final, no interface) — `BatteryMonitor(EventPublisherPort, EventLiveUpdatePort, BatteryAlertSettings)`, both publisher params nullable (`GeofenceMonitor`'s own convention). `void evaluate(AssetId, Telemetry)`: a `null`/absent `batteryPercent` sample is ignored entirely (not a 0%, not a re-arm); a rising edge — the *first* sample at or below `criticalPercent` since the asset last re-armed — raises exactly one `BATTERY_LOW` `Event` (attributes `{assetId, batteryPercent}`) via both ports and latches; every further below-critical sample for that asset is silent until a sample at or above `warningPercent` re-arms it (hysteresis: the warning line, not the critical line, clears the latch, so a battery hovering at 10% cannot spam). Per-`AssetId` latch state is a `ConcurrentHashMap<AssetId,Boolean>`, in-heap only, resets on restart — same honest limitation as `GeofenceMonitor`'s breach state.
- `LinkLossNotifier` (public, final, no interface) — `LinkLossNotifier(EventPublisherPort, EventLiveUpdatePort)`, both nullable. `void reportLinkLost(AssetId, String summary)` raises exactly one `LINK_LOST` `Event` (attributes `{assetId}`, `message` = `summary` verbatim) via both ports, unconditionally — deliberately stateless, unlike `BatteryMonitor`: the caller's own signal is already guaranteed edge-only (see Gotchas), so no latch is needed here. **Wired into production (docs/plans/active/ASSET-FLOWS-PLAN.md S4/BK2b)** — perception's `UsageTracker#subscribeTelemetry` calls `reportLinkLost` from its `SupervisedPublisher` outage callback; see that module's own MODULE.md and the Gotchas entry below for the exact call site.

## `application.seat` — the crew seat registry (docs/plans/active/CREW-CONTROL-PLAN.md §3.1/§3.2, wave W1)
"One hand per surface": who currently holds each of an asset's two seats. Carries **no authorization
model of its own** — every "may this actor…" question is answered by the caller (§3.7's imported
`AssetAuthority` contract, `station/vision-api`, not yet built as of W1) before any method here is
invoked; this service only tracks *who currently does*, never *who may*.
- `SeatService` (interface) → `DefaultSeatService(Clock, AuditTrailPort, long ttlMs)` — in-heap, TTL'd, no persistence, no scheduler.
  - `Optional<Seat> holder(AssetId, SeatKind)` — the live holder, or empty if free or lazily expired.
  - `Seat take(AssetId, SeatKind, UserId actor)` — takes a free seat or renews the caller's own already-held one (only `expiresAt` moves on a renewal; `acquiredAt` is untouched); throws `IllegalStateException` naming the current holder's `UserId` value if held by someone else.
  - `Seat preempt(AssetId, SeatKind, UserId actor)` — takes the seat unconditionally, regardless of who (if anyone) holds it now; always starts a fresh hold (`acquiredAt = now`); never throws. Authorization is entirely the caller's job — in practice only ever called with `CAMERA` (§3.2 rule 3, the flight-seat holder always wins the camera), but the method itself does not special-case kind.
  - `void release(AssetId, SeatKind, UserId actor)` — idempotent; clears the seat only if `actor` is its current live holder, silent no-op otherwise (free, expired, or held by someone else).
  - `void forceRelease(AssetId, SeatKind)` — idempotent; clears the seat regardless of holder. **Deliberately two-argument, no `actor`** — the manager path (§3.2 rule 4); see Gotchas for why this means the `FORCE:<KIND>` audit entry is **not** written here.
  - `void onPreempted(AssetId, SeatKind, Runnable listener)` — registers a synchronous, same-thread listener fired whenever that seat is displaced by `preempt` or by a `forceRelease` that actually held something. This is the RC-release hook: a manager forcing the flight seat synchronously releases any live RC session through this callback, so a displaced pilot's sticks die within one watchdog period rather than two stick sources being live at once.
  - **Storage**: both of one asset's seats live together in one `SeatPair` (private nested record), keyed by `AssetId` in a single `ConcurrentHashMap`. Every transition — `take`/`preempt`/`release`/`forceRelease` — goes through exactly one `ConcurrentHashMap#compute` call on that key, so concurrent operations on the same asset can never interleave into a torn state. The remapping function never calls back into this class; audit writes and `onPreempted` callbacks happen only *after* `compute` returns, outside the map's per-bin lock — this sidesteps the `IllegalStateException("Recursive update")` `ConcurrentHashMap` raises on same-key reentrancy, and keeps a slow audit write or a misbehaving listener from blocking unrelated seat operations.
  - **Lazy expiry**: no sweeper thread, no scheduled executor. `now >= expiresAt` reads as free the next time anything looks — `holder`/`take`/`release`/`forceRelease` all route through one `liveOrNull` check. Expiry itself writes nothing to the audit trail; the next real `take` tells the story. Same honest in-heap posture as `GeofenceMonitor`'s own breach state.
  - **Audit** (`AuditAction.UPDATED`, target `AuditTargetType.ASSET`, attrs `{assetId, command:"SEAT", result}`): `take` (genuine, not a renewal) → `TAKE:<KIND>`; `release` (actually held by the actor) → `RELEASE:<KIND>`; `preempt` → `PREEMPT:<KIND>`. A renewal is deliberately **not** audited (a five-second heartbeat is noise, not history). `forceRelease` writes **no** audit entry from this class — see Gotchas.

## Conventions
- **Validation**: domain records validate in their compact constructor; the application layer uses `Objects.requireNonNull`.
- **The acting user is a method parameter (`UserId actor`/`VisibilityScope scope`)**, never a constructor dependency.
- **Constructor injection only**, collaborators `Objects.requireNonNull`-wrapped.
- **Every port interface's javadoc documents Contract and Threading**; `TelemetrySourcePort` adds a Backpressure section describing the latest-wins drop policy.
- **One public constructor per class**: a new field means updating call sites, or bundling optional data into a settings/collaborators record with a `defaults()` factory — never one more constructor overload (`.claude/skills/java-clean-code/SKILL.md` §3, `CLAUDE.md` rule 10; the withdrawn N-1-arg convenience-constructor idiom this replaces does not appear anywhere in this module). `ManualControlSession` is an interface with no constructors of its own; its sole implementation has exactly one 5-arg constructor.
- **Audit shape**: `AuditEntry.of(actor, action, targetType, targetId, summary, attrs)` with a short `result` token (`ACCEPTED`/`NO_ACK`/`REFUSED:<msg>`/`REFUSED:unidentified-vehicle:<reason>` (FLEET-RADIO R2, `DefaultManualControlService#engage`)/`REFUSED:not-ready:<featureKey>` (FLEET-RADIO R6, same method)/`REFUSED:maintenance-grounded` (WAREHOUSE-UX W5, same method)/`DENIED:out of scope`/`ENGAGE`/`RELEASE`/`WATCHDOG`) plus a human `summary`.

## Gotchas

- **`DefaultManualControlService#engage`'s "already active" refusal text is a wire contract, not prose.** `station/vision-api`'s `ManualControlWebSocketHandler#mapIllegalState` matches the substring `already active` to emit the frozen `ALREADY_ENGAGED` denial code; every other `IllegalStateException` from `engage` falls through to `NOT_COMMANDABLE`. Rewording that message without updating the matcher silently downgrades the operator's denial to the wrong code — the exception type alone cannot distinguish the cases, since `engage` throws plain `IllegalStateException` for four different causes. `DefaultManualControlServiceTest` asserts the substring directly so a rename fails a test rather than a live flight.
- **`GeofenceZone.contains` is a planar approximation, not spherical geometry** — accurate at the scale a geofence actually operates at (tens of meters to a few km), **not** valid near the poles or across the antimeridian. No attempt is made to detect/reject a pathological polygon spanning either.
- **`ControlBinding#toMicros` ignores `centerMicros` entirely for `Source.BUTTON`** — a button's mapping is a straight 2-point line from `minMicros` to `maxMicros`.
- **`ChannelMap#apply` never throws on missing input** — an unreported/`null`/short `axes`/`buttons` list reads as `0.0` for whichever bindings fall outside it (a browser gamepad frame that hasn't yet reported every axis/button must not crash the relay).
- **A profile that binds nothing above CH*n* produces an *n*-channel frame, not a 16-channel one with `IGNORE`s.** A rover's frame is 3 channels long and CH4 is simply absent — `mavlink-core`'s `RcChannels#wireValue` (renamed from `channelOrIgnore` in FLEET-RADIO R3) pads the wire frame with `IGNORE` downstream, for any channel past the frame's own length **and** for 17/18 unconditionally. Assert on frame *size* when testing a profile, not on a CH4 value.
- **FLEET-RADIO R3 (F3/F4/F17) — CH9+ used to be silently dropped, and this module's own `RcChannels` deliberately does not grow a wire-encoding method to fix it.** Before this wave, `mavlink-core`'s `ManualControlService.buildFrame` hardcoded chan9..18 to `IGNORE` regardless of what a caller's `RcChannels` carried (F3) — so a CH9 binding here (e.g. a rover mode switch via `ControlFunction.AUX_1`) reached `MavlinkManualControlSender`, reached `mavlink-core`'s own `RcChannels`, and vanished at the very last step. Fixing F3 exposed F4: MAVLink's extension channels (9..16) don't share base channels' `RELEASE=0` sentinel — `0` means "ignore" there, and only `65534` means "release" — so a domain-uniform `RELEASE` constant needs a *wire-position-aware* translation somewhere before it hits the message. That translation (`wireValue(int)`) was added only to `mavlink-core`'s `RcChannels`, not this module's: nothing in this module's own call chain needs wire-level encoding — `MavlinkManualControlSender.toCoreChannels` just copies this module's `microsByChannel` list verbatim into a new `mavlink-core.RcChannels`, and the translation happens entirely on that side. Adding an unused `wireValue`-equivalent here would be exactly the kind of speculative, never-called method `.claude/skills/java-clean-code/SKILL.md` warns against. This module's own range narrowing (`MAX_CHANNELS=16` on `RcChannels`, `[1,16]` on `ControlBinding`) is F17's half of the same fix, enforced independently on both sides of the module boundary so a bad value can never cross it either direction.
- **A stored `ControlProfile`/`ControlBinding` from before this narrowing (CH17/18) fails loudly on load, not silently.** `storage/persistence`'s `ControlProfileEntity` stores `List<ControlBinding>` as JSON (`@JdbcTypeCode(SqlTypes.JSON)`); Hibernate's deserialization re-invokes this record's own compact constructor, so a legacy row bound to channel 17 or 18 throws `IllegalArgumentException` at *read* time. `storage/persistence`'s `JpaOperations.read` has no catch block, so that exception propagates unmodified through the repository and the application service to `vision-api`'s global `ApiExceptionHandler`, which maps `IllegalArgumentException` to HTTP 400 — a human-visible error on the next load of that profile, never a quietly-truncated one. No code in `storage/persistence` needed to change for this — the "fail loudly at read time" behaviour was already the entity's own documented design intent (see its javadoc), and F17's range narrowing simply gives it a real case to catch.
- **A `ControlProfile`'s two invariants are about accidents, not typos** — "the same physical control bound twice" and "the same RC channel driven twice" are both rejected in the compact constructor, so a bad assignment fails at *save*, not at *fly*.
- **A switch-kind `ControlBinding` ignores the raw stick value and sends a detent** — `SWITCH_2`/`SWITCH_3` quantize through `SwitchPosition.of` to `min`/`center`/`max`; `reversed` flips the *position* (`LOW ↔ HIGH`), not the microseconds.
- **`ControlProfileId.builtIn(kind)` is derived, not stored** — a UUIDv3 over a fixed namespace string. The built-in ids are stable across restarts/machines, but changing that namespace string or a `VehicleKind` name silently changes an id, which would make every stored `active` pointer at a built-in dangle. Nothing stores one today (`OwnedControlProfile` refuses to wrap a built-in), which keeps that safe.
- **`ControlProfileService` has no `VisibilityScope` gate, unlike every other service in this module** — deliberate: a stick layout is owned by one user, so the authority question is "is this yours", not "is this asset in your scope".
- **`DefaultManualControlService` depends on `ControlProfileSelector`, not `ControlProfileService`** — and defaults to `builtInOnly()`. A 6-arg construction therefore silently ignores saved profiles; only the 7-arg one honours them. A missed wiring shows up as "my saved layout does nothing", never as a crash.
- **`VehicleKind.UNKNOWN` no longer gets a flyable map (FLEET-RADIO R2)** — before this wave, `ControlProfile.forKind(UNKNOWN)` returned a centred four-axis map, and a centred throttle is ~50% power: not safe on a multirotor. There is no safe *default* for a vehicle that never said what it is, and R1 deliberately did not invent one — R2 stops trying: `forKind(UNKNOWN)` now returns an empty `ChannelMap`, and `DefaultManualControlService#engage` refuses to open a session on an `UNKNOWN`-kind link at all, with a reason (`UnidentifiedReason`) the operator can act on rather than a silently-armed 50%-throttle guess. The fix a genuinely unidentified vehicle needs is still a heartbeat (or an operator who can see it and says what it is), never a better guess baked into this module.
- **The three-way `UnidentifiedReason` distinction exists because "refused" is not one fact.** `NOT_A_VEHICLE` (a gimbal/GCS shares this link), `UNSUPPORTED_VEHICLE` (a real airframe this platform doesn't fly), and `NEVER_IDENTIFIED` (a genuinely unseen `MAV_TYPE`) have three different operator remedies, and collapsing them into one refusal message would make "a gimbal is on your link" indistinguishable from a correct, boring refusal. `VehicleKind` itself stays exactly 4 values (`COPTER|PLANE|ROVER|UNKNOWN`) — a **control-shape** taxonomy that only needs to answer "what `ChannelMap` applies", where all three unidentified cases genuinely need the same answer (none) — while `UnidentifiedReason` carries the operator-facing "why", read once at `engage` refusal time and nowhere else in the control path. Widening `VehicleKind` itself was rejected: every `ControlProfile.forKind`/`ControlProfileSelector`/`ControlProfileRepositoryPort` switch would gain 2 more cases whose channel map is identical to `UNKNOWN`'s (empty), for zero behavioral gain over a second, orthogonal enum consulted only at the refusal boundary.
- **`GeofenceMonitor` must not take perception's `UsageTracker` directly** — `UsageTracker` (perception) declares a plain `BiConsumer<AssetId,Telemetry>` seam and `vision-app` wires `geofenceMonitor::evaluate` into it. Any future "perception needs to react to a flight-context fact" need should reuse this functional-seam pattern, not add a constructor parameter naming the other context's type.
- **`GeofenceMonitor`'s per-`(assetId,zoneId)` breach state is in-heap only and resets on every restart** — an asset already inside a `KEEP_OUT` zone at boot reports a spurious "enter" on the next sample; a zone deleted/disabled mid-breach never gets a synthetic "exit".
- **`GeofenceMonitor`'s enabled-zone cache has two refresh paths, and both matter**: `GeofenceService` calls `refresh()` synchronously after every CRUD write; `evaluate()` also self-populates lazily on its own first call. Skipping either is a real bug, not redundancy.
- **A `KEEP_IN` breach with 2+ enabled `KEEP_IN` zones can fire one event per zone simultaneously** — the "outside every enabled zone" condition is computed once per sample and shared across every zone's own per-zone state entry. Known, accepted edge case.
- **`maxAltitudeMeters` only has an effect on a `KEEP_IN` zone** — a `KEEP_OUT` zone breaches at any altitude per the frozen rule.
- **Command-gate exception mapping is deliberately asymmetric between reads and writes**: a scoped *read* (`capabilities`) hides an out-of-scope asset as 404; a *command* instead 403s and audits the denial.
- **`DefaultSeatService#forceRelease` writes no audit entry itself.** Its frozen, two-argument signature (`AssetId`, `SeatKind` — no `actor`, docs/plans/active/CREW-CONTROL-PLAN.md §3.1) cannot construct an `AuditEntry`, which requires a non-null actor. The `FORCE:<KIND>` entry §3.6 names is therefore the enforcement layer's own responsibility (W2's `SeatController`/`SeatAccess`, which already resolved the forcing manager's identity via `AssetAuthority#mayForceSeat` to authorize the call before ever reaching this method) — flagged here so W2 does not assume `DefaultSeatService` already covers it. Every other transition this class makes (`take`, `release`, `preempt`) does carry an actor and is audited directly, per its own API-surface entry above.
- **`DefaultSeatService`'s side effects (audit writes, `onPreempted` listener callbacks) all happen after its `ConcurrentHashMap#compute` call returns, never inside the remapping function.** The function only reads the map's current value plus local parameters and writes its verdict into same-thread-only local arrays; calling back into the same class from inside `compute` (e.g. a listener that itself calls `holder`/`release` for the same asset) would risk `ConcurrentHashMap`'s own `IllegalStateException("Recursive update")` on same-key reentrancy. Any future seat transition should keep this split, not fold audit/listener logic into the compute lambda for convenience.
- **`DefaultFlightCommandService`'s IAE→ISE translation exists so `vision-api`'s exception handler needs no endpoint-specific carve-out** — `IllegalArgumentException`→400 stays global for every other endpoint; only this one call site catches the port's `IllegalArgumentException` and rethrows as `IllegalStateException`.
- **`FlightPhaseRule` has two entry points, not one function** — "session closed while armed" fires when the platform stops observing (the session itself ends), not on any telemetry sample. `nextPhase` is the sample-driven half; `onSessionClosed` is the explicit-close half.
- **A firmware rename is a comparison problem, not a data problem** — `ReadinessService` and `ParameterTier` both route parameter-name comparisons through `ParameterAliases`, and `ParameterReading` is left carrying the exact name the vehicle answered under. Normalising at probe time was rejected: it would make a profile claim a reading the vehicle never gave, breaking `ParameterReading`'s "only what was actually read" invariant. Adding a newly-renamed parameter means one entry in `ParameterAliases.GROUPS` — not a migration, and not a patch at each comparison site. Before this existed, `V18`'s `fleet-identity` row wanted `SYSID_THISMAV` while the probe read `MAV_SYSID`, so the row was `MISSING` on every ArduPilot vehicle of every generation, which made the readiness verdict a constant `NO_GO` (docs/plans/active/FLEET-RADIO-PLAN.md F0). The `V18` row is deliberately **not** rewritten — either spelling now satisfies it.
- **`ParameterTier.PATTERNS` is a representative seed, not a verified-complete enumeration** — illustrative ArduPilot parameter names, not cross-checked against firmware source. Tightening the list only ever removes writability that was never explicitly granted.
- **`RC_OPTIONS` classifies Tier B (FLEET-RADIO R6)**, alongside `RC_FS_TIMEOUT`/`RC*_OVERRIDE_.*` — same "link/failsafe behavior, writable with consent, never flight-critical-geometry" class. Without this, `rc-relay`'s R6 `PARAM_WRITE` remedy would recommend a write `DefaultRemediationService#writeParameter` refuses outright (`ParameterTier.classify` empty/`C` → 400 before the asset is even looked up), an honesty gap the readiness row's remedy would otherwise silently promise and never deliver.
- **The onboarding authority predicate (`canManage`/`canAdminister`) diverges from every pre-onboarding command gate in this module (`includes`)** — the plan's own table, not drift; do not "fix" it into consistency with the older code.
- **`ReadinessService` evaluates only half of "readiness"** — the configuration-derived half. The telemetry-derived half (video/telemetry/GPS/battery/armable) is not merged into `ReadinessReport` — doing so honestly needs a live-telemetry collaborator and named threshold sources for battery/GPS (no magic numbers).
- **FLEET-RADIO R6 — two rows under one key, deliberately, not two new frozen keys.** `FeatureRequirement.FEATURE_KEYS` stays the frozen eleven: a GCS-sysid mismatch and `RC_OPTIONS` ignoring overrides are two independent ways a MAVLink RC override silently fails to reach the servos, but `ReadinessRowResponse` (`vision-api`'s fleet-board wire shape) is a `{featureKey: status}` map that structurally cannot hold two statuses under one key regardless of how many domain keys exist — `V18__feature_requirements.sql`'s own header comment already hit this exact limit once (`preflight-checks`, `GPS_RAW_INT`+`STATUSTEXT`). Extending `FEATURE_KEYS` would not have avoided that wire constraint, only added a key the map still could not carry independently — so both facts are seeded as two rows sharing `rc-relay` (V27) and folded into one `FeatureReadiness` by `DefaultReadinessService#combine` before either ever reaches the wire. The alternative this rejects: inventing e.g. `rc-relay-sysid`/`rc-relay-options` as two new frozen keys, which would still need the exact same fold-into-one-row step at the `vision-api` boundary (a fleet-board tile is one cell per feature) while additionally breaking every existing `FEATURE_KEYS.size() == 11` assumption in this module's own tests and its consumers.
- **`CLEAR_OVERRIDES_BY_RC` (ArduPilot `RC_OPTIONS` bit 14) is adjacent to R6's bit 1 and deliberately not modeled as a readiness blocker.** With it set, an operator touching the physical transmitter sticks silently ends manual control mid-session — but unlike GCS-sysid/`IGNORE_OVERRIDES`, this is the vehicle's *designed* pilot-override behavior, not a link misconfiguration, and this table only ever sees a vehicle's static parameter values, never live-stick telemetry — there is no way to check "is the pilot's own RC currently moving a stick" from a `VehicleProfile`. Surfacing it usefully needs a telemetry-derived signal this service does not have (the same documented gap as the rest of "readiness" — see the entry above). Left as a fact for whoever next extends this table with that telemetry-derived half; V27's own migration header carries the same reasoning.
- **`RemediationResultCode`/`MessageIntervalOutcome`/`ParameterWriteOutcome` are not named in the onboarding plan's own type list** — added because `VehicleConfigPort`'s write/message-interval methods need an honest return type; naming stayed inside the frozen vocabulary rather than inventing new spellings.
- **`linkKeyOf(Device)` is duplicated, not shared, between `DefaultVehicleProfileService` and `DefaultRemediationService`** — both derive `"udp://host:port#sysid"` from `device.stream().uri()` + `options.get("sysid")`. A third caller needing the same derivation should prompt extracting it.
- **`probeCandidate` has no authority/audit model at all** — by design: registration is the last stage, so no `Asset`/ownership exists yet to gate against. This means *any* caller holding a `VehicleProfileService` reference can probe an arbitrary `linkKey` with no scope check — `vision-api`/`vision-app` must restrict which endpoint/role can reach this method, since this module has nothing to enforce it against.
- **`passport`/`driftFromPreviousFlight`'s asset-ownership check (`requireUsageBelongsToAsset`) is deliberately *not* built on `AssetDetails#recentUsages()`'s 20-item cap** — a passport's whole purpose is answering "what was this aircraft's configuration on *that* flight", routinely not one of the last 20. It delegates to warehouse's `UsageSessionService#usageBelongsToAsset(UsageId, AssetId)` — an uncapped boolean, not an `AssetUsage` fetch — throwing `NoSuchElementException` on a `false` result. Filed on `UsageSessionService` rather than `UsageService` deliberately: every `UsageService` method takes a `VisibilityScope` and this check does not (the asset is already scope-gated by the caller) — an unscoped method beside `UsageService`'s all-scoped ones would be a footgun the next reader could copy without noticing. `driftFromPreviousFlight`'s separate "find the previous flight" traversal stays on the capped `recentUsages()` on purpose — two compared flights are adjacent by definition, so the previous one is always well inside any reasonable recent-window size; `current` is resolved from that same capped list (provably safe: if the real current usage fell outside it, every entry remaining would be newer, so the "find an older one" search would find nothing either way).
- **`driftFromPreviousFlight` compares the *previous* flight's `POSTFLIGHT` snapshot against the *current* flight's `PREFLIGHT` snapshot** — not two consecutive `PREFLIGHT`s or `POSTFLIGHT`s. The disarmed-only write interlock means a parameter can only actually change between one flight's landing and the next one's arming, so that is the only pair of snapshots a real drift could show up between.
- **`DefaultTrackCorrectionService` always uses `divergence.defaultRawRadiusMeters()`, never a real HDOP-derived raw radius.** No HDOP→meters conversion exists anywhere in this codebase today (`Telemetry`/`FlightState` carry only the dimensionless `hdop`); inventing an unsourced conversion formula would violate the no-magic-numbers rule, so this service always uses the configured default instead (docs/plans/done/VISUAL-GEO-V2-PLAN.md §4.5 amendment).
- **`TrackCorrectionService#forUsage`/`#latest`'s `VisibilityScope.Kind.GROUPS` case is pass-through (unfiltered), not enforced.** This service's frozen 4-argument constructor has no warehouse dependency to resolve an asset's `Ownership` from, so `UNBOUNDED`/`ASSIGNED_ASSETS` are enforced but `GROUPS` cannot be. The intended real gate is at the `vision-api` edge: resolve the asset via warehouse's scoped `AssetService#details(scope, assetId)` (which does enforce `GROUPS`) *before* calling this service. Until that wiring exists, a MANAGER-scoped caller of this service directly would see every asset's corrections, not just their group's.
- **`first_armed_at`/`last_disarmed_at` on `asset_usages` are schema-only, mapped to no domain field.** They live on `AssetUsage` (`vision-warehouse`'s type). `FlightPassport`/`ConfigDriftCalculator` key entirely off `VehicleProfile.observedAt()` plus `AssetUsage.startedAt()`, never off arm/disarm timestamps — a future "time armed" passport field needs a `vision-warehouse` change, not a `vision-flight` one.
- **WAREHOUSE-UX W5 — maintenance blockers only come from warehouse, and only two `MaintenanceKind` values ever block.** `DefaultReadinessService#maintenanceBlockers` filters `maintenanceQuery.openBlockers(assetId)` defensively to `MaintenanceRecord#isOpen()` and `MaintenanceKind#blocksFlight()` (true only for `GROUNDING`/`INSPECTION_DUE`; `REPAIR`/`NOTE` are informational-only and never appear in `blockers`) rather than trusting `MaintenanceQuery#openBlockers`'s own "open, blocking only" contract alone — the same "defense in depth, not the mechanism" posture this module already applies to `ControlProfile.forKind(UNKNOWN)` (see that Gotchas entry above). A closed record, or an open `NOTE`/`REPAIR` record, is silently invisible here by design, not a bug.
- **`LinkLossNotifier.reportLinkLost` is called from perception's `UsageTracker#subscribeTelemetry`** (`contexts/vision-perception/src/main/java/com/drones/vision/perception/application/pipeline/UsageTracker.java`), which wraps every telemetry subscription in a `SupervisedPublisher<Telemetry>` whose `onOutageBegan` callback calls `linkLossNotifier.reportLinkLost(asset.id(), "MAVLink link to " + asset.displayName() + " lost")`. That callback fires exactly once per outage by construction (`SupervisedPublisher`'s own "outageAnnounced" latch, not a retry counter), so `LinkLossNotifier` needs no hysteresis logic of its own to get edge-only semantics — unlike `BatteryMonitor`, which evaluates a raw telemetry field and has to latch itself. `linkLossNotifier` is a required field (9th component) on perception's `UsageTrackerSettings` rather than `Optional`/nullable, mirroring `maintenanceQuery`'s own "required, safety-adjacent collaborator" rationale — see that module's own MODULE.md for the full wiring.
- **`BatteryMonitor` is composed into `GeofenceMonitor`'s existing wiring seam, not a second slot.** `UsageTrackerSettings` has exactly one `Optional<BiConsumer<AssetId,Telemetry>> telemetryObserver` slot; `vision-app`'s `ApplicationServiceWiring#usageTracker` now passes a lambda that calls `geofenceMonitor.evaluate(...)` then `batteryMonitor.evaluate(...)`, both against the same sample — same rationale as the existing `GeofenceMonitor` Gotchas entry above ("must not take perception's `UsageTracker` directly"): a functional seam composes, it does not grow constructor parameters on either side of the module boundary.

## Status
Fully implemented: flight-session telemetry writes (`TelemetryService`), geofencing (zone CRUD, live breach detection on the `KEEP_IN`/`KEEP_OUT` rules, `zones` SSE topic), flight command TX (RTH/mode/arm/disarm/emergency-stop/aux-function, with vehicle-kind-dependent emergency-stop and arm gated on maintenance grounding), manual-control RC relay with per-asset session exclusivity and watchdog (refusing an unidentified vehicle, a not-ready `rc-relay` feature, or a maintenance-grounded asset before any link opens), per-vehicle control profiles (built-in + saved, switch-bound actions, the operator's own transmitter layout via `TransmitterView`), vehicle onboarding (probe/readiness/remediation, flight passport/config-drift), the crew seat registry (`SeatService`), battery/link-loss alerting (`BatteryMonitor`/`LinkLossNotifier`), and visual-geolocation track correction/divergence gating.

Deferred/known gaps:
- `ReadinessService` evaluates only the configuration-derived half of readiness — the telemetry-derived half (video/telemetry/GPS/battery/armable) is not merged into `ReadinessReport`; doing so honestly needs a live-telemetry collaborator and named threshold sources (no magic numbers).
- `CLEAR_OVERRIDES_BY_RC` (ArduPilot `RC_OPTIONS` bit 14) is not modeled as a readiness blocker — it is the vehicle's designed pilot-override behavior, not a link misconfiguration, and needs a live-stick telemetry signal this configuration-only table cannot see (see Gotchas).
- `TrackCorrectionService#forUsage`/`#latest`'s `VisibilityScope.Kind.GROUPS` case is pass-through, unenforced — the frozen 4-argument constructor has no warehouse dependency to resolve an asset's `Ownership` from; the intended gate is at the `vision-api` edge (see Gotchas).
- No explicit "operator picks the vehicle's kind and retries engage" override exists for an `UNKNOWN`-kind link — it needs a kind-override parameter on `vision-web`'s `ManualControlClient.engage`, and trusting an operator-supplied kind over live MAVLink classification is an open safety question independent of that.
- Calibration (per-axis range learning) stays deferred — bindings are configurable, learning them is not.

Wave-by-wave history: [`MODULE-HISTORY.md`](MODULE-HISTORY.md).
