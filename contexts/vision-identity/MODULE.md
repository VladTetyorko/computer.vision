# vision-identity

Users, groups, roles, authentication, and the pilot→asset assignment roster.

**What it deliberately does not do**: resolve *what* a user may see — `VisibilityScope`/`AccessDeniedException` live in `vision-platform` (every context filters by them, not just this one). This context turns a `User` into a `VisibilityScope` (`ScopeResolver`) and manages the roster a scope is computed from — memberships, group tree, pilot→asset assignments. It does not decide whether a command may proceed against a specific asset/device — that is each consuming context's own gate (e.g. flight's `DefaultFlightCommandService`).

**Depends on:** `vision-kernel` (`UserId`, `GroupId`, `AssetId`…) · `vision-platform` (`VisibilityScope`, `AccessDeniedException`, `AuditTrailPort`/`AuditEntry`) · `vision-warehouse` — `DefaultAssignmentService` reaches `AssetService#details(AssetId)` for the one fact it needs (asset exists, its group) to run the grant check
**Used by:** `vision-map` (viewer resolution), `vision-app`, `vision-api`, `adapter-persistence`
**Build/test:** `./mvnw -B -pl contexts/vision-identity test`

## API surface

### `com.drones.vision.identity.domain.model`
- `record Group(GroupId id, String name, GroupId parentGroupId)` — org-chart node a `User` holds a `Membership` in; `parentGroupId` nullable (`null` = root); `name` non-blank; subtree scoping is `ScopeResolver`'s job
- `record Membership(GroupId groupId, Role role) implements Serializable` — a `User`'s role within one group; rides on the `User` aggregate, no separate membership repository
- `enum Role` — `VIEWER`, `PILOT`, `MANAGER`, `ADMIN` (docs/plans/active/AUTH-ROLES-PLAN.md §3.2, wave B1 prepended `VIEWER`), declared least→most privileged; ordinal ordering is load-bearing (`User#topRole()` picks the highest role by ordinal). **Read the ordering as authority, not visibility** — `VIEWER` sits at the bottom of what a caller may *do* (holds no `Capability`) while being *deliberately* handed one of the *widest* visibility scopes as of wave B6 (a group-wide read, matching a `MANAGER`'s shape — see `ScopeResolver#scopeFor`'s own entry below) — see the enum's own javadoc and `RoleTest`/`UserTest#topRolePicksPilotOverViewerRegardlessOfInsertionOrder` for the pinned prepend-is-not-a-reorder proof
- `enum AssignmentRole` (docs/plans/active/AUTH-ROLES-PLAN.md §3.4, wave B1) — `PILOT` (flight seat + camera seat), `CREW` (camera seat only) — the CREW-CONTROL-PLAN.md IC-2 answer, held per (pilot, asset) link rather than on `Role`, because the same person is routinely `PILOT` on one asset and `CREW` on another and a global role could never express that. **No ordinal semantics** — unlike `Role`, declaration order here means nothing
- `record Assignment(UserId pilot, AssetId asset, AssignmentRole role)` (wave B1) — one roster row, as returned by `AssignmentRepositoryPort#assignmentsForAsset(AssetId)`; not a separate aggregate, just a read shape over the assignment link
- `record User(UserId id, String username, String displayName, String email, String passwordHash, boolean enabled, boolean mustChangePassword, List<Membership> memberships) implements Serializable` — `username` non-blank, normalized lower-case in the compact ctor; `email` shape-checked only (non-blank, contains `@`), not RFC-validated; `passwordHash` opaque to the domain (BCrypt lives behind `PasswordHasherPort`); `memberships` defensively copied; 6-arg convenience ctor defaults **both** new fields, `mustChangePassword=false, memberships=List.of()`; `topRole()` → `Optional<Role>`; `toString()` renders `passwordHash=***`. `mustChangePassword` (docs/plans/active/AUTH-ROLES-PLAN.md §3.5/D13, wave B2, NEW) — set `true` by `UserService#create` (admin-created user, unknown password to the admin) and `UserService#setPassword` (admin reset), cleared to `false` by `AuthService#changePassword` (self-service) and `UserService#createFirstAdmin` (bootstrap, self-chosen password); purely a UI/flow signal — nothing in this module enforces it (no gate refuses a stale-flag session), enforcement is the API layer's job (B3)

### `com.drones.vision.identity.domain.port` (driven — implemented by adapters)
- `AssignmentRepositoryPort` — the pilot→asset join, independent of both `User` and `Asset`: `void assign(UserId, AssetId, AssignmentRole)` idempotent upsert **(docs/plans/active/AUTH-ROLES-PLAN.md §3.4, wave B1 replaced the 2-arg form — re-assigning an existing link with a different `AssignmentRole` changes the seat rather than creating a second link)**; `void unassign(UserId, AssetId)` idempotent; `Set<AssetId> assetsForPilot(UserId)`; `Set<UserId> pilotsForAsset(AssetId)`; `boolean isAssigned(UserId, AssetId)`; `Optional<AssignmentRole> roleFor(UserId, AssetId)` (wave B1, NEW — the IC-2 seat lookup); `List<Assignment> assignmentsForAsset(AssetId)` (wave B1, NEW — the roster, with roles). Empty set/list (never `null`) means "no links"; `assetsForPilot`/`pilotsForAsset` include a link regardless of its `AssignmentRole` — visibility follows from being assigned at all, only the verb a seat grants differs. **Gotcha for adapter authors** (found wave B2): an in-memory `Map`-of-`Map` implementation of `unassign` must not write `byPilot.getOrDefault(pilot, Map.of()).remove(asset)` — `Map.of()` is immutable, so unassigning a pilot with zero existing links throws `UnsupportedOperationException`; `DefaultAssignmentServiceTest`/`DefaultScopeResolverTest`'s fakes both had this latent bug (fixed in wave B2, see Status)
- `GroupRepositoryPort` — `Optional<Group> findById(GroupId)`; `List<Group> findAll()` snapshot; `Group save(Group)` upsert
- `PasswordHasherPort` — `String hash(String rawPassword)`; `boolean verify(String rawPassword, String hash)`; implemented outside domain/application (BCrypt, `vision-app`)
- `UserRepositoryPort` — `Optional<User> findByUsername(String)` case-insensitive; `Optional<User> findById(UserId)`; `User save(User)` upsert; `List<User> findAll()` snapshot

### `application` (root package)
- `AuthService` (interface) → `DefaultAuthService(UserRepositoryPort, PasswordHasherPort, AuditTrailPort)` (wave B2 widened from 2 to 3 args) — login checks + principal reload + self-service password change + bootstrap probe
  - `Optional<User> authenticate(String username, String rawPassword)` — `null`/blank input short-circuits to empty **with no audit write** (no known actor id to attribute a blank attempt to); returns the user only if **both** `enabled()` and `passwordHasher.verify(...)` hold. Unknown username, disabled user, wrong password are **all** `Optional.empty()` alike — never throws, deliberately, to avoid an information leak. **Wave B2**: looks the user up by username *first*; an unknown username still audits nothing (same "no actor id" reason), but once a real user is found, every attempt is audited — `AuditAction.LOGIN`/`AuditTargetType.USER` on success, `AuditAction.LOGIN_FAILED`/`AuditTargetType.USER` on a wrong password or a disabled account — always attributed to that user's own id
  - `Optional<User> find(UserId)` — reload the current session's principal fresh on every request
  - `Optional<User> loadByUsername(String)` — for Spring Security's `UserDetailsService` bridge. Both are kept — two different callers, neither subsumes the other
  - `Optional<User> changePassword(UserId id, String currentPassword, String newPassword)` (wave B2, NEW, D15) — **self-service**, unlike `authenticate` may safely reveal less by returning `Optional.empty()` uniformly (caller is already authenticated as themself), since the only failure paths are: unknown id, disabled account, wrong `currentPassword`. Rejects a blank `newPassword` with `IllegalArgumentException` (a caller bug, not an auth outcome). On success: replaces the hash, clears `mustChangePassword` to `false`, saves, records one `AuditAction.UPDATED`/`AuditTargetType.USER` entry attributed to `id` itself. **No audit entry on any failure path** (nothing changed)
  - `boolean adminExists()` (wave B2, NEW, D14 bootstrap gate) — `true` iff some user is both `enabled()` and has `Role.ADMIN` as `topRole()`. The one-way latch a first-run "create the admin" flow polls before offering itself; a disabled admin does not count (matches `DefaultUserService#createFirstAdmin`'s own independent check — see Gotchas, the two are deliberately not shared)
- `UserService` (interface) → `DefaultUserService(UserRepositoryPort, PasswordHasherPort, AuditTrailPort)` (wave B2 widened from 2 to 3 args) — every scope-gated method takes the acting `Authority` (wave B6, was `VisibilityScope`) **and, wave B2, a separate `UserId actor`** to attribute its audit entry to (see Gotchas — deliberately not bundled with `Authority`); `Authority.full()` (ADMIN/auth-off) passes every gate
  - `User create(UserSpec, UserId actor, Authority acting)` (wave B2 added `actor`; `acting` widened `VisibilityScope`→`Authority` wave B6) — `!acting.mayManageOrg()` → 403; no-memberships spec allowed only for `acting.scope().isUnbounded()`; per membership, `!acting.scope().includesGroup(m.groupId())` → 403, a role above `maxGrantableRole(acting.scope())` → 403; duplicate username (case-insensitive) → 409. **Always** sets `mustChangePassword=true` on the created user (D13 — an admin-created account starts with a password the admin, not the new user, chose). Records one `AuditAction.CREATED`/`AuditTargetType.USER` entry attributed to `actor` after save
  - `private static Optional<Role> maxGrantableRole(VisibilityScope)` (still scope-typed — this is a pure visibility-shaped lookup, not an authority gate, so wave B6 left it alone and callers pass `acting.scope()`) — `UNBOUNDED`→`ADMIN`, `GROUPS`→`MANAGER`, `ASSIGNED_ASSETS`→empty (unreachable, `mayManageOrg()` already excludes it)
  - `List<User> list(VisibilityScope acting)` (unchanged by wave B6 — a plain read, no authority question) — `unbounded()`→every user; `GROUPS`→users with ≥1 membership `acting.includesGroup(...)`; any other scope→empty
  - `User setEnabled(UserId, boolean, UserId actor, Authority acting)` (wave B2 added `actor`; `acting` widened wave B6) — `!acting.mayManageOrg()` → 403; non-unbounded `acting.scope()` must include one of the target's membership groups → 403; idempotent; preserves the target's existing `mustChangePassword`. Records one `AuditAction.ACTIVATED`/`DEACTIVATED` (`AuditTargetType.USER`) entry attributed to `actor`, **only if** the enabled flag actually changes — re-disabling an already-disabled user audits nothing
  - `User setMemberships(UserId id, List<Membership> memberships, UserId actor, Authority acting)` (wave B2, NEW; `acting` widened wave B6) — same management gate shape as `create`/`setEnabled` (checked against the **union** of the target's old and new membership groups, so a manager can both add a group they administer and remove one they no longer can see); replaces the target's whole membership list. Diffs old vs. new and records one `AuditAction.GRANTED`/`AuditTargetType.GROUP` entry per group added and one `REVOKED`/`GROUP` per group removed (both attributed to `actor`) — an unchanged set audits nothing
  - `User setPassword(UserId id, String rawPassword, UserId actor, Authority acting)` (wave B2, NEW, D15 admin reset; `acting` widened wave B6) — same management gate as `setEnabled`; rejects a blank `rawPassword` with `IllegalArgumentException`; **always** sets `mustChangePassword=true` (D13 — the target didn't choose this password). Records one `AuditAction.UPDATED`/`AuditTargetType.USER` entry **unconditionally** (no way to detect a no-op reset — `PasswordHasherPort#hash` never produces comparable output for the same input twice, e.g. BCrypt's salt)
  - `User createFirstAdmin(FirstAdminSpec)` (wave B2, NEW, D14 bootstrap) — **no scope/authority, no actor parameter**: by definition there is no acting user yet. Refuses with `IllegalStateException` if an enabled admin already exists (own `hasEnabledAdmin()` check — see Gotchas) or the username is taken. Creates a user with `enabled=true`, `mustChangePassword=false` (self-chosen password), exactly one `Membership(spec.groupId(), Role.ADMIN)`. Records one `AuditAction.CREATED`/`AuditTargetType.USER` entry attributed to **the new admin's own id** (the only sensible actor — nobody else exists yet)
- `GroupService` (interface) → `DefaultGroupService(GroupRepositoryPort)` — `create` takes the acting `Authority` (wave B6, was `VisibilityScope`), `list` the acting `VisibilityScope` (unchanged — a plain read)
  - `Group create(GroupSpec, Authority acting)` (wave B6 widened from `VisibilityScope`) — `!acting.mayManageOrg()` → 403; a **root** group (`null` `parentGroupId`) only by `acting.scope().isUnbounded()`; a parent must be `acting.scope().includesGroup(parent)` (403) before the existence check (404)
  - `List<Group> list(VisibilityScope acting)` — name-sorted; `unbounded()`→every group; `GROUPS`→groups `acting.includesGroup(id)`; any other scope→empty. **No `tree()` read model** — a manager UI wanting a nested view builds it client-side from the flat list's `parentGroupId` links
- Records: `UserSpec(username, displayName, email, rawPassword, memberships, enabled)` — only `rawPassword` non-blank is validated here, `User`'s own compact ctor validates the rest; `GroupSpec(name, parentGroupId)` — `name` non-blank, `parentGroupId` nullable (root); `FirstAdminSpec(username, displayName, email, rawPassword, GroupId groupId)` (wave B2, NEW) — `rawPassword` non-blank, `groupId` non-null (the bootstrap admin's one membership needs a home group); no `enabled`/`memberships` fields, `createFirstAdmin` fixes both

### `application.scope`
- `ScopeResolver` (interface) → `DefaultScopeResolver(GroupRepositoryPort, AssignmentRepositoryPort)` — turns a `User` into a `VisibilityScope`, and (wave B1) a `User` into an `Authority`
  - `VisibilityScope scopeFor(User)` — precedence: any **ADMIN** membership → `unbounded()`; else any **MANAGER or VIEWER** membership (wave B6 widened this from MANAGER-only) → `groups(union of each such group's subtree)`; else (**PILOT**-only, or no membership) → `assignedAssets(assignmentRepo.assetsForPilot(user.id()))` (empty for an unassigned user). Subtree = self + descendants via `Group.parentGroupId`, walked BFS per root; a `visited` set both dedupes overlapping subtrees and breaks any malformed cycle in the stored tree. **`VIEWER` now gets the same group-wide read a `MANAGER` gets** (docs/plans/active/AUTH-ROLES-PLAN.md §3.2/§4 wave B1's "staging rule", satisfied as of wave B6 — every `canManageOrg()`/`canManage()`/`canAdminister()` call site had migrated onto `Authority` first, so a `VIEWER` handed a wide `GROUPS` scope cannot accidentally pass one of those now-deleted predicates directly; `Authority#mayManageOrg()`/`#mayManageFleet(Ownership)`/`#mayAdminister()` each additionally require a `Capability` `RoleAuthority` never grants `VIEWER`, so the wide scope stays read-only in effect). Was the pre-existing `PILOT`-only/no-membership branch through wave B2 — see `DefaultScopeResolverTest#viewerSeesItsGroupSubtreeJustLikeAManager`/`#authorityForAViewerPairsGroupsScopeWithNoCapabilities` (both NEW, wave B6)
  - `Authority authorityFor(User)` (wave B1, NEW) — `new Authority(scopeFor(user), RoleAuthority.capabilitiesOf(user.topRole()))` (empty capability set for no membership); pairs the existing visibility scope with the new authority axis without changing `scopeFor`'s own behavior at all — see `com.drones.vision.platform.Authority`/`Capability` (vision-platform) and `RoleAuthority` below
- `RoleAuthority` (wave B1, NEW, `application.scope`, final class + private ctor, not an interface — no second implementation exists or is expected) — `static Set<Capability> capabilitiesOf(Role)`, the frozen table from docs/plans/active/AUTH-ROLES-PLAN.md §3.3: `VIEWER`→`Set.of()`, `PILOT`→`{OPERATE_PAYLOAD, COMMAND_FLIGHT}`, `MANAGER`/`ADMIN`→all four `Capability` constants
- `AssignmentService` (interface) → `DefaultAssignmentService(AssignmentRepositoryPort, AssetService, AuditTrailPort)` (wave B2 widened from 2 to 3 args) — the pilot→asset roster
  - `void assign(UserId pilot, AssetId asset, AssignmentRole role, UserId actor, Authority granterScope)` (wave B2 added `role` and `actor` — B1 had shipped this 3-arg with a temporary hardcoded `AssignmentRole.PILOT` literal, now gone; `granterScope` widened `VisibilityScope`→`Authority` wave B6) — validates the asset exists (404) then `granterScope.mayManageFleet(asset.ownership())` or 403 (an authority check, not a visibility one — a PILOT may not grant/revoke even their own assignment). Idempotent; `unbounded()` may assign anything, `groups()` (MANAGER) within their own subtree. Records one `AuditAction.GRANTED`/`AuditTargetType.ASSIGNMENT` entry attributed to `actor` for a **new** link or a **seat change** (previous `AssignmentRole` differs from the new one) — a true no-op (identical re-assign) audits nothing; `targetId` is the composite string `"<pilotId>:<assetId>"` since a link has no typed id of its own
  - `void unassign(UserId pilot, AssetId asset, UserId actor, Authority granterScope)` (wave B2 added `actor`; `granterScope` widened wave B6) — same grant check as `assign`; records one `AuditAction.REVOKED`/`AuditTargetType.ASSIGNMENT` entry (same composite `targetId` shape) attributed to `actor`, only if a link actually existed — unassigning a pilot who was never assigned that asset audits nothing
  - `Set<AssetId> assignmentsFor(UserId pilot)` — thin pass-through, not scope-checked (a pilot reading their own roster)
  - `Optional<AssignmentRole> roleFor(UserId pilot, AssetId asset)` (wave B2, NEW) — thin pass-through to the port's own `roleFor`; the IC-2 seat lookup exposed at the application layer
- `ActivityService` (interface) → `DefaultActivityService(AuditTrailPort)`
  - `List<AuditEntry> myActivity(UserId actor, int limit)` — thin pass-through over `AuditTrailPort.findByActor`; scoping is just "your own actor id" (a manager-sees-team view is deferred)

## Conventions
- Domain records validate in their compact constructor (`if (…) throw new IllegalArgumentException(…)`); the application layer uses `Objects.requireNonNull`.
- The acting user is a method parameter (`UserId actor`/`VisibilityScope acting`), never a constructor dependency. **Wave B2**: `actor` (who to attribute an audit entry to) and `acting`/`granterScope` (what the caller may see/manage) are passed as **two separate plain parameters**, never bundled into one type — see Gotchas.
- Constructor injection only, every collaborator `Objects.requireNonNull`-wrapped.
- Management gates repeat the same shape across `UserService`/`GroupService`/`AssignmentService`: "`mayManageOrg()`/`mayManageFleet()` passes, else check `includesGroup`" (wave B6 — was `canManageOrg()` on the bare scope before this wave's migration onto `Authority`) — a reviewer checking one understands the other two.
- `Role`'s ordinal ordering is load-bearing — reordering the enum constants silently changes what "top role" and "max grantable role" mean.
- **D15 audit idiom (wave B2)**: audit only on an actual state change (`setEnabled` only if the flag differs, `setMemberships`/`assign`/`unassign` only for links actually added/changed/removed), following the precedent set by `DefaultCameraPoseService#delete` in `vision-map`. The one deliberate exception: `setPassword`/`changePassword` always audit, since a hash-replacement has no cheap way to detect a no-op.

## Gotchas
- `VisibilityScope`/`AccessDeniedException`/the `AuditEntry` family live in `vision-platform`, not here — it is the authorization *value*, not this context's aggregate. Do not add a field to `VisibilityScope` here.
- `maxGrantableRole` lives on `DefaultUserService` (its only caller), not on `VisibilityScope` — granting a role is user-administration policy, not a fact about what a scope can see.
- `ScopeResolver`'s cycle guard is a real safety net: nothing in `GroupRepositoryPort`/`Group` prevents a cyclic group tree from being stored; `DefaultScopeResolverTest` proves it still terminates.
- `DefaultAssignmentService` reaches warehouse's `AssetService#details(AssetId)`, not its `AssetRepositoryPort` — a cross-context read goes through the published service, even though `details` assembles more than this call needs (devices, recent usages too). If a narrower read (e.g. `AssetService#ownershipOf(AssetId)`) is added for another caller, reconsider swapping this one onto it.
- `AuthService#authenticate`'s "never distinguish the failure reason" behavior is a security requirement — do not add a more specific exception/return type to help a caller show a friendlier error message. `changePassword` is different on purpose (own API-surface entry above) — it is self-service, the caller already proved who they are, so its `Optional.empty()` collapsing wrong-current-password/unknown-id/disabled is a simpler uniform contract, not an anti-leak one.
- `Role`'s frozen four-value order (`VIEWER, PILOT, MANAGER, ADMIN`) backs two independent axes that must not be conflated: ordinal order is *authority* (what `topRole()`/`maxGrantableRole` compare), while `RoleAuthority.capabilitiesOf` and `ScopeResolver` each look up a role by identity, not position — adding a fifth role later must update `RoleAuthority`'s switch explicitly (it does not fall through) or it silently grants zero capabilities.
- **`UserId actor` vs. `VisibilityScope acting`/`granterScope` (wave B2, D15)**: `VisibilityScope` (in `vision-platform`) carries **no identity at all** — only `Kind`, `groups`, `assignedAssets` — so once every gated mutation needed to write an `AuditEntry` (whose `actor`/`targetId` are both mandatory, non-null/non-blank), there was no identity already in scope to attribute the entry to. Considered and rejected: a bundled `ActingUser(UserId, VisibilityScope)` type — rejected because `AssetService` (warehouse) never bundles the two either (its write methods take a plain `UserId actor` with no scope; its read methods take a plain `VisibilityScope` with no actor), and introducing a new bundling type here would be unannounced scope-creep the plan's terse B2 line didn't ask for. If a *third* wave adds a *third* per-call parameter to these same methods, revisit this decision — three parameters repeated across five methods is the point a settings/context record starts paying for itself (java-clean-code §3).
- **`AuditTargetType.GROUP`, not `USER`, for membership grant/revoke** (wave B2): chosen by re-reading the two enum constants' own wave-B1 javadoc — `USER`'s is a closed list (created/enabled-disabled/password/authenticated) that doesn't mention memberships, `GROUP`'s reads as "an org-chart node users hold memberships in," the better semantic fit for "user X was granted/revoked membership in group Y."
- **`hasEnabledAdmin()` is duplicated, not shared, between `DefaultUserService#createFirstAdmin` and `DefaultAuthService#adminExists`** (wave B2) — both are a 3-line predicate over `UserRepositoryPort#findAll()`; sharing it would mean `DefaultUserService` taking a constructor dependency on `AuthService` (or vice versa) for one predicate, which this module's services deliberately never do (each depends only on ports, never on a sibling application service). If the check ever grows non-trivial, extract it to a small package-private helper both constructors can take instead of doing it as `AuthService`.
- **Incidental bug found and fixed, wave B2**: both `DefaultAssignmentServiceTest` and `DefaultScopeResolverTest`'s hand-fake `AssignmentRepositoryPort.unassign()` used to read `byPilot.getOrDefault(pilot, Map.of()).remove(asset)` — `Map.of()` is immutable, so unassigning a pilot with **zero** existing assignment links threw `UnsupportedOperationException`. Pre-existing in both files before B2; nothing exercised the path until B2 added `unassigningANonExistentLinkIsANoOpAndAuditsNothing`. Fixed in both (null-check the per-pilot map before removing) — not a production-code bug, but flagged here in case another test fake was copied from the same original.

## Status
Auth, user/group management, scope resolution, pilot↔asset assignment, and the activity feed are fully implemented. Deferred: `GroupService#tree()` (nested hierarchy view — flat `list()` only), the invite flow.

**AUTH-ROLES-PLAN wave B1 done** (docs/plans/active/AUTH-ROLES-PLAN.md §3.2/§3.3/§3.4) — the authority axis's domain half: `Role.VIEWER` prepended (own entry above; verified by repo-wide grep that no exhaustive `switch`/`.ordinal()`/`Role.values()` call site exists to break); new `AssignmentRole` enum and `Assignment` record (own entries above) for the CREW-CONTROL-PLAN.md IC-2 per-asset seat; `AssignmentRepositoryPort` widened from 5 to 6 methods (`assign` now 3-arg, plus `roleFor`/`assignmentsForAsset`); new `RoleAuthority.capabilitiesOf(Role)` pure function and `ScopeResolver#authorityFor(User)` pairing it with the unchanged `scopeFor`. **Zero change to `scopeFor`'s own resolution logic** — the "staging rule" (own Gotchas/API-surface entries above) keeps `VIEWER` on the pre-existing PILOT-only/no-membership branch until wave B6. One forced, disclosed compile-fix carried inside this wave: `DefaultAssignmentService` passes a temporary `AssignmentRole.PILOT` literal into the widened port (own Gotchas entry above) — B2 removes it.

`./mvnw -B -pl contexts/vision-identity test` — **109/109** green (includes `RoleTest` NEW 3, `RoleAuthorityTest` NEW 7, 5 new `authorityFor` cases in `DefaultScopeResolverTest`, 1 new `UserTest` case, plus the pre-existing suite updated only where the `AssignmentRepositoryPort` signature change forced a fake-port rewrite — no behavior assertion changed). `./mvnw -B -pl core/vision-platform,contexts/vision-identity,contexts/vision-map -am test` also verified green for this wave (cross-module check, since `Role`'s prepend and `Authority`'s new type cross the vision-platform/vision-identity boundary).

**Deviation disclosed**: the plan's B1 item asking for an ArchUnit rule forbidding new `VisibilityScope#canManageOrg`/`canManage`/`canAdminister` call sites could not be added inside this wave — ArchUnit exists only in `station/vision-app` (`ArchitectureTest.java`/`ContextArchitectureTest.java`), which B1's own green command excludes and which this wave's scope explicitly does not touch. Deferred to wave B6, whose green command includes `station/vision-app` — **resolved as moot in wave B6** (see this module's own B6 Status entry below and `core/vision-platform/MODULE.md`'s): the three methods were deleted outright rather than merely guarded, so no ArchUnit rule was ever written.

**AUTH-ROLES-PLAN wave B2 done** (docs/plans/active/AUTH-ROLES-PLAN.md §3.5, D13/D14/D15) — the identity-lifecycle + audit layer on top of B1's authority axis: `User.mustChangePassword` (own API-surface entry above); `AuthService.changePassword`/`adminExists` (NEW); `UserService.create`/`setEnabled` widened with a new `UserId actor` parameter and now audit; `UserService.setMemberships`/`setPassword`/`createFirstAdmin` (all NEW) plus the `FirstAdminSpec` record (NEW); `AssignmentService.assign` threaded with the real `AssignmentRole` parameter (deleting B1's temporary `AssignmentRole.PILOT` literal — that Gotchas entry has been removed, it no longer applies) and a new `UserId actor`; `AssignmentService.unassign` widened with `actor`; `AssignmentService.roleFor` (NEW). Every one of these newly-widened/added methods writes to `AuditTrailPort` per D15 — see the Conventions "D15 audit idiom" entry and each method's own API-surface line for exactly which `AuditAction`/`AuditTargetType` it uses and when it fires versus stays silent. `DefaultAuthService`, `DefaultUserService`, `DefaultAssignmentService` constructors all widened from 2→3 args (added `AuditTrailPort`).

`./mvnw -B -pl contexts/vision-identity test` — **151/151** green, 0 failures, 0 errors (`DefaultAuthServiceTest` 23, `DefaultUserServiceTest` 40, `DefaultAssignmentServiceTest` 16, `DefaultScopeResolverTest` 14 unchanged-count but with a fixed fake, `UserTest` 22, plus the rest of the pre-existing suite untouched in behavior). Includes the incidental test-fake fix described in Gotchas.

**Deviation disclosed**: D15 requires every audit write to carry a real `actor` (`AuditEntry.actor()` is non-null), but `VisibilityScope` — the only "who is asking" value already threaded through these methods — carries no identity. Rather than silently reusing `granterScope`/`acting` as a stand-in identity (impossible — it has none) or inventing an undisclosed bundling type, this wave added a plain, separate `UserId actor` parameter to every gated mutating method that now audits. See the "`UserId actor` vs. `VisibilityScope acting`" Gotchas entry above for the full reasoning and the explicitly-rejected `ActingUser` bundle alternative. This is a necessary elaboration of the plan's D15 requirement, not a silent scope change — flagged here because it widens several signatures beyond what the plan's terse B2 line spelled out parameter-by-parameter.

**Out-of-module call sites now needing updates (not touched by this wave — B3's job)**:
- `station/vision-app`'s auth wiring configuration (constructing `DefaultUserService`/`DefaultAuthService`/`DefaultAssignmentService`) must now pass an `AuditTrailPort` bean as the third constructor argument to all three.
- Any direct caller of `UserService.create`/`setEnabled` and `AssignmentService.assign`/`unassign` outside this module must add a `UserId actor` argument (and `AssignmentService.assign` also needs an `AssignmentRole` argument, no longer implicit).
- 7-arg `new User(...)` call sites will fail to compile until updated for the new `mustChangePassword` component: `station/vision-app/.../PrincipalResolverTest.java` (3 call sites), `station/vision-api/.../UserAdminControllerTest.java` (1 call site), `station/vision-api/.../AuthControllerTest.java` (1 call site). The pre-existing 6-arg call sites (`LiveControllerTest`, `DemoScenarioTest`, `LiveAssetScopingTest`, `LiveMapScopingTest`) remain source-compatible (the convenience ctor now defaults both new fields) and need no change.
- `vision-api`'s not-yet-built `AssignmentController`/`UserAdminController`/`AuthController` (B3's job per the plan) must resolve the caller's own `UserId` at the API edge (java-clean-code §4 — "the user comes from the token") and pass it as `actor` on every call into these widened methods.

**AUTH-ROLES-PLAN wave B6 done** (docs/plans/active/AUTH-ROLES-PLAN.md §3.6) — the two changes this
wave's staging rule had been holding back until every `canManageOrg()`/`canManage()`/`canAdminister()`
call site (repo-wide) had first migrated onto `Authority`:

1. **`UserService.create`/`setEnabled`/`setMemberships`/`setPassword`** and **`GroupService.create`**
   widened their `acting` parameter from `VisibilityScope` to `Authority`; **`AssignmentService.assign`/
   `unassign`** widened `granterScope` the same way. Every gate body changed from the bare-scope
   predicate to the matching `Authority` method (`canManageOrg()`→`mayManageOrg()`,
   `canManage(ownership)`→`mayManageFleet(ownership)`) — own API-surface entries above have the exact
   before/after per method. `GroupService.list` and `UserService.list` stayed on `VisibilityScope`
   (plain reads, no authority question). `private maxGrantableRole(VisibilityScope)` also stayed
   scope-typed — callers now pass `acting.scope()` into it, since picking a grant ceiling is a
   visibility-shaped lookup ("what roles exist inside what this scope can reach"), not an authority
   gate.
2. **`ScopeResolver#scopeFor(User)` widened its `MANAGER`-only branch to `MANAGER` or `VIEWER`**
   (own API-surface entry above) — the reclassification the B1-era javadoc had explicitly deferred to
   this wave, now safe because every deprecated bare-scope predicate has a migrated `Authority`
   equivalent (and, by the time this wave landed, those three predicates were deleted outright — see
   `core/vision-platform/MODULE.md`'s own B6 entry). A `VIEWER`'s wide `GROUPS` scope stays read-only
   in effect: `RoleAuthority.capabilitiesOf(Role.VIEWER)` is still the empty set, so every
   `mayManageOrg()`/`mayManageFleet()`/`mayAdminister()` gate still refuses it on the capability half
   of the check.

`./mvnw -B -pl contexts/vision-identity test` — **153/153** green (151 before this wave +
`DefaultScopeResolverTest`'s 2 new cases, `viewerSeesItsGroupSubtreeJustLikeAManager` and
`authorityForAViewerPairsGroupsScopeWithNoCapabilities` — own API-surface entry above; the rest of the
suite's assertions are unchanged, only the fakes/call sites in `DefaultAssignmentServiceTest`/
`DefaultGroupServiceTest`/`DefaultUserServiceTest` were updated for the widened parameter types).
Plan's full green line (`core/vision-platform,contexts/vision-warehouse,contexts/vision-flight,
contexts/vision-perception,contexts/vision-learning,contexts/vision-map,contexts/vision-identity,
station/vision-api,station/vision-app -am test`) — **BUILD SUCCESS**, this module's 153/153 among
every other listed module's own green count (see `core/vision-platform/MODULE.md`'s B6 entry for the
full per-module tally).

**AUTH-ROLES-PLAN B5-fix** (2026-09-05, live-verification defect) — `User` and `Membership` each
gained `implements java.io.Serializable`, alongside kernel's `UserId`/`GroupId`/`Ownership` (see
`core/vision-kernel/MODULE.md`'s own entry for the full root-cause writeup). `station/vision-app`'s
`VisionUserDetails` holds a whole `User` (plus a derived `Ownership`) as the Spring Security
principal Spring Session JDBC java-serializes on every authenticated request once the real
`spring-boot-session-jdbc` filter is active; `Role` needed no change (an enum is `Serializable` for
free). No behavior change — `./mvnw -B -pl core/vision-kernel,contexts/vision-identity test` —
**153/153** green, unchanged (this module's own suite constructs `User`/`Membership` values but never
serializes them, so the count is unaffected).
