# Copy this file to .env and adjust as needed for local development.
# docker-compose.yml reads these values for the postgres service.

POSTGRES_DB=vision
POSTGRES_USER=vision
POSTGRES_PASSWORD=vision
POSTGRES_PORT=5432
