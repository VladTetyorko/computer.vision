# cv-service

Python gRPC CV inference & training service — real Ultralytics YOLO inference behind `Inference.DetectStream`, with an echo fallback when the model backend is unavailable. `DetectStream` runs each stream's frame receipt and inference concurrently (V-d), bounded by a process-wide inference gate. A `ModelRegistry` (`cv_service/registry.py`, CP-b/`docs/CV-MODELS-PLAN.md`) lazily loads whichever local model(s) `model_id` names — including a comma-separated composite list that runs more than one model per frame — falling back to today's single default model for anything unrecognized.

**Depends on:** grpcio, grpcio-tools, protobuf (Python); `cv` extra: ultralytics (pulls in torch, opencv-python transitively); shared contract `proto/vision/v1/cv.proto` · **Used by:** adapter-cv-grpc, wired end-to-end (`PipelineConfig.model` → `FrameRequest.model_id` — see "Java plumb verification" under CP-b below)
**Build/test:** `python -m cv_service.server` (run). `scripts/test.sh` or `python -m pytest` (test; needs `pip install -e '.[dev]'` at minimum, `-e '.[cv,dev]'` for the mock+real-model suites — see Status). Codegen: `scripts/gen_proto.sh` (regenerates `cv_service/gen/`, not committed). The two local checkpoints this repo ships (`yolo11n.pt`, `orion12l.pt`, both gitignored, provenance: `docs/CV-MODELS-PLAN.md`) exercise the registry/composite tests in `test_registry.py`/`test_real_model.py` when present. Deploying this service on a separate CUDA machine (offloading inference off the laptop): `cv-service/DEPLOY-GPU.md`.

## API surface
### `cv_service/concurrency.py`
Pure-stdlib (`threading` only — no `cv_pb2`, no cv2/numpy/ultralytics), protobuf-agnostic by the same convention as `inference.py`. Two independent primitives, wired to `FrameRequest`/`DetectStream` by `server.py`:
- `class LatestOnlyMailbox[T]` — single-slot, latest-wins handoff between one producer thread and one consumer thread. `put(value)` never blocks/queues: if a value is already waiting unconsumed, it's silently replaced (`.dropped` counts replacements). `get() -> T | None` blocks until a value is available or the mailbox is `close()`d and drained (`None` sentinel — don't use this for a value where `None` is itself meaningful; `FrameRequest`s never are). `close()` unblocks every pending/future `get()` with `None` once any already-pending value has been delivered once.
- `class InferenceGate(max_concurrent: int)` — a plain `threading.Semaphore(max_concurrent)` wrapped as a context manager (`gate.acquire()`), not a thread pool: it gates how many *existing* stream-consumer threads may be inside `detect()` at once, it never creates threads of its own. Raises `ValueError` if `max_concurrent < 1`.
- `DEFAULT_MAX_CONCURRENT_INFERENCES: int` — resolved once at import time from `CV_MAX_CONCURRENT_INFERENCES` (same forgiving-parse idiom as `CV_IMGSZ`: unset/non-numeric/non-positive all fall back), default `min(2, cpu_count // 2)` floored at 1 — see `_default_max_concurrent_inferences`'s own docstring and "V-d: cross-stream concurrency bound" below for the reasoning.
- `process_gate() -> InferenceGate` — the shared, process-wide gate every `InferenceServicer` uses by default; tests construct their own differently-sized `InferenceGate` and inject it instead (`InferenceServicer(detector=..., inference_gate=...)`).

### `cv_service/inference.py`
Independent of the generated protobuf stubs (importable/testable without codegen) — only `cv2`/`numpy`/optionally `ultralytics`.
- `ENCODING_JPEG`, `ENCODING_BGR24: str` — match `ImageEncoding` enum names one-for-one (get via `cv_pb2.ImageEncoding.Name(request.encoding)`).
- `DEFAULT_MODEL = "yolo26n.pt"`, `DEFAULT_CONFIDENCE = 0.25`.
- `class ModelUnavailableError(RuntimeError)` — raised by `YoloDetector.__init__` when `ultralytics` isn't installed, or the model/weights can't be loaded.
- `@dataclass(frozen=True) class Detection(label: str, confidence: float, x: float, y: float, width: float, height: float)` — mirrors proto `Detection`/`BoundingBox`: `x`/`y` top-left corner, `width`/`height` extend right/down, all normalized to `[0, 1]`.
- `def decode_frame(width: int, height: int, encoding: str, data: bytes) -> np.ndarray` — JPEG via `cv2.imdecode`; BGR24 via `np.frombuffer(...).reshape((height, width, 3))`. Raises `ValueError` on bad length/bytes/encoding.
- `def map_detections(result: Any, frame_width: int, frame_height: int) -> list[Detection]` — maps one Ultralytics `Results` (duck-typed: `.boxes.xyxy`/`.conf`/`.cls`, `.names`) to normalized `Detection`s (Ultralytics already rescales `xyxy` to the original frame, so no letterbox un-padding needed); clamps to `[0, 1]`; `[]` if `boxes` is `None`/empty.
- `DEFAULT_IMGSZ = 416` — inference input size (pixels, square), overridable via `CV_IMGSZ`.
- `DEFAULT_DEVICE: str | None` — resolved once at import time from `CV_DEVICE` via `_parse_device` (same forgiving-parse idiom as `CV_IMGSZ`/`CV_MAX_CONCURRENT_INFERENCES`: unset/blank → `None`, meaning "don't pass `device=` to `predict()` at all, let ultralytics auto-select"). Any non-blank value (`"cpu"`, `"cuda"`, `"cuda:0"`, `"0"`, ...) passes through as-is — this module never enumerates/validates device names, ultralytics/torch own that. Explicit-device deployment runbook: `cv-service/DEPLOY-GPU.md`.
- `class YoloDetector(model_name: str | None = None, *, model: Any = None, device: str | None = None)` — loads `ultralytics.YOLO(model_name or $CV_MODEL or DEFAULT_MODEL)` once; `model=` injects a test double, bypassing `ultralytics` entirely. `.model_name -> str`. `.imgsz -> int` — parsed from `$CV_IMGSZ` (default `DEFAULT_IMGSZ`; unset/non-numeric/non-positive all fall back to the default, never raise). `.device -> str | None` — the constructor `device` kwarg if given (falsy falls through), else `DEFAULT_DEVICE`; logged once at INFO at construction (`"cv-service inference device=%s (model=%r)"`, `"auto"` when `None`) alongside the model-load log. After the model is loaded (real or injected), construction runs one warmup `predict()` on a black `np.zeros((imgsz, imgsz, 3), uint8)` frame; a warmup failure (including an invalid/unavailable `device`, e.g. `"cuda:0"` with no GPU — ultralytics raises inside `predict()`) raises `ModelUnavailableError` same as a load failure, no special-casing needed. `.detect(*, width, height, encoding, data, confidence_threshold: float | None = None) -> tuple[list[Detection], int]` — decode → `model.predict(frame, conf=conf, imgsz=self.imgsz, verbose=False, **({"device": self.device} if self.device is not None else {}))` → map; returns `(detections, inference_millis)`. `device=` is passed to *every* `predict()` call (warmup and `detect()`) only when resolved — omitted entirely (not passed as `None`) when unset, keeping behavior byte-identical to before this knob existed. Falsy/`None` `confidence_threshold` uses `DEFAULT_CONFIDENCE`.

### `cv_service/registry.py` (CP-b, `docs/CV-MODELS-PLAN.md`)
Same "no `cv_pb2`" convention as `inference.py` — importable/testable without codegen, but importing it (it imports `cv_service.inference`) requires the `cv` extra. See "Model registry & composite mode" below for the full design/rationale.
- `def discover_roster(base_dir: Path, default_model: str) -> dict[str, str]` — scans `base_dir` for `*.pt` files and `*_openvino_model` directories (one level, non-recursive); always includes `default_model` even if not found there (a not-yet-downloaded alias, or a `CV_MODEL` path elsewhere). Returns `{model_id -> loadable name/path}`.
- `def short_name(model_id: str) -> str` — strips `.pt`/`_openvino_model` for use as a composite-mode label prefix (`"yolo11n.pt"`/`"yolo11n_openvino_model"` → `"yolo11n"`).
- `class ModelRegistry(roster: dict[str, str], default_id: str, *, detector_factory: Callable = YoloDetector)` — lazy `{model_id -> YoloDetector}`; `default_id` must be a `roster` key (raises `ValueError` otherwise — always true when `roster` came from `discover_roster`). `.roster -> dict[str, str]` (a copy). `.default_detector() -> YoloDetector | None` — lazily builds/caches the default; `None` if even it fails to load. `.resolve(requested_model_id: str) -> list[tuple[str, YoloDetector]]` — splits on `,` (composite mode), drops unknown ids (logs once per distinct unknown id, INFO) and ids that fail to load (logs once per id, WARNING) from that specific request, keeping any other known/loadable members; falls back to `[(default_id, default_detector())]` only when *nothing* in the request resolved (empty/unknown/all-failed), and to `[]` if even the default itself is unavailable (caller echoes that frame). `.loaded_ids() -> list[str]` — diagnostics/tests.
- `def detect_composite(resolved: list[tuple[str, YoloDetector]], *, gate: InferenceGate, width, height, encoding, data, confidence_threshold=None) -> tuple[list[Detection], int]` — runs every resolved member's `detect()` (each individually through `gate.acquire()`, same as a lone model), concatenates detections, sums `inference_millis`. When `len(resolved) > 1`, prefixes every label `"{short_name}:{label}"` (e.g. `"orion12l:tank"`) — a single-model call is untouched. Logs each member's own `inference_millis` at DEBUG (not INFO — see Gotchas, this runs every frame).

### `cv_service/server.py`
- `class InferenceServicer(cv_pb2_grpc.InferenceServicer)` — `__init__(detector: YoloDetector | None = None, *, inference_gate: InferenceGate | None = None, registry: ModelRegistry | None = None)`. Two mutually-exclusive ways to give it a model:
  - **`detector=`** (explicit injection — every pre-registry test still uses this): exactly one detector serves every frame regardless of `model_id`; an unrecognized `model_id` logs once (`_warn_once_on_unknown_model`, unchanged) and serves this same detector anyway. No registry, no composite mode.
  - **omitted** (the default/production path): builds a `ModelRegistry` via `_build_default_registry()` (or takes `registry=` directly, for tests) — `request.model_id` (comma-separated for composite) is routed through `registry.resolve()` + `registry.detect_composite()` in `_detect_via_registry()`.
  `inference_gate` defaults to `concurrency.process_gate()` (shared process-wide) — tests inject a smaller/differently-sized one. `DetectStream(request_iterator, context)`: runs real inference per frame when a model is loaded (either path), mapping `Detection`s to `cv_pb2.Detection`/`BoundingBox`; "no model at all" (`self._detector is None and self._registry is None`) and any per-frame exception (including a registry that resolves to nothing for one frame) degrade to the original Phase 0 echo response (stream_id/sequence/timestamp_millis/model_id/model_version copied, empty detections, `inference_millis=0`) — a bad frame or missing model never kills the bidi stream. **When a model is loaded, frame receipt and inference run concurrently within the stream, and every `detect()` call is gated across streams** (composite members individually) — see "V-d: parallel per-stream inference" below for the full design; the echo-only path is unchanged, plain synchronous `for request in request_iterator: yield echo(request)`.
- `def _build_default_registry() -> ModelRegistry | None` — mirrors `_build_default_detector()`'s import/degrade contract: local import (keeps the module importable without the `cv` extra), resolves `CV_MODEL`/`DEFAULT_MODEL`, calls `discover_roster(_MODEL_SEARCH_DIR, default_model)` (`_MODEL_SEARCH_DIR` = `cv_service/server.py`'s grandparent dir, i.e. `cv-service/` — same place weights already land per "CV_MODEL / weights location" below), builds the `ModelRegistry`, eagerly forces the default detector to load (so a broken default degrades the whole servicer to echo mode exactly like before the registry existed), and logs the discovered roster once at INFO.
- `class _StreamReader` — package-private; one instance per active `DetectStream` call with a model loaded. Spawns a daemon thread (`cv-detectstream-reader`) that drains the *rest* of `request_iterator` (everything after the first frame — see below) into a `LatestOnlyMailbox`. `.next()` blocks for the next (latest) frame, `None` at end of stream, re-raises whatever the iterator itself raised. `.stop()` closes the mailbox (best-effort — see Gotchas for what it can't do).
- `class TrainingServicer(cv_pb2_grpc.TrainingServicer)` — unchanged, all three RPCs `UNIMPLEMENTED` (Phase 3).
- `_KEEPALIVE_SERVER_OPTIONS: list[tuple[str, int]]` — module-level constant, HTTP/2 keepalive server options (docs/REMOTE-CV-PLAN.md "Transport decisions" P1): `grpc.keepalive_permit_without_calls=1`, `grpc.http2.min_ping_interval_without_data_ms=10000`, `grpc.keepalive_time_ms=30000`, `grpc.keepalive_timeout_ms=10000`. Passed to `grpc.server(...)` via its `options=` kwarg in `serve()` below.
- `def serve(port: int = DEFAULT_PORT) -> grpc.Server` — builds `grpc.server(futures.ThreadPoolExecutor(max_workers=10), options=_KEEPALIVE_SERVER_OPTIONS)`, registers both servicers, binds, starts, returns. `def main() -> None` — unchanged.

## Conventions
- Generated stubs live under `cv_service/gen/` and are **never committed** — run `scripts/gen_proto.sh` after cloning and whenever `proto/vision/v1/cv.proto` changes.
- `inference.py` never imports `cv_pb2` — `server.py` is the sole translation point between wire types (`cv_pb2.FrameRequest`, `encoding` as an enum int) and `inference.py`'s plain types (`width`/`height`/`encoding: str`/`data: bytes`, `Detection`). `registry.py` follows the same rule (it only ever calls `YoloDetector.detect()` with the same plain kwargs). This keeps both unit-testable without running codegen first.
- `cv_service/inference.py` (and, transitively, `cv_service/registry.py`) imports `cv2`/`numpy` at module scope, so importing either at all requires the `cv` extra. `server.py` only imports them lazily inside `_build_default_detector()`/`_build_default_registry()` — `cv_service.server`, and hence `python -m cv_service.server`, stays importable/runnable without the `cv` extra (echo mode).
- Tests live in `tests/` (not under `cv_service/`), plain `pytest`, fixtures in `tests/conftest.py` (a tiny synthetic frame, no fixture image files on disk).

## V-d: parallel per-stream inference (`docs/MVP2-PLAN.md` §V-d)

**Problem this closes:** pre-V-d, `DetectStream` was `for request in request_iterator: infer(request); yield response` — receiving the next frame off the network only started *after* the current frame's inference finished and its response had been handed back to gRPC, so inference and network transfer never overlapped within one stream; and every stream's `detect()` calls ran fully unbounded/uncoordinated against every other stream's, with nothing protecting the CPU as more streams were added.

**Design chosen — a background reader thread + a 1-slot latest-wins mailbox, per stream, plus a small process-wide semaphore across streams:**

- **Within a stream** (`_StreamReader` + `LatestOnlyMailbox`, both above): `DetectStream` claims the stream's **first** frame directly and synchronously (there's nothing to overlap with before the first frame's own inference even begins), *then* spawns a `_StreamReader` background thread to drain the rest of `request_iterator` into a `LatestOnlyMailbox` while the consumer (the thread gRPC itself drives as the `DetectStream` generator) is inside `detect()`. If a second frame arrives in the mailbox before the consumer has taken the first, it silently replaces it (`.dropped` counts this) — **never a growing backlog**, always the newest available frame. **Response ordering per stream is still always increasing/in-order** (frames are only ever consumed and yielded one at a time, in the order the reader thread received them, minus whichever got overwritten) — this was a deliberate simplicity choice, not a requirement: `adapter-cv-grpc/MODULE.md`'s "Correlation design" section was read first and confirms the Java side matches responses back to pending futures **by their own `sequence` field**, independently per response (`ConcurrentHashMap<Long, CompletableFuture<DetectionResult>>` keyed by sequence, proven under out-of-order server replies by its own `detectCorrelatesInterleavedResponsesBySequence` test) — so mild reordering would have been tolerated too, but keeping this side in-order is simpler to reason about and costs nothing.
  - **Why the first frame is claimed synchronously rather than also going through the reader thread:** confirmed empirically (not assumed) while building this — spawning the reader thread immediately and letting *it* claim frame 0 too creates a real, reproducible race between the reader thread racing ahead through an already-available burst of frames and the consumer's very first `mailbox.get()` call; with a zero-latency in-memory iterator (exactly what this module's own tests use, and plausible for a very bursty real sender too) the reader can legitimately win that race and have already overwritten frame 0 with a later frame before the consumer ever asks for anything. Claiming frame 0 directly removes the possibility entirely, and matches reality: there's no earlier frame to be overlapping the first one's *reception* with anyway.
  - **Why drops are expected to be rare in production, not just "possible in theory":** the mailbox only drops when a *second* new frame arrives while a *first* one is still unconsumed — i.e. only when the sender is at least two frames ahead of what's been inferred+responded-to. `adapter-cv-grpc` (the only real caller) allows at most a small, bounded number of frames in flight per stream at once (see its own MODULE.md "Failure semantics"/correlation design) rather than sending unboundedly far ahead of received responses, so in practice the reader is rarely more than one frame ahead of the consumer. This module's own tests deliberately *do* trigger drops (with instantaneous/fast fake iterators and a gated fake detector) specifically because proving the drop behavior needs to force that "sender got ahead" condition on demand — that's a test-engineering choice, not evidence it's common in production.
- **Across streams** (`InferenceGate`, in `concurrency.py`): every `detect()` call, regardless of which stream it's for, acquires the same process-wide `InferenceGate` (default size `min(2, cpu_count // 2)`, floored at 1, overridable via `CV_MAX_CONCURRENT_INFERENCES`) before running and releases it after — bounding how many inferences may run *concurrently* across the whole process, independent of how many streams happen to be open. This is a plain semaphore around each stream's own existing consumer thread, not a new thread pool — inference was already running on a per-stream thread (gRPC's own `ThreadPoolExecutor`, see `serve()`), this only caps concurrent admission into `detect()`.
  - **Why 2, not `cpu_count`, and the honest OpenVINO/torch interplay:** a single Ultralytics/PyTorch `detect()` call is already internally multi-threaded (intra-op parallelism) — verified in this task's own dev environment, `torch.get_num_threads()` reported `6` on a 12-logical-core box, entirely PyTorch's own default heuristic, nothing this module configures or was asked to configure. So even 2 *concurrent* `detect()` calls can already ask for `2 x 6 = 12` threads' worth of work on that same 12-thread box — deliberately in the right ballpark rather than under- or wildly over-subscribing it, and it's capped at 2 regardless of core count because the realistic demo load (1-3 simultaneous streams) never needs more concurrency than that anyway. **What was NOT independently re-verified**: OpenVINO's own CPU-plugin thread auto-configuration (the Docker image's actual inference backend, see CP-a) — OpenVINO isn't installed in this dev environment (it's `pip install`ed only inside the Dockerfile, deliberately kept out of the `cv` extra), so this reasoning is carried over from the verified PyTorch behavior on the assumption OpenVINO's own default thread auto-tuning is comparably core-proportional (documented OpenVINO CPU plugin behavior, not measured here) — a real difference here would only change the *justification's* precision, not the gate's correctness, since `CV_MAX_CONCURRENT_INFERENCES` is an env var precisely so this number can be tuned without a code change once it's run against the real Docker image under load.
- **Sync `grpc.server`, not `grpc.aio`:** the existing synchronous gRPC server (`futures.ThreadPoolExecutor`-backed, see `serve()`) was kept rather than switching to `grpc.aio`. The concurrency this task needed — overlap within a stream, a bound across streams — is fully expressible with plain `threading` primitives (`LatestOnlyMailbox`, `InferenceGate`) layered onto the existing per-call-gets-its-own-thread model; a switch to `grpc.aio` would have meant rewriting `DetectStream` as an `async def`, `detect()`'s blocking `model.predict()` call would need `run_in_executor`-wrapping to avoid blocking the event loop anyway (it's not async-native), and the Dockerfile/test setup would gain a second async runtime to reason about for no behavioral gain this task needed. Kept simple; revisit only if a future task needs `grpc.aio`-specific features (e.g. true async model backends).
- **Echo mode (no model loaded) is untouched**: `DetectStream` special-cases `self._detector is None` to the original plain `for request in request_iterator: yield echo(request)` loop — no reader thread, no gate, nothing to overlap or bound without a model in the loop.
- **Graceful teardown (client cancel):** verified empirically against this grpc version (not assumed) — cancelling a call unblocks a **background** thread's `next(request_iterator)` within milliseconds, the exact same as it would the main thread; `_StreamReader`'s reader thread is therefore reliably unblocked and exits on its own once a stream ends for any reason, and `DetectStream`'s `finally: reader.stop()` (closing the mailbox) handles the rest. Covered by `test_reader_thread_does_not_leak_on_client_cancel` (real `grpc.server` + `grpc.insecure_channel`, not a fake) and `test_reader_thread_does_not_leak_on_normal_stream_completion`.

**Measured/estimated effect on detection rate:**
- *Per-stream overlap (goal 1), deterministic (fake-clock, immune to real CPU-inference noise — controlled `time.sleep`s standing in for network transit and inference)*: when simulated network transit is comparable to or larger than simulated inference (the case this task's goal targets — hiding real gRPC network latency behind compute), overlap measured **1.3-1.7x faster than the naive receive-then-infer bound** with zero frames dropped (`transit=50ms/infer=50ms/n=5`: 301ms measured vs 500ms naive-serial, 1.66x; `transit=100ms/infer=50ms/n=10`: 1102ms vs 1500ms, 1.36x) — the naive-serial bound is exactly what the pre-V-d `for request in iterator: infer(); yield` loop's wall time equals by construction (each frame pays `transit + infer` in strict sequence), so this is a direct, honest A/B, not just a measurement of the new code in isolation.
- *Cross-stream (goal 2), real Ultralytics `.pt` CPU inference at `imgsz=416`, illustrative only*: 1 stream alone sustained **~10-14 detections/sec** and 3 concurrent streams sustained **~9-10 detections/sec in aggregate** (~3/sec each) on this task's dev machine — **caveat, stated honestly**: this box was under real, visible, concurrent load from other agents' own builds/IDE for the whole task (`uptime` load average 4.7-7.9 on a 12-thread box, several `java`/Maven processes and an IDE running at the same time — confirmed via `ps`), so the absolute numbers vary run-to-run by 30%+ and are **not** a clean isolated benchmark; presented for directional insight, not a performance SLA. The qualitative, more load-independent finding is the one that matters for the goal: 3 streams' *aggregate* throughput does not collapse to a fraction of 1 stream's (it stays in the same ballpark), and each stream gets a fair, bounded share (~1/3 each) rather than any one stream starving the others or all three uncoordinatedly thrashing the CPU against each other — which is exactly what `InferenceGate` is for. The gate's own admission-control correctness (distinct from real-inference throughput) is verified deterministically and without any real-model noise by `test_inference_gate_caps_concurrent_detects_across_four_streams` (4 contending fake streams, gate size 2 → observed peak concurrency is exactly 2, never more).

## Model registry & composite mode (CP-b, `docs/CV-MODELS-PLAN.md`)

**Problem this closes:** before CP-b, `DetectStream` hard-loaded exactly one `YoloDetector` at construction time (`CV_MODEL`/`DEFAULT_MODEL`); any other `request.model_id` was logged once and silently served the same default — there was no way for a client to actually pick between the two local checkpoints this repo ships (`yolo11n.pt`, a general COCO model, and `orion12l.pt`, a user-provided 53MB YOLO12-L military-vehicle model), let alone run both on the same frame.

**Design chosen — a lazy `{model_id -> YoloDetector}` registry, keyed by what's actually on disk, plus a comma-separated composite mode:**

- **Roster discovery** (`registry.discover_roster`, called once by `_build_default_registry()` at servicer construction): scans `cv-service/` (the same directory `YOLO("x.pt")`'s own cwd-relative download/cache behavior already uses — see "`CV_MODEL` / weights location" below, and where the Dockerfile's `WORKDIR` puts things too) for every `*.pt` file and `*_openvino_model` export directory, plus always includes the resolved default (`CV_MODEL` env or `DEFAULT_MODEL`) even if discovery didn't find it there. On this repo's checkout that roster is `{"yolo11n.pt": ..., "orion12l.pt": ...}` (or, inside the Docker image, `{"yolo11n.pt": ..., "yolo11n_openvino_model": ..., "orion12l.pt": ...}` — see the Docker gotcha below) — **logged once at INFO at startup** (`"cv-service model registry roster: [...] (default=...)"`), so an operator can see exactly which ids are routable without reading code.
- **Lazy loading**: no model is constructed (no ultralytics weights load, no warmup `predict()`) until first requested. Each is cached for the process's lifetime after that — the existing single-detector code already paid this cost once at startup for the default; the registry only defers it (for non-default ids) to first use, exactly as the plan specified ("each model pays its warmup once").
- **Routing** (`ModelRegistry.resolve`): `request.model_id` may be one id or a comma-separated list. Every requested id not in the roster is dropped with a once-per-distinct-id INFO log (same log-and-serve-default contract as before CP-b — old clients that never set `model_id`, or clients naming a genuinely unknown model, see zero behavior change). If *nothing* in the request resolved (empty, all-unknown, or a roster id that itself failed to load), falls back to the default model — exactly today's behavior. A **partially**-unknown composite list (e.g. `"orion12l.pt,typo"`) keeps its known member(s) rather than discarding the whole request, since at least one requested model *was* real.
- **Composite mode** (`registry.detect_composite`): every resolved model runs `detect()` on the *same* frame, gated individually through the existing process-wide `InferenceGate` (no separate bound for composite — it's just N gated calls, same as N single-model requests would be), and detections are concatenated. `inference_millis` returned is the **sum** of every member's own `inference_millis` — composite work is strictly more compute run sequentially on one stream's consumer thread, never overlapped (V-d's overlap is about *network receipt* vs. *inference*, not about running two models at once). Confidence filtering already happens per-model inside each `YoloDetector.detect()` call; composite mode adds no second filtering pass.

**Composite mode: no free proto field, so labels get prefixed instead.** The plan asked to check whether `Detection` (`proto/vision/v1/cv.proto`) has a spare field to carry which model produced a detection — it doesn't (`label`/`confidence`/`box` are the only three fields, no reserved-but-unused tag); adding one is a wire-format change out of this task's scope (Java owns codegen concerns, see below). Instead, when more than one model actually ran, every detection's `label` gets prefixed `"{short_name}:{label}"` (e.g. `"orion12l:tank"`, `"yolo11n:person"`) — verified safe before choosing this: `adapter-overlay`'s `Java2DOverlayRenderer` (`adapters/adapter-overlay/src/main/java/com/drones/vision/adapter/overlay/Java2DOverlayRenderer.java`) treats `Detection.label()` as a fully opaque string — it's only ever hashed (`colorForLabel`, `String.hashCode()`) to pick a stable box color and drawn as literal text (`drawLabel`); there is no label catalog/enum/lookup anywhere in the Java overlay path that a prefixed string could break. A useful side effect: because the hash includes the prefix, `"yolo11n:person"` and a hypothetical `"orion12l:person"` get *different* colors automatically, without any dedicated per-model color logic. **Single-model requests are completely unaffected** — `short_name`/prefixing only applies when `len(resolved) > 1`, so this is purely additive for anyone not yet using composite mode.

**Java plumb verification (read-only — vision-application/adapter-cv-grpc/vision-proto were NOT modified by this task):**
`PipelineConfig.model` (a `ModelRef(id, version)`, `vision-domain/src/main/java/com/drones/vision/domain/model/PipelineConfig.java:43`) reaches the wire intact:
- `adapters/adapter-cv-grpc/src/main/java/com/drones/vision/adapter/cvgrpc/GrpcDetectionPort.java:263` — `.setModelId(config.model().id())` (and `:264` `.setModelVersion(config.model().version())`) inside `buildRequest()`, building the `FrameRequest` sent to this service.
- `proto/vision/v1/cv.proto:30` — `FrameRequest.model_id` (`string model_id = 8;`) is exactly the field `cv_service.server`'s `_handle_request`/`ModelRegistry.resolve` reads (`request.model_id`).
- **Verdict: the plumb is intact, zero Java changes needed for registry/composite routing to work today.** The only gap is on the *default value* side, not the plumbing: `PipelineConfig.defaults()` (`PipelineConfig.java:107`) ships `new ModelRef("yolo", "latest")` — `"yolo"` isn't (and was never) a roster id either before or after this task, so it already hit the pre-existing "unknown id, log once, serve default" path and continues to after CP-b unchanged. Wiring an actual model picker (`"yolo11n.pt"` / `"orion12l.pt"` / `"yolo11n.pt,orion12l.pt"`) into `PipelineConfig.model` is a UI/settings-surface task (plan item 4, out of this task's scope — no `ModelRef(` call site outside `PipelineConfig.defaults()`/`GrpcDetectionPort`'s response-mapping was found repo-wide).

**Measured per-model latency and memory (this dev machine, 12 logical cores, `torch.get_num_threads()`=6, `imgsz=416`, plain PyTorch `.pt` weights — not the Docker image's OpenVINO IR; `uptime` load average ~2.9-4.3 during the run, i.e. some background contention, numbers are directional not a clean isolated benchmark, same honest caveat as V-d's own throughput numbers above):**

| model | construct+warmup | detect() mean (20 calls, 64x48 synthetic frame) | detect() min/max | RSS after load |
|---|---|---|---|---|
| `yolo11n.pt` (general, 5.6MB) | ~4.0s | **58.5ms** | 46 / 96ms | ~416MB (+363MB from Python/torch baseline) |
| `orion12l.pt` (military YOLO12-L, 53MB) | ~0.9s (torch/cv2 already warm from loading yolo11n first) | **343ms** | 281 / 451ms | ~608MB (+184MB more, both models resident) |
| composite (`yolo11n.pt,orion12l.pt`) | n/a | **~346ms** (sum of both members' own `inference_millis`) | 303 / 392ms | (both already resident above) |

`orion12l` is confirmed **~5-6x slower per frame** than `yolo11n` on CPU — expected for a YOLO12-**L**arge vs. a YOLO11-**n**ano checkpoint, and directly explains why composite mode's cost is dominated by whichever member is the heavier one, not split evenly. Both models loaded simultaneously (the composite/registry-with-both-cached scenario) cost **~608MB RSS** total on this process — plan for at least that much headroom (plus per-stream buffers) if composite mode is enabled by default rather than opt-in. Reproduced via a throwaway benchmark script (not committed — ad-hoc, not part of the test suite) exercising `YoloDetector`/`ModelRegistry`/`detect_composite` directly against both real local `.pt` files.

## Gotchas
- **`sys.path` gotcha** (unchanged): `protoc`'s Python codegen emits imports rooted at the proto package path (`from vision.v1 import cv_pb2`) — the generated tree's *root* (`cv_service/gen`) must be on `sys.path`. `gen_proto.sh` touches `__init__.py` everywhere so the tree imports as regular packages.
- **protobuf version-scheme note** (unchanged): `protobuf>=5.26.1,<6` vs Java's `protobuf-java` 3.25.5 is not a real mismatch — don't "fix" it.
- **Dockerfile build context** (unchanged): must build from the **repository root** — `docker build -f cv-service/Dockerfile -t cv-service .`.
- **`opencv-python` vs `opencv-python-headless`**: do NOT add `opencv-python-headless` to the `cv` extra even though it would be preferable for Docker. `ultralytics` hard-depends on plain `opencv-python` (the GUI-enabled build), and pip has no notion that the two packages both provide `cv2` and conflict — installing both leaves whichever installs last silently overwriting the other's files on disk (confirmed locally: uninstalling one after both were present broke `cv2` entirely, `ImportError`). Instead the Dockerfile installs `libgl1`/`libglib2.0-0` at the OS level so plain `opencv-python` imports cleanly in the `python:3.12-slim` image.
- **`CV_MODEL` / weights location**: `YOLO("yolo11n.pt")` (a relative/known-alias name) downloads into the **current working directory** if not already present there — not a global cache dir. Locally this drops `yolo11n.pt` under `cv-service/` when running the server/tests from there; it's gitignored (`*.pt`/`*.onnx`/`*.engine`) — never commit it. The Dockerfile pre-downloads the `.pt` weights the same way at build time from `WORKDIR /app/cv-service`, then (CP-a) exports them to an OpenVINO IR directory (`yolo11n_openvino_model/`, also gitignored via `*_openvino_model/`) and points `CV_MODEL` at that directory instead — both the `.pt` source and the exported IR end up baked into the image so the container needs no network at runtime. If the export step ever proves unbuildable (network/size), drop the `yolo export` line and leave `CV_MODEL=yolo11n.pt` — the server falls back to plain PyTorch inference, just slower (documented inline in the Dockerfile).
- **`confidence_threshold` ambiguity**: proto3 `float` means an unset `FrameRequest.confidence_threshold` reads as `0.0`, indistinguishable from "explicitly zero". Both `server.py` and `YoloDetector.detect()` treat falsy/`None` as "use `DEFAULT_CONFIDENCE` (0.25)" — a caller wanting threshold `0.0` (accept everything) isn't representable on the wire today; not expected to matter for the demo.
- **First-request latency**: model construction + first `predict()` call include Ultralytics warmup (~1s observed locally even with cached weights); `inference_millis` only measures the `predict()` call itself, not model construction. As of CP-a, `YoloDetector.__init__` pays this cost itself (a dedicated warmup `predict()` on a black frame, at `self.imgsz`) so the *first real* `detect()` call no longer does — this is what protects adapter-cv-grpc's 2s response timeout from tripping into outage backoff right at stream start. The warmup call runs for injected `model=` doubles too, not just the real model.
- **`imgsz`/OpenVINO export coupling (CP-a)**: `CV_IMGSZ` (default `416`, must be a multiple of 32 by Ultralytics convention — not enforced in code) is passed as `imgsz=` to every `predict()` call, including warmup. The Dockerfile exports the model to OpenVINO IR at build time with a **fixed** input size baked in (`yolo export model=yolo11n.pt format=openvino imgsz=416`) — that export `imgsz` MUST equal the runtime `CV_IMGSZ`, or inference silently runs against the wrong size. If you change one, change both (Dockerfile `ENV CV_IMGSZ=416` and `DEFAULT_IMGSZ` in `inference.py` are the two places). Local/dev (non-Docker) runs plain `.pt` weights via `ultralytics.YOLO`, which has no such fixed-size constraint — `openvino` is intentionally NOT in the `cv` extra in `pyproject.toml`, only `pip install`ed directly in the Dockerfile.
- **Warmup failure semantics**: a warmup `predict()` exception (bad model state, OpenVINO backend mismatch, etc.) raises `ModelUnavailableError` from `YoloDetector.__init__`, exactly like a load failure — `server.py`'s `_build_default_detector()` catches it and degrades to the echo fallback rather than crash-looping.
- **`LatestOnlyMailbox` drops a frame with no response at all, ever, for that frame's `sequence`** (V-d): a dropped frame never reaches `detect()`, so no `DetectionResponse` is ever produced for it. On the Java side this means that specific pending future simply times out (`adapter-cv-grpc`'s own `RESPONSE_TIMEOUT_SECONDS=2s`, see its MODULE.md) rather than being answered quickly — expected and harmless given how rare drops are meant to be in practice (see "V-d" above), but worth knowing if a dropped-frame's timeout ever shows up in logs: it is not a bug, it's the latest-wins design working as intended.
- **`CV_MAX_CONCURRENT_INFERENCES` is a startup-time knob, not a live one** (V-d): `concurrency.py` resolves it once at **import time** into `DEFAULT_MAX_CONCURRENT_INFERENCES`/`process_gate()`'s module-level singleton — changing the env var after the process has started has no effect (matches `CV_MODEL`/`CV_IMGSZ`'s own existing "read once at construction" precedent, just at import time instead of `YoloDetector` construction time since the gate has no natural "construction" event of its own to hook).
- **`CV_DEVICE` is also resolved once at import time** (same pattern as `CV_MAX_CONCURRENT_INFERENCES` immediately above), into `inference.DEFAULT_DEVICE` — but unlike that gate, `YoloDetector.__init__` reads it fresh per-instance (`device or DEFAULT_DEVICE`), so a *new* `YoloDetector`/`ModelRegistry` entry constructed later in the same process still picks up whatever `DEFAULT_DEVICE` resolved to at import (env changes after process start still have no effect — there's no re-parse point). A bad/unavailable device (e.g. `"cuda:0"` with no GPU present) is not special-cased: it surfaces as a normal warmup-`predict()` exception, which `YoloDetector._warmup` already converts to `ModelUnavailableError` like any other load/warmup failure — `server.py` degrades to echo mode exactly as it would for a bad `CV_MODEL`.
- **Test-only nuance if you add more `DetectStream` tests with multiple frames**: any test iterator delivering 2+ frames with no artificial per-item delay races `_StreamReader`'s background thread against however fast the test happens to get scheduled — the reader can (and, empirically, sometimes does) put and overwrite several items before the consumer's first `get()` call, which is `LatestOnlyMailbox` correctly doing its job against a producer that (in the test) is unrealistically instantaneous, not a bug. Tests asserting an exact multi-frame response count/order use a small artificial per-item delay (`_PacedIterator`/`_DelayedIterator` in the test files) to avoid this — a real network is never actually instantaneous, so this is purely a test-engineering concern, not a production one.
- **Registry base dir is resolved from `__file__`, not `os.getcwd()`** (CP-b): `server._MODEL_SEARCH_DIR = Path(__file__).resolve().parent.parent`, i.e. always `cv-service/` regardless of the directory the process happens to be launched from — deliberately more robust than the cwd-relative behavior `ultralytics.YOLO("x.pt")` itself uses for *downloading* a not-yet-cached alias (see "`CV_MODEL` / weights location" above), which is still cwd-dependent and unchanged; discovery just needs to reliably *find* whatever's already sitting in the checkout, not decide where a fresh download lands.
- **Docker image bakes an OpenVINO export only for the default model (`yolo11n`)** (unchanged by CP-b): the Dockerfile's `yolo export ... format=openvino` step (CP-a) only runs for `yolo11n.pt`; `orion12l.pt` is present in the image (the `COPY cv-service/ ./cv-service/` step copies it, same as any other checked-in file) but the registry would load it via plain PyTorch inside the container too, same as locally — functional, just without OpenVINO's ~2-3x CPU speedup. Exporting `orion12l.pt` to OpenVINO IR as well was left out of this task's scope (not requested; would roughly double the image's build time/disk for the export step) — revisit if `orion12l`/composite mode moves from "available" to "default" in Docker.
- **`detect_composite` logs each member's `inference_millis` at DEBUG, not INFO** (CP-b): this runs once per resolved model per *frame* (so up to 10+/sec per stream at the sampling rate `PipelineConfig.inferenceFps` targets) — INFO would spam logs at anything above a trivial frame rate. DEBUG keeps it available (`logging.getLogger("cv_service.registry").setLevel(logging.DEBUG)`) without being on by default; contrast with the once-per-distinct-id unknown-model/load-failure warnings elsewhere in this module, which stay at INFO/WARNING because they're bounded (once per id, not once per frame).
- **`test_real_model.py`'s per-local-model and composite smoke tests skip (not fail) if a weights file is absent** (CP-b): `orion12l.pt` is a large (53MB), user-provided, gitignored file (see docs/CV-MODELS-PLAN.md's provenance note — **never fetched from the network**, only ever the checked-out local file) — a clone without it present still gets a green (skipped, not failed) test suite, same contract `test_real_model.py`'s original `real_detector` fixture already established for "ultralytics not installed at all".
- **grpc Python servers GOAWAY ("too_many_pings") a client that pings with no active calls, by default** (docs/REMOTE-CV-PLAN.md "Transport decisions" P1): `adapter-cv-grpc`'s `GrpcDetectionPort` pings every `KEEPALIVE_TIME_SECONDS` (20s) *including while idle* (`keepAliveWithoutCalls`, needed because a stream mid-`StreamPipeline`-outage-backoff has no in-flight RPC to piggyback a ping on) — without `grpc.keepalive_permit_without_calls=1` in `_KEEPALIVE_SERVER_OPTIONS`, this server would tear down that exact connection as abusive, defeating the client-side tuning entirely rather than helping it. `grpc.http2.min_ping_interval_without_data_ms=10000` is deliberately half the client's 20s interval (jitter headroom), not equal to it.
- **`test_serve_passes_keepalive_options_to_grpc_server` fakes every collaborator `serve()` touches except `grpc.server` itself** (monkeypatches `InferenceServicer`/`TrainingServicer`/`add_*Servicer_to_server` to no-ops): calling the real `InferenceServicer()` default constructor would eagerly load whatever model `_build_default_registry()` finds on disk, which is slow and environment-dependent — irrelevant to what this test actually checks (the `options=` kwarg reaching `grpc.server`).

## Status
Real inference (Ultralytics YOLO11n, CPU by default) implements `Inference.DetectStream` per `docs/MVP1-PLAN.md` §C7 bullet 1; falls back to the Phase 0 echo stub when the `cv` extra is absent or the model can't load, so the service never crash-loops for lack of a model. `Training` is still stubbed `UNIMPLEMENTED` (Phase 3). **`docs/MVP2-PLAN.md` §V-d done**: `DetectStream` decouples per-stream frame receipt from inference (background reader thread + `LatestOnlyMailbox`) and bounds cross-stream concurrent inference (`InferenceGate`) — see "V-d: parallel per-stream inference" above for the full design, what was verified against `adapter-cv-grpc`'s correlator, and measured/estimated numbers. **`docs/CV-MODELS-PLAN.md` items 1-2 done (CP-b)**: model registry (`cv_service/registry.py`) replaces the single hard-loaded detector with a lazy `{model_id -> YoloDetector}` keyed by locally-discovered `*.pt`/`*_openvino_model` entries, plus comma-separated composite mode (run N models on one frame, concatenate + label-prefix detections) — see "Model registry & composite mode" above for the full design, the Java-side plumb verification (verdict: already wired, zero Java changes needed), and measured `yolo11n.pt`/`orion12l.pt` latency+RSS numbers. Item 3 (concurrency/perf) reuses the existing `InferenceGate` unchanged, gating composite members individually; item 4 (UI model picker) is out of this task's scope (a settings-surface change, not cv-service). **`docs/REMOTE-CV-PLAN.md` P0 items 2-3 done**: explicit `CV_DEVICE` knob (`inference.DEFAULT_DEVICE`/`YoloDetector.device`, see API surface above) makes the inference device observable/settable instead of relying on ultralytics' own silent auto-selection, for remote GPU-box deployment — see `cv-service/DEPLOY-GPU.md` for the deployment runbook (install, run, systemd unit, network, laptop-side config, verification steps including the CPU-baseline comparison table). Item 2 (deploying cv-service to a GPU machine) needed no code change — see that doc. **`docs/REMOTE-CV-PLAN.md` "Transport decisions" P1 done**: `serve()` now passes `_KEEPALIVE_SERVER_OPTIONS` to `grpc.server(...)` (see API surface and Gotchas above) so this service permits and reciprocates `adapter-cv-grpc`'s HTTP/2 keepalive pings instead of GOAWAY-ing the pinging client — verification runbook in `cv-service/DEPLOY-GPU.md` §6 item 5.

Test suite (`tests/`): `test_inference.py` + `test_server.py` + `test_server_concurrency.py` + `test_registry.py` are mock-based (need `cv2`/`numpy`, i.e. the `cv` extra's transitive opencv-python, but not `ultralytics`/`torch` — they inject fake model/detector doubles); `test_concurrency.py` is pure-stdlib (no `cv` extra needed at all); `test_real_model.py` is a real-weights integration test, auto-skipped/individually-skipped unless `ultralytics` is importable and the relevant weights (default, `yolo11n.pt`, or `orion12l.pt`) load. Verified locally end-to-end: full `pip install -e '.[cv,dev]' --extra-index-url https://download.pytorch.org/whl/cpu` + `scripts/gen_proto.sh` + `scripts/test.sh` all green (**108/108** — 106 prior + 2 new keepalive tests in `test_server.py`: `_KEEPALIVE_SERVER_OPTIONS`'s values, and `serve()` actually passing them to `grpc.server`'s `options=` kwarg; run twice with no flakes observed). `CV_DEVICE` env-resolution tests monkeypatch `inference.DEFAULT_DEVICE` directly (it's resolved once at import time, same as `DEFAULT_MAX_CONCURRENT_INFERENCES` — see Gotchas) rather than the env var + a module reload. `python -m cv_service.server` confirmed to start and serve echo cleanly with the `cv` extra absent, and to load the real model and detect on a real JPEG frame end-to-end through `InferenceServicer.DetectStream` with the extra present (unchanged by V-d/CP-b); `CV_DEVICE=cpu`/an invalid device string manually verified against a real `yolo11n.pt` load (resolved-device log line present; invalid device correctly raises `ModelUnavailableError` from warmup, degrading to echo, no special-casing needed). `docker build -f cv-service/Dockerfile .` was not re-verified in this task — no Dockerfile change was needed for the keepalive options (`grpc.server()` behavior only, no OS/package change). CP-b's registry discovery works against the same on-disk layout the image already produces (see "Docker image bakes an OpenVINO export only for the default model" gotcha above for the one known gap, `orion12l` not yet OpenVINO-exported in the image). Real-GPU-box deployment itself (DEPLOY-GPU.md's runbook end-to-end, `nvidia-smi` confirmation, CUDA latency numbers, and the new keepalive network-drop verification) was not executed in this task — no GPU hardware/second network path in this dev environment; the runbook is written from the existing CPU-side contract plus verified `CV_DEVICE`/keepalive plumbing, not from a live CUDA/flaky-link run.
