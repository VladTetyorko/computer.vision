# adapter-overlay

Placeholder — frame annotation adapter (bounding boxes, labels, confidence, telemetry OSD).

**Depends on:** vision-domain · **Used by:** none yet (Phase 2 wiring)
**Build/test:** `./mvnw -B -pl adapters/adapter-overlay test`

## API surface
None yet — module contains only `package-info.java`.

## Status
Placeholder until Phase 2.
