# adapter-cv-grpc

Placeholder — gRPC client to the Python CV service (`DetectionPort`, `ModelRegistryPort`).

**Depends on:** vision-domain, vision-proto · **Used by:** none yet (Phase 2 wiring)
**Build/test:** `./mvnw -B -pl adapters/adapter-cv-grpc test`

## API surface
None yet — module contains only `package-info.java`.

## Status
Placeholder until Phase 2. Will implement `DetectionPort`/`ModelRegistryPort` over the `Inference`/`Training` gRPC stubs generated by `vision-proto`.
