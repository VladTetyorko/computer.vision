# adapter-persistence

Placeholder — JPA/Postgres persistence adapter (devices, detections, events).

**Depends on:** vision-domain · **Used by:** none yet (Phase 2 wiring)
**Build/test:** `./mvnw -B -pl adapters/adapter-persistence test`

## API surface
None yet — module contains only `package-info.java`.

## Status
Placeholder until Phase 2.
