# vision-domain

Framework-free domain: immutable models plus driven (`port.out`) ports.

**There is no `port.in` package.** Driving interfaces collapsed into one service interface + one implementation per area, which live in `vision-application`; their commands and read models are top-level records there. Driven `*Port` interfaces stay, because each genuinely has several implementations. See `.claude/skills/java-clean-code/SKILL.md`.

**Depends on:** nothing but `java.base` (no third-party/framework imports anywhere in this module) · **Used by:** vision-application, all adapters, vision-app, vision-api
**Build/test:** `./mvnw -B -pl vision-domain test`

## API surface

### `com.drones.vision.domain.model`
- `enum AccessLevel` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — `VIEW | CONTRIBUTE | MANAGE`, declared least&rarr;most permissive; ordinal ordering is meaningful (mirrors `Role`'s own "declaration order is meaningful" contract) — `MapAccessPolicy` (vision-application, Wave B) takes the max across every applicable {@link LayerGrant}/rule and answers "at least CONTRIBUTE" checks via ordinal comparison; reordering these constants would silently change what "more access" means. Pure marker otherwise, no dedicated test (same convention as `Role`/`Capability`/`EventType`)
- `enum Affiliation` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — `FRIENDLY | HOSTILE | NEUTRAL | UNKNOWN`, APP-6-inspired friend/enemy symbology for a `Mark`; orthogonal to `MarkKind` ("whose it is" vs "what it is"); own assets always render as `FRIENDLY` (a web-layer rule, not modeled here). Pure marker, no dedicated test
- `record AnnotatedFrame(VideoFrame frame, List<Detection> detections, Telemetry telemetry)` — telemetry nullable; detections defensively copied
- `record Annotation(String label, BoundingBox box, AnnotationSource source)` (docs/CV-TRAINING-PLAN.md §1, Wave T1) — one ground-truth box on a captured `TrainingSample`; reuses `BoundingBox`, deliberately **no confidence** (this is human-confirmed truth, not a model's guess) unlike `Detection`; `label` is free-form (matches `Detection#label`), not a `CategoryId` — open-vocab detection labels (e.g. `"skyscraper"`) aren't categories; must not be blank; `source` records whether this annotation was kept from a live `MODEL` detection or drawn/edited by the `OPERATOR`; whether `label` is a member of its sample's dataset's `classes` is an application-layer check (`LabelingService.label`), not this record's
- `enum AnnotationSource` (docs/CV-TRAINING-PLAN.md §1, Wave T1) — MODEL, OPERATOR; provenance only, no behavior
- `record Asset(AssetId id, String displayName, CategoryId category, Ownership ownership, Set<DeviceId> devices, Map<String,String> attributes, LifecycleState state)` — devices must be non-empty; 6-arg convenience ctor defaults `state=ACTIVE`; `isActive()`, `isDeleted()`, `withDevices(Set)`, `withAttributes(Map)`, `withDetails(String,CategoryId,Map)`, `withState(LifecycleState)` return copies
- `record AssetId(UUID value)` — `static random()`, `static of(String)`
- `record AssetImage(byte[] data, String contentType)` (docs/UX-REWORK-PLAN.md §U-d item 3) — the asset's user-facing photo, one per asset, no history; `data` non-empty, `contentType` non-blank (which types are actually accepted, plus the max-size cap, is a wire-boundary concern enforced by `vision-api`, not this record); `data()` overridden to return a fresh `data.clone()` on every access, mirroring `VideoFrame#data()`'s defensive-copy-on-every-access discipline (adapted for `byte[]` — no read-only-view equivalent for arrays)
- `record AssetUsage(UsageId id, AssetId assetId, Instant startedAt, Instant endedAt, GeoPosition startPosition, GeoPosition lastPosition, long sampleCount, StreamId streamId)` — endedAt/startPosition/lastPosition/**streamId** nullable, endedAt≥startedAt if present; a 7-arg convenience ctor defaults `streamId=null` (same "N-1-arg convenience ctor" idiom as `Asset`'s 6-arg one); `closed(Instant)`, `withPositions(GeoPosition,GeoPosition)`, `withSampleCount(long)` all preserve `streamId` unchanged. `streamId` (docs/MVP2-PLAN.md §R, R-a2) is the `StreamId` of the stream whose start opened this usage, recorded once by `UsageTracker` (vision-application) and never changed afterward — it exists purely so `ReplayService` can join a finished usage back to its `DetectionResult`s; `null` for a legacy usage or one opened by an asset with no video device
- `enum AuditAction` — CREATED, UPDATED, DEACTIVATED, ACTIVATED, DELETED, RESTORED
- `record AuditEntry(AuditId id, Instant occurredAt, UserId actor, AuditAction action, AuditTargetType targetType, String targetId, String summary, Map<String,String> details)` — immutable trail line; `static of(...)` stamps id + now; `details` carries `before → after` for edits
- `record AuditId(UUID value)` — `static random()`, `static of(String)`
- `enum AuditTargetType` — ASSET, DEVICE, **DATASET, MODEL** (docs/CV-TRAINING-PLAN.md Wave T1 — a training dataset / a trained CV model version in the registry) — opaque `targetId` string, so new auditable kinds need no storage change
- `record BearingDistance(double bearingDegrees, double distanceMeters)` (docs/TACTICAL-MARKS-PLAN.md §1, Wave M1) — the output of `GeoProjection.bearingDistance`: initial bearing degrees [0,360) clockwise from true north, plus great-circle distance in meters (never negative); validated in its own compact ctor like every other record here, even though it's typically machine-constructed
- `record BoundingBox(double x, double y, double width, double height)` — each component in [0,1]
- `enum Capability` — VIDEO, TELEMETRY, PTZ, AUDIO
- `record CategoryId(String slug)` — must match `[a-z0-9]+(-[a-z0-9]+)*`; no random-id factories (reference-data key, not a generated id)
- `record ChannelMap(List<ControlBinding> bindings)` (docs/RC-CONTROL-PHASE1-PLAN.md §1/§5, RC-CONTROL Phase 1 R1) — a full axis/button → RC-channel map; `bindings` defensively copied via `List.copyOf`; `static defaultMap()` returns the frozen v1 table (gamepad axes 0–3 → RC channels 1–4 centered at 1500µs, gamepad buttons 0–3 → RC channels 5–8, channels 9–18 unused — see `RcChannels`'s own entry for why); `RcChannels apply(List<Double> axes, List<Double> buttons)` reads each binding's source value by index (a missing/out-of-range/`null` index reads as `0.0` — center for an axis, unpressed for a button — never throws), maps it through `ControlBinding#toMicros`, and assembles an `RcChannels` spanning channels 1..N (N = the highest `rcChannel` any binding targets, or `1` if `bindings` is empty); a channel no binding targets is `RcChannels#IGNORE`
- `enum CommandResult` (docs/DRONE-INFRA-PLAN.md I-e Stage 1) — `ACCEPTED`/`NO_ACK`, the outcome of a `FlightCommandPort` call; an explicit refusal is deliberately not a value here — see the port's own javadoc for why that is instead a thrown `IllegalStateException`. Pure marker enum, no behavior, no dedicated test (same convention as `Capability`/`EventType`/`PixelFormat`)
- `record ControlBinding(Source source, int sourceIndex, int rcChannel, int minMicros, int centerMicros, int maxMicros, double deadband, boolean reversed)` (docs/RC-CONTROL-PHASE1-PLAN.md §1, RC-CONTROL Phase 1 R1) — one physical gamepad control (`enum Source { AXIS, BUTTON }`) mapped to one RC channel with linear calibration; compact ctor validates `rcChannel` 1..18, `minMicros<=centerMicros<=maxMicros` each within `[RcChannels.MIN_MICROS, RcChannels.MAX_MICROS]`, `deadband` in [0,1], `sourceIndex>=0`; `int toMicros(double normalized)` — `AXIS` clamps to [-1,1], reverses/deadband-snaps around 0, then piecewise-linear-maps `[-1,0]`→`[minMicros,centerMicros]` and `[0,1]`→`[centerMicros,maxMicros]`; `BUTTON` clamps to [0,1], reverses as `1-normalized`, deadband-snaps near 0 (unpressed), then linear-maps `[0,1]` straight onto `[minMicros,maxMicros]` — `centerMicros` plays no part in a button's own mapping (see Gotchas for why the default map still gives it a value); both clamp the microsecond result to `[minMicros,maxMicros]`
- `record Dataset(DatasetId id, String name, CategoryId targetCategory, List<String> classes, Ownership ownership, DatasetStatus status, Instant createdAt)` (docs/CV-TRAINING-PLAN.md §1, Wave T1) — a named accumulation of labeled `TrainingSample`s aimed at improving detection of one target; `targetCategory` nullable (the optional `CategoryId` this dataset improves, e.g. `"building"`); `classes` is the **ordered** YOLO class list (an annotation's export class index is its label's position here), free-form strings — not `CategoryId` slugs, matching `Annotation#label`'s open-vocab reasoning — defensively copied preserving order, may be empty; `ownership` scopes it like `Asset#ownership`, with the same "no scope filtering baked into the repository" convention — group-scoped visibility is `vision-application`'s job
- `record DatasetId(UUID value)` (docs/CV-TRAINING-PLAN.md §1, Wave T1) — `static random()`, `static of(String)`
- `enum DatasetStatus` (docs/CV-TRAINING-PLAN.md §1, Wave T1; `EXPORTING` removed docs/CV-TRAINING-V2-PLAN.md §3, Wave W1) — OPEN, ARCHIVED. Nothing ever set `EXPORTING` — it existed only for a manual-export step that no longer exists (see `DatasetUploadPort`)
- `record DatasetUpload(DatasetId datasetId, Instant uploadedAt, int sampleCount, long sizeBytes)` (docs/CV-TRAINING-V2-PLAN.md §3, Wave W1) — what one completed `DatasetUploadPort.upload` run delivered to the training host; the replacement for `DatasetExport` — a delivery receipt for a gRPC upload rather than a manifest for a filesystem artifact; `datasetId`/`uploadedAt` non-null, `sampleCount`/`sizeBytes` non-negative
- `record Detection(String label, double confidence, BoundingBox box, ModelRef model)` — confidence [0,1]
- `record DetectionEvent(DetectionEventId id, StreamId streamId, AssetId assetId, String label, double peakConfidence, Instant firstSeen, Instant lastSeen, DetectionEventState state, GeoPosition position)` (docs/MVP2-PLAN.md §E, E-a) — a debounced "label X was seen for a while" occurrence, opened/evolved by `DetectionEventEngine` (vision-application); `assetId`/`position` nullable (unresolvable device→asset, or no telemetry yet); `peakConfidence` [0,1]; `lastSeen`≥`firstSeen`; `withObservation(Instant,double)` advances `lastSeen`/raises `peakConfidence` (max), `closed()` flips `state`→CLOSED without moving `lastSeen`. `position` is stamped once, at open time, and never updated afterward — see `DetectionEventEngine`'s javadoc
- `record DetectionEventId(UUID value)` — `static random()`, `static of(String)`
- `enum DetectionEventState` — OPEN, CLOSED
- `record DetectionQuery(StreamId streamId, Instant from, Instant to, String label, int limit)` — all but `limit` nullable = "don't filter on this"; limit must be positive
- `record DetectionResult(StreamId streamId, long frameSequence, Instant capturedAt, List<Detection> detections, Duration inferenceLatency)` — frameSequence≥0, latency≥0
- `record DeviceCategory(CategoryId id, String name, CategoryId parent, List<String> attributeHints)` — parent nullable (top-level)
- `record DeviceId(UUID value)` — `static random()`, `static of(String)`
- `record Device(DeviceId id, String name, Set<Capability> capabilities, StreamDescriptor stream, LifecycleState state)` — 4-arg convenience ctor defaults `state=ACTIVE`; `isActive()`, `isDeleted()`, `withDetails(String,Set,StreamDescriptor)`, `withState(LifecycleState)`
- `record DiscoveredDevice(String method, String name, URI address, CategoryId suggestedCategory, StreamDescriptor suggestedStream, Map<String,String> details)` — suggestedCategory/suggestedStream nullable (mechanism may lack enough info, e.g. ONVIF needs creds)
- `enum DrawKind` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — `LINE | POLYGON | ARROW | TEXT`; drives `Drawing`'s own point-count invariant (see its entry below). Pure marker otherwise, no dedicated test
- `record Drawing(DrawingId id, LayerId layerId, DrawKind kind, List<GeoPosition> points, String label, String colorToken, Ownership ownership, Instant createdAt)` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — a line/polygon/arrow/text annotation on a `MapLayer`, the substrate for a later "plans" slice; `points`' required count depends on `kind` — `LINE`/`ARROW` &ge;2 (open polyline), `POLYGON` &ge;3 (closed area, implicitly closed like `GeofenceZone#polygon`), `TEXT` exactly 1 — defensively copied; `label` required non-blank only for `TEXT` (the text a text annotation shows), optional otherwise (blank normalizes to `null`, mirroring `Mark#note`), at most `MAX_LABEL_LENGTH` (120) chars either way; `colorToken` optional lower-case-kebab UI design-token name (e.g. `"accent"`, `"danger"`) — **not** a hex value — at most `MAX_COLOR_TOKEN_LENGTH` (30) chars, blank normalizes to `null`, validated with the same kebab-case pattern `CategoryId` uses; `ownership`/`createdAt` non-null. `withGeometry(List<GeoPosition>)` — replaces points only (re-validates against `kind`), everything else (including label/colour) untouched; `withDetails(String,String)` — replaces label/colorToken only, geometry/identity/layer/ownership/createdAt untouched — the same "drag vs. relabel are separate acts" split `Mark#withPosition`/`withDetails` follow
- `record DrawingId(UUID value)` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — `static random()`, `static of(String)`; typed identity for a `Drawing`, mirrors `MarkId`/`LayerId`/`ZoneId` exactly
- `record Event(String id, StreamId streamId, Instant at, EventType type, String message, Map<String,String> attributes)` — **streamId nullable** (device-level events); `static of(StreamId,EventType,String)` generates id+now(); **not the same thing as `DetectionEvent`** — see Gotchas
- `record EventRuleConfig(Set<String> labels, double confidenceThreshold, int consecutiveToOpen, Duration absenceToClose)` (docs/MVP2-PLAN.md §E, E-a) — debounce rule for `DetectionEvent` tracking, referenced by `PipelineConfig#eventRule()`; `labels` empty is valid ("track nothing"), otherwise a label outside the set is never tracked regardless of confidence; `confidenceThreshold` [0,1]; `consecutiveToOpen`>0; `absenceToClose` positive; `static defaults()` = {person,car} @ 0.5, N=3, 5s absence
- `enum EventType` — DETECTION, DEVICE_ONLINE, DEVICE_OFFLINE, STREAM_STARTED, STREAM_STOPPED, PIPELINE_ERROR, TRAINING, GEOFENCE_BREACH (docs/OPS-CORE-PLAN.md §G — raised by `GeofenceMonitor`, vision-application, on a breach edge transition; attributes carry `{assetId, zoneId, zoneName, kind, direction}`, `streamId` always `null` since a breach is asset-scoped, not stream-scoped)
- `record FeedId(UUID value)` — `static random()`, `static of(String)`; identity for a transmitted feed (see `FeedTransmitterPort`)
- `record FeedSpec(String protocol, URI source, Map<String,String> options)` — `protocol` lower-case-validated exactly like `StreamDescriptor.protocol`; `source`/`options` must not be `null` (no defaulting convenience ctor — follows `StreamDescriptor`'s closest precedent of validating rather than defaulting); `options` defensively copied via `Map.copyOf`
- `record FlightCapability(boolean commandable, boolean armSupported, boolean modeSelectSupported, List<String> selectableModes)` (docs/DRONE-INFRA-PLAN.md I-e Stage 2) — a best-effort snapshot of what flight commands a device's aircraft accepts, derived from the last-heard firmware/vehicle-family; `selectableModes` non-null, defensively copied via `List.copyOf`. A driving UI shows only the controls the snapshot allows. `armSupported`/`modeSelectSupported` are never `true` when `commandable` is `false`; `static notCommandable()` = all-false + empty list (the honest answer for an unheard/unsupported/Betaflight vehicle). Returned by `FlightCommandPort#capabilities`
- `record FlightState(String firmware, String mode, Boolean armed, Boolean failsafe, Integer gpsFixType, Integer satellites, Double hdop, Integer rssiPercent, List<String> armingBlockers)` (docs/FC-INTEGRATIONS-PLAN.md F-a) — flight-controller-reported state decoded from ArduPilot/INAV/Betaflight/PX4 telemetry; every field except `armingBlockers` individually nullable (a decoder merges this incrementally as different MAVLink messages arrive — "unknown" and "known false/zero" must stay distinguishable field by field); `firmware` is `"ardupilot"`/`"generic"`/`"px4"`/`null`; `gpsFixType` (MAVLink `GPS_FIX_TYPE` ordinal) ∈[0,8] if present; `rssiPercent` ∈[0,100] if present; `satellites`/`hdop` ≥0 if present; `armingBlockers` non-null, defensively copied via `List.copyOf`, empty meaning "none currently known"; `static empty()` = all null + empty list
- `record GeoPosition(double latitude, double longitude, Double altitudeMeters)` — lat [-90,90], lon [-180,180], altitude nullable
- `final class GeoProjection` (docs/TACTICAL-MARKS-PLAN.md §1, Wave M1) — pure, stateless geo-math; private ctor, static methods only (no interface — one implementation, no substitution point, per java-clean-code SKILL.md §1). Reuses `GeoPosition` for both input (drone pose) and output (a "ground point" is just a `GeoPosition` with `altitudeMeters=null`) — no second geo type. `EARTH_RADIUS_METERS = 6_371_000.0` (IUGG mean radius, used by both methods); `DEFAULT_DEPRESSION_DEGREES = 45.0` (documented guess — no gimbal telemetry exists to read a real value from). `static GeoPosition project(GeoPosition drone, double headingDegrees, double altitudeMeters, double depressionDegrees)` — estimates the ground point a drone's camera is looking at: slant ground range = `altitudeMeters / tan(depressionDegrees)`, then a spherical destination-point (great-circle forward) projection of that range from `drone` along `headingDegrees`; `depressionDegrees==90` (nadir) or `altitudeMeters==0` short-circuits to the drone's own ground position (range 0) rather than trusting `tan(90°)`'s floating-point behavior; `headingDegrees` normalized mod 360 (any finite value accepted, including negative/>360); throws `IllegalArgumentException` for `drone==null`, negative/non-finite `altitudeMeters`, `depressionDegrees` outside `(0,90]`, or non-finite `headingDegrees`; the returned position's `altitudeMeters` is always `null` (ground elevation unknown — no terrain model). `static BearingDistance bearingDistance(GeoPosition from, GeoPosition to)` — haversine distance (meters) + initial great-circle bearing (degrees, [0,360), clockwise from north); throws `IllegalArgumentException` on either argument `null`. Both methods clamp/wrap internal trig results (asin argument clamped to [-1,1], longitude wrapped across the antimeridian into `[-180,180]`) so no valid input can produce `NaN` or an out-of-range `GeoPosition`/`BearingDistance`. **This is an honest estimate, not a precise fix** — no gimbal orientation or camera-intrinsics data anywhere in the platform; a mark built from `project` is stamped `MarkSource.DETECTION` and stays draggable/editable so an operator can correct it on the map
- `record GeofenceZone(ZoneId id, String name, ZoneKind kind, List<GeoPosition> polygon, Double maxAltitudeMeters, boolean enabled)` (docs/OPS-CORE-PLAN.md §G) — polygon ≥3 vertices (validated, defensively copied), implicitly closed (first vertex not repeated as last); `maxAltitudeMeters` nullable (no ceiling), must not be negative if present; zones are global (no per-asset/per-group scoping yet — docs/OPS-CORE-PLAN.md §G, U-e is later). `boolean contains(GeoPosition)` — ray-casting point-in-polygon treating latitude/longitude as planar Cartesian coordinates (longitude=x, latitude=y): accurate at fence scale (tens of meters to a few km), **not** valid near the poles or across the antimeridian (±180°) — see the method's own javadoc for the full caveat, no attempt is made to detect/reject a pathological polygon spanning either. `withEnabled(boolean)`, `withDetails(String,ZoneKind,List<GeoPosition>,Double)` return copies (identity/enabled untouched by `withDetails`, mirroring `Asset`/`Device`'s own `withDetails` leaving identity/lifecycle alone)
- `record Group(GroupId id, String name, GroupId parentGroupId)` (docs/U-AUTH-PLAN.md, wave 1) — org-chart node a `User` can hold a `Membership` in; `parentGroupId` nullable (`null` = root group); `name` non-blank; no other validation — subtree visibility scoping is a later slice, not part of this record
- `record GroupId(UUID value)` — `static random()`, `static of(String)`
- `enum JobState` (docs/CV-TRAINING-PLAN.md §6/§7, Phase 2) — RUNNING, SUCCEEDED, FAILED; mirrors `cv.proto`'s `JobState` enum minus `JOB_STATE_UNSPECIFIED` — mapping that proto zero-value to a concrete state (or rejecting it) is an adapter-boundary concern, not this enum's. Pure marker, no behavior, no dedicated test (same convention as `Capability`/`EventType`/`PixelFormat`)
- `record LayerGrant(SubjectType subjectType, UUID subjectId, AccessLevel level)` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — one explicit "give this user or group this access level" grant on a `MapLayer` (DELTA's own layer-sharing idea); nested `enum SubjectType { USER, GROUP }`; `subjectId` is deliberately a plain `UUID`, not a typed `UserId`/`GroupId` union — which typed id it is depends on `subjectType`, and resolving/matching it against a viewer's identity is `MapAccessPolicy`'s job (vision-application, Wave B), not this record's; all three components non-null
- `record LayerId(UUID value)` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — `static random()`, `static of(String)`; typed identity for a `MapLayer`, mirrors `MarkId`/`ZoneId` exactly
- `enum LayerKind` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — `COP | TEAM | PERSONAL`; sets the default visibility/write baseline a `MapLayer`'s `grants` extend — `COP` is the single system-wide shared-picture layer (org-wide view, MANAGER+ writes, exactly one per deployment, the default mark-promotion target), `TEAM` is group-owned (members see+contribute), `PERSONAL` is owner-only by default. Pure marker otherwise, no dedicated test
- `enum LifecycleState` — ACTIVE, DEACTIVATED, **DELETED (soft)**. Nothing is ever destroyed: a deleted asset/device is hidden from listings and refuses to stream, but its record, usages and telemetry survive and the removal is reversible. Transitions: ACTIVE⇄DEACTIVATED; ACTIVE|DEACTIVATED→DELETED; DELETED→DEACTIVATED (restore — never straight back to ACTIVE)
- `record MapEvent(EntityType entity, Action action, LayerId layerId, Object payload)` (docs/MAP-REWORK-PLAN.md §2.3, Wave A) — a live change to a `Mark`/`Drawing`/`MapLayer`, the payload `LiveUpdatePublisherPort#publishMapEvent` carries; nested `enum EntityType { MARK, DRAWING, LAYER }`, `enum Action { CREATED, UPDATED, CLEARED, DELETED }`; all four components non-null; **`payload`'s runtime type is validated against `entity`** (`MARK`&rarr;`Mark`, `DRAWING`&rarr;`Drawing`, `LAYER`&rarr;`MapLayer`) — a real cross-field invariant, not just documentation, catching a wiring bug at construction rather than letting a mismatched payload reach the API layer's DTO mapping; `Action.CLEARED` is likewise only valid for `entity == MARK` (a drawing/layer has no "cleared" lifecycle state)
- `record MapLayer(LayerId id, String name, LayerKind kind, Ownership ownership, List<LayerGrant> grants, Instant createdAt)` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — a named, access-controlled layer marks and drawings live on (DELTA's "give N participants access to a layer" idea); `name` non-blank, at most `MAX_NAME_LENGTH` (80) chars; `grants` non-null, defensively copied, may be empty; exactly one `LayerKind.COP` layer per deployment is an application-layer invariant (the service that bootstraps/creates layers enforces it), not checked here since a single record has no way to see the rest of the fleet. `withName(String)`/`withGrants(List<LayerGrant>)` — each replaces one field, everything else (including `id`/`kind`/`ownership`/`createdAt`) kept
- `record Mark(MarkId id, LayerId layerId, GeoPosition position, MarkKind kind, Affiliation affiliation, String label, String note, Ownership ownership, Instant createdAt, MarkStatus status, MarkSource source, Verification verification)` (docs/MAP-REWORK-PLAN.md §2.2, superseding docs/TACTICAL-MARKS-PLAN.md §1's shape) — a geolocated tactical mark on the shared operational picture: structurally a *point* version of `GeofenceZone` (one `GeoPosition` instead of a ≥3-vertex polygon, no breach semantics) but, unlike `GeofenceZone`, owned and group-scoped — `ownership` mirrors how `Asset` carries `Ownership` (reused verbatim, no new owner type); `label` must not be blank, `note` nullable (blank normalizes to `null`); every component non-null except `note`. Three components added by the MAP-REWORK: `layerId` (which `MapLayer` the mark lives on — visibility/write access resolves from that, not from `ownership` alone), `affiliation` (APP-6 symbology, orthogonal to `kind`), `verification` (DELTA review state, see `Verification`). `createdBy()` derives `ownership.ownerId()` (display + "creator can edit while unverified"). Wither methods, each replacing only the fields it names: `withPosition(GeoPosition)` — drag-correct; `withDetails(String,String,MarkKind,Affiliation)` — label/note/kind/affiliation (position/layer/status/verification untouched, mirroring how `GeofenceZone#withDetails` leaves `enabled` untouched); `withStatus(MarkStatus)` — ACTIVE⇄CLEARED; `withVerification(Verification)` — review-state transitions; `withLayer(LayerId)` — promotion (or a plain re-file) to a different layer. A `MarkSource.DETECTION` mark is an honest estimate (see `GeoProjection`) — always draggable/editable, never a hidden precise fix
- `record MarkId(UUID value)` (docs/TACTICAL-MARKS-PLAN.md §1, Wave M1) — `static random()`, `static of(String)`; typed identity for a `Mark`, mirrors `ZoneId` exactly
- `enum MarkKind` (docs/MAP-REWORK-PLAN.md §2.2, superseding docs/TACTICAL-MARKS-PLAN.md §1's set) — `UNIT | EQUIPMENT | HAZARD | POI | TARGET`; an icon/colour category (styling lives in web), not a breach semantic like `ZoneKind`, and orthogonal to `Affiliation` ("what it is" vs "whose it is"). `FRIENDLY` is **deleted** — a friendly mark is now any kind with `Affiliation.FRIENDLY` — and `UNIT`/`EQUIPMENT` are added. Old-kind→(new kind, default affiliation) migration/seed mapping: `TARGET`→(`TARGET`,`HOSTILE`), `HAZARD`→(`HAZARD`,`UNKNOWN`), `POI`→(`POI`,`NEUTRAL`), `FRIENDLY`→(`UNIT`,`FRIENDLY`)
- `enum MarkSource` (docs/TACTICAL-MARKS-PLAN.md §1, Wave M1) — `MANUAL` (dropped by a user clicking the map) `| DETECTION` (created via `GeoProjection.project` from a drone's pose — an estimate, not a fix)
- `enum MarkStatus` (docs/TACTICAL-MARKS-PLAN.md §1, Wave M1) — `ACTIVE | CLEARED`; lifecycle state, mirrors the shape (not the meaning) of `ZoneKind`/`LifecycleState`
- `record Membership(GroupId groupId, Role role)` (docs/U-AUTH-PLAN.md, wave 1) — a `User`'s role within one group; both components non-null; rides on the `User` aggregate (saved whole) — no separate membership repository port
- `record ModelRef(String id, String version)`
- `record Ownership(UserId ownerId, GroupId groupId)`
- `record PipelineConfig(ModelRef model, double confidenceThreshold, int inferenceFps, int maxInFlightInferences, boolean overlayTelemetry, Set<String> labelFilter, EventRuleConfig eventRule, boolean overlayBurnIn, boolean detectionEnabled)` — inferenceFps/maxInFlightInferences>0; `eventRule` non-null (docs/MVP2-PLAN.md §E, E-a); `overlayBurnIn` (docs/MVP2-PLAN.md §V, V-e) gates whether the application layer renders overlays onto published video at all — `true` (default) burns detection boxes/OSD into every published frame (what HLS-fallback/external players/future recordings see), `false` skips rendering entirely (cheaper encode path for streams whose only consumers are the SPA's own vector-overlay viewers); `detectionEnabled` (docs/CV-CONTROL-PLAN.md §1, Wave B) is the per-stream detection on/off switch — `false` means the pipeline skips `detect()` entirely (zero `DetectionPort` calls, zero inference CPU) while video keeps flowing untouched; the skip itself is enforced by `StreamPipeline` (vision-application, Wave C), this record only carries the flag; an 8-arg convenience ctor defaults `detectionEnabled=true` (`DEFAULT_DETECTION_ENABLED`) — this was the *canonical* ctor before Wave B, so `mergeOntoDefaults()`/every pre-existing test/call site compiles unchanged — chaining onto it, a 7-arg convenience ctor still defaults `overlayBurnIn=true`, and a 6-arg one still defaults `eventRule=EventRuleConfig.defaults()` too — same "N-1-arg convenience ctor" idiom as `Asset`/`AssetUsage`, now three deep; `static defaults()` = `yolo26n.pt`/latest (docs/CV-CONTROL-PLAN.md §1/§B — fixes the previously-dead `"yolo"` id, which matched no real checkpoint and silently relied on cv-service's own fallback, to the real checkpoint id cv-service already defaults to), 0.4, 10fps, 2 in-flight, telemetry on, no filter, `EventRuleConfig.defaults()`, burn-in on, detection on (10fps since docs/CYCLES-PLAN.md §12, CP-c — was 5fps)
- `enum PixelFormat` — BGR24, RGB24, YUV420P, JPEG, H264_PACKET, UNKNOWN
- `record RcChannels(List<Integer> microsByChannel)` (docs/RC-CONTROL-PHASE1-PLAN.md §1, RC-CONTROL Phase 1 R1) — one 1-based microsecond value per RC channel, the payload `ManualControlPort` relays as a MAVLink `RC_CHANNELS_OVERRIDE` (#70) frame; constants `RELEASE=0` ("release this channel back to the RC radio"), `IGNORE=0xFFFF` ("leave this channel unchanged"), `MIN_MICROS=1000`, `MAX_MICROS=2000`; compact ctor does `List.copyOf`, length 1..18, each entry within `[MIN_MICROS,MAX_MICROS]` or one of the two sentinels; `int channel(int oneBased)` — validated range; `static released(int channelCount)` — `RELEASE` for channels 1..count. The record itself allows 1..18 channels, but v1's `ChannelMap#defaultMap()` only ever populates channels 1..8 — channels 9..18 use a different, unresolved extension release sentinel (plan Open Questions §4)
- `enum Role` (docs/U-AUTH-PLAN.md, wave 1) — `PILOT`, `MANAGER`, `ADMIN`, declared **least→most privileged**; ordinal ordering is meaningful (`User#topRole()` picks the highest role by natural/ordinal ordering) — reordering these constants would silently change what "top" means. Pure marker otherwise, no dedicated test (same convention as `Capability`/`EventType`)
- `record SampleImage(byte[] data, String contentType)` (docs/CV-TRAINING-PLAN.md §1/§C, Wave T1) — image bytes for one `TrainingSample`, stored via `SampleImageStorePort` keyed by `TrainingSampleId`; mirrors `AssetImage` bit-for-bit — `data` non-empty, `contentType` non-blank, clone-in via the compact ctor, `data()` overridden to clone-out on every access — the same bytea-backed, no-history binary-payload precedent reused deliberately (docs/CV-TRAINING-PLAN.md §C) rather than a second shape invented for training frames
- `enum SampleStatus` (docs/CV-TRAINING-PLAN.md §1, Wave T1) — PENDING (captured, annotations are unreviewed model suggestions), LABELED (operator confirmed/corrected — the only status a dataset export includes), DISCARDED (operator rejected the frame, kept for provenance, never exported)
- `record StreamDescriptor(String protocol, URI uri, Map<String,String> options)` — **protocol must be lower-case** (ctor throws otherwise)
- `record StreamId(UUID value)` — `static random()`, `static of(String)`
- `record Telemetry(DeviceId deviceId, Instant at, Double latitude, Double longitude, Double altitudeMeters, Double headingDegrees, Double batteryPercent, Map<String,Double> extra, FlightState flightState)` — all Double fields and `flightState` nullable; an 8-arg convenience ctor defaults `flightState=null` (docs/FC-INTEGRATIONS-PLAN.md F-a, same "N-1-arg convenience ctor" idiom as `AssetUsage`'s 7-arg ctor) so every pre-existing call site compiles unchanged
- `record TrackedObject(long trackId, Detection detection, Instant firstSeen, Instant lastSeen)` — trackId≥0, lastSeen≥firstSeen
- `record TrainingJobSpec(String baseModel, String datasetId, int epochs)` (docs/CV-TRAINING-PLAN.md §6/§7, Phase 2) — a request to fine-tune a CV model; the Java-side shape of `cv.proto`'s `Training.StartTraining` request message. `baseModel`/`datasetId` must not be blank, `epochs` must be positive. `datasetId` is deliberately a plain string, not a typed `DatasetId` — it crosses a gRPC boundary to a training host that has no notion of the platform's id types, mirroring the wire message field-for-field (same reasoning as `ModelRef`'s plain-string `id`)
- `record TrainingProgress(String jobId, int epoch, int totalEpochs, double loss, double map50, JobState state, String message)` (docs/CV-TRAINING-PLAN.md §6/§7, Phase 2) — one message in a training job's progress stream; the Java-side shape of `cv.proto`'s `Training.StartTraining` response message. `jobId` non-blank (plain string, not a typed id — same wire-boundary reasoning as `TrainingJobSpec#datasetId`); `epoch`/`totalEpochs` non-negative (0 valid on a terminal message with no epoch of its own); `state` non-null; `message` non-null but may be empty — free-form text: the produced model id on `SUCCEEDED`, the failure reason on `FAILED`, empty on `RUNNING`. `loss`/`map50` are unconstrained (meaningful only alongside `RUNNING`; no bound imposed here, unlike `Detection#confidence`'s [0,1] clamp — cv-service's own stream is the source of truth for what these mean at each state). cv-service's contract: one `RUNNING` per epoch, then exactly one terminal `SUCCEEDED`/`FAILED`; cancellation ends the stream with no terminal message
- `record TrainingSample(TrainingSampleId id, DatasetId datasetId, StreamId streamId, AssetId assetId, Instant capturedAt, int width, int height, List<Annotation> annotations, SampleStatus status, UserId labeledBy, Instant labeledAt)` (docs/CV-TRAINING-PLAN.md §1, Wave T1) — a captured frame plus its (evolving) annotations, one row of a `Dataset`; image bytes are **not** carried here (see `SampleImage`/`SampleImageStorePort`, the `AssetImage` precedent applied to training frames); `width`/`height` must be positive; `annotations` defensively copied, may be empty (a negative/background sample); `streamId` non-nullable, `assetId`/`labeledBy`/`labeledAt` nullable (asset unresolved at capture time, or not yet reviewed); status/annotation transitions — capture seeds PENDING+MODEL annotations from live detections, labeling replaces them and moves to LABELED/DISCARDED+OPERATOR provenance while stamping `labeledBy`/`labeledAt`, rejecting any label outside the dataset's `classes` — are all an application-layer concern (`LabelingService`), not this record's; a `TrainingSample` itself permits any status/annotation combination its own component-level validation allows
- `record TrainingSampleId(UUID value)` (docs/CV-TRAINING-PLAN.md §1, Wave T1) — `static random()`, `static of(String)`
- `record UsageId(UUID value)` — `static random()`, `static of(String)`
- `record User(UserId id, String username, String displayName, String email, String passwordHash, boolean enabled, List<Membership> memberships)` (docs/U-AUTH-PLAN.md, wave 1) — the identity aggregate; `username` non-blank, normalized to lower-case in the compact ctor (trim + `toLowerCase(Locale.ROOT)`) so lookups are case-insensitive by construction — actual uniqueness on the normalized username is enforced by `UserRepositoryPort` implementations, not this record; `email` shape-checked only (non-blank, contains `@`) — deliberately not a full RFC validator; `passwordHash` non-blank and **opaque to the domain**: stored exactly as given, never hashed or verified here (BCrypt lives behind the application layer's `PasswordVerifier` seam, wave 2 — nobody should add hashing logic in this module); `memberships` defensively copied via `List.copyOf`, may be empty; 6-arg convenience ctor defaults `memberships=List.of()` (no convenience ctor omitting `passwordHash` — a user without one can never authenticate, so it stays required); `topRole()` → `Optional<Role>`, the highest role across memberships by `Role`'s ordinal ordering, `Optional.empty()` (not a throw) when there are no memberships — an unassigned user is a valid state; `toString()` overridden to render `passwordHash=***` instead of the real value, so it never leaks into logs (records auto-generate a `toString()` that would otherwise include every component)
- `record UserId(UUID value)` — `static random()`, `static of(String)`
- `record Verification(VerificationState state, UserId verifiedBy, Instant verifiedAt)` (docs/MAP-REWORK-PLAN.md §2.1, Wave A) — a `Mark`'s DELTA-style verify&rarr;confirm&rarr;share-wider review state; nested `enum VerificationState { UNVERIFIED, CONFIRMED, REJECTED }`; `state` non-null; `verifiedBy`/`verifiedAt` required non-null when `state` is `CONFIRMED` or `REJECTED`, otherwise unconstrained (an `UNVERIFIED` verification is never actually constructed with reviewer fields set, but this record doesn't forbid it — `static unverified()` is the one canonical way to get one, returning `(UNVERIFIED, null, null)`)
- `record VideoFrame(StreamId streamId, long sequence, Instant capturedAt, int width, int height, PixelFormat format, ByteBuffer data)` — sequence≥0, width/height>0; ctor stores `data.asReadOnlyBuffer()`; `data()` overridden to return `data.duplicate()` fresh each call
- `record ZoneId(UUID value)` — `static random()`, `static of(String)`; typed identity for a `GeofenceZone` (docs/OPS-CORE-PLAN.md §G)
- `enum ZoneKind` — `KEEP_IN | KEEP_OUT` (docs/OPS-CORE-PLAN.md §G); pure marker read by `GeofenceMonitor` (vision-application) to decide what "inside"/"outside" a `GeofenceZone`'s polygon means for breach purposes — see that enum's own javadoc

### `com.drones.vision.domain.port.out` (driven — implemented by adapters)
- `AssetImageRepositoryPort` (docs/UX-REWORK-PLAN.md §U-d item 3): `void save(AssetId, AssetImage)` — upsert, replaces whatever was stored; `Optional<AssetImage> findByAssetId(AssetId)` — empty for no image, never distinguishing "unknown asset" from "known asset, no image" (this port has no dependency on `AssetRepositoryPort`, same "no referential integrity between repositories" convention every other port already follows); `boolean existsByAssetId(AssetId)` — the cheap presence check a fleet/asset list uses to populate `hasImage` without loading image bytes for every row; `void deleteByAssetId(AssetId)` — idempotent
- `AssetRepositoryPort`: `Asset save(Asset)`; `Optional<Asset> findById(AssetId)`; `List<Asset> findAll()`; `Optional<Asset> findByDeviceId(DeviceId)` — a device belongs to ≤1 asset; `void deleteById(AssetId)` — idempotent
- `AssignmentRepositoryPort` (docs/U-SCOPE-PLAN.md, U-e slice 2, feature 2): the pilot→asset assignment join — `void assign(UserId, AssetId)` (idempotent upsert), `void unassign(UserId, AssetId)` (idempotent), `Set<AssetId> assetsForPilot(UserId)`, `Set<UserId> pilotsForAsset(AssetId)`, `boolean isAssigned(UserId, AssetId)`. A standalone many-to-many join, deliberately independent of both the `User` and `Asset` aggregates (a pilot's roster changes far more often than either identity), with the same "no cross-repository foreign keys" convention every other port follows — pilot/asset existence is the application layer's concern. Empty set (never `null`) means "no links". JPA + in-memory implementations land in waves 2–3
- `AssetUsageRepositoryPort`: `AssetUsage save(AssetUsage)` — upsert by id; `Optional<AssetUsage> findById(UsageId)`; `List<AssetUsage> findRecentByAsset(AssetId, int limit)` — newest first; `Optional<AssetUsage> findOpenByAsset(AssetId)` — ≤1 open per asset
- `CategoryRepositoryPort`: `DeviceCategory save(DeviceCategory)`; `Optional<DeviceCategory> findById(CategoryId)`; `List<DeviceCategory> findAll()`
- `DetectionPort`: `CompletionStage<DetectionResult> detect(VideoFrame, PipelineConfig)` — **must not block**; performs no internal queuing/limiting — caller bounds in-flight count and skips (never queues) once at the bound
- `AuditTrailPort`: `AuditEntry record(AuditEntry)` — append-only, never updated or deleted, not even when its target is soft-deleted; `List<AuditEntry> findRecent(int limit)`, `List<AuditEntry> findByTarget(AuditTargetType, String targetId, int limit)`, `List<AuditEntry> findByActor(UserId, int limit)` (docs/U-SCOPE-PLAN.md, U-e slice 2, feature 7 — the "my activity" feed for one actor), all newest-first
- `DetectionEventRepositoryPort` (docs/MVP2-PLAN.md §E, E-a): `DetectionEvent save(DetectionEvent)` — **upsert** by `DetectionEvent#id()` (unlike `DetectionRepositoryPort#save`'s append-only rows, a `DetectionEvent` mutates over its own open lifetime); `List<DetectionEvent> findRecent(Instant sinceInclusive, int limit)` — across every stream, newest-first by `lastSeen`, `sinceInclusive` nullable (no lower bound); `List<DetectionEvent> findByStream(StreamId, int limit)` — one stream, same ordering, empty (not an error) for an unknown/eventless stream
- `DetectionRepositoryPort`: `void save(DetectionResult)` — append-only; `List<DetectionResult> query(DetectionQuery)`
- `DatasetRepositoryPort` (docs/CV-TRAINING-PLAN.md §1, Wave T1): `Dataset save(Dataset)` — upsert by `DatasetId`; `Optional<Dataset> findById(DatasetId)`; `List<Dataset> findAll()` — snapshot, **no** ownership/group filter baked in (same "no scope filtering in the repository" convention as `MarkRepositoryPort`) — group-scoped visibility is `vision-application`'s job; `void delete(DatasetId)` — idempotent, does not cascade to the dataset's samples/images
- `TrainingSampleRepositoryPort` (docs/CV-TRAINING-PLAN.md §1, Wave T1): `TrainingSample save(TrainingSample)` — **upsert** by `TrainingSampleId` (a sample mutates over its own review lifecycle, same "upsert not append" shape as `DetectionEventRepositoryPort`, unlike `DetectionRepositoryPort`'s immutable rows); `Optional<TrainingSample> findById(TrainingSampleId)`; `List<TrainingSample> findByDataset(DatasetId, SampleStatus statusOrNull, int limit)` — `statusOrNull` nullable = every status, order implementation-defined, empty list (not an error) for an unknown/sampleless dataset; `int countByDataset(DatasetId, SampleStatus statusOrNull)` — same filter, no row loading, the cheap count a dataset's `sampleCounts` summary uses; `void delete(TrainingSampleId)` — idempotent, does not cascade to the sample's stored image
- `SampleImageStorePort` (docs/CV-TRAINING-PLAN.md §1/§C, Wave T1) — the `AssetImageRepositoryPort` shape, verbatim, reused for training-sample frames instead of asset photos: `void save(TrainingSampleId, SampleImage)` — upsert; `Optional<SampleImage> findById(TrainingSampleId)` — empty for no image, never distinguishing "unknown sample" from "known sample, no image" (no dependency on `TrainingSampleRepositoryPort`, same "no referential integrity between repositories" convention); `void delete(TrainingSampleId)` — idempotent
- `DatasetUploadPort` (docs/CV-TRAINING-V2-PLAN.md §3, Wave W1) — **replaces `DatasetExportPort`** (deleted; export was a manual filesystem step, upload is an implicit part of Train over gRPC): `DatasetUpload upload(DatasetId, String dataYaml, List<ExportEntry> entries)` — ships a YOLO-format dataset to the training host, **replacing any prior upload for the same `datasetId`** (idempotent — "label more, train again" just re-uploads and supersedes); nested `record ExportEntry(String imageName, byte[] imageBytes, String labelFileText)` — the exact same shape and compact-ctor validation `DatasetExportPort.ExportEntry` had (`imageName` non-blank, `imageBytes` non-empty, `labelFileText` non-null but may be empty for a zero-annotation/background sample), carried over verbatim. The application layer (`YoloDatasetWriter`, vision-application) composes `entries` + `dataYaml` purely in memory; this port only frames/transports the bytes — a streamed zip over a gRPC client-streaming call is the intended implementation (`adapters/adapter-cv-grpc`, a later wave), but framing is this port's implementation detail to hide, not its contract
- `DeviceDiscoveryPort`: `String method()` — stable lower-case key; `List<DiscoveredDevice> scan(Duration timeout)` — **blocking**, must self-time-box to ~timeout, empty list ≠ error
- `DeviceRepositoryPort`: `Device save(Device)`; `Optional<Device> findById(DeviceId)`; `List<Device> findAll()`; `void deleteById(DeviceId)` — idempotent
- `EventPublisherPort`: `void publish(Event)` — must not throw on ordinary delivery failure; called on hot pipeline path, must return quickly
- `GeofenceRepositoryPort` (docs/OPS-CORE-PLAN.md §G): `GeofenceZone save(GeofenceZone)` — upsert by `ZoneId`; `Optional<GeofenceZone> findById(ZoneId)`; `List<GeofenceZone> findAll()` — snapshot, global (no ownership/scoping filter, same convention as `CategoryRepositoryPort`); `void deleteById(ZoneId)` — idempotent
- `MarkRepositoryPort` (docs/TACTICAL-MARKS-PLAN.md §1, Wave M1; unchanged by docs/MAP-REWORK-PLAN.md Wave A) — mirrors `GeofenceRepositoryPort`'s exact shape: `Mark save(Mark)` upsert by `MarkId`; `Optional<Mark> findById(MarkId)`; `List<Mark> findAll()` — snapshot, **no** ownership/group/layer filter baked in (same "no scope filtering in the repository" convention as every other port) — access filtering over this snapshot is the application layer's job (`MapAccessPolicy`/`MarkService`, Wave B); `void deleteById(MarkId)` — idempotent
- `MapLayerRepositoryPort` (docs/MAP-REWORK-PLAN.md §2.3, Wave A) — same minimal shape as `MarkRepositoryPort`: `MapLayer save(MapLayer)` upsert by `LayerId`; `Optional<MapLayer> findById(LayerId)`; `List<MapLayer> findAll()` — snapshot, no access filter baked in; `void deleteById(LayerId)` — idempotent, **does not cascade** to marks/drawings on the deleted layer (cascading + emitting a `MapEvent` per cascaded object is the application layer's job, docs/MAP-REWORK-PLAN.md §3's "layer CRUD" rule)
- `DrawingRepositoryPort` (docs/MAP-REWORK-PLAN.md §2.3, Wave A) — same minimal shape as `MarkRepositoryPort`: `Drawing save(Drawing)` upsert by `DrawingId`; `Optional<Drawing> findById(DrawingId)`; `List<Drawing> findAll()` — snapshot, no access filter baked in; `void deleteById(DrawingId)` — idempotent
- `GroupRepositoryPort` (docs/U-AUTH-PLAN.md, wave 1): `Optional<Group> findById(GroupId)`; `List<Group> findAll()` — snapshot; `Group save(Group)` — upsert by `GroupId`. Same minimal shape as `AssetRepositoryPort`/`GeofenceRepositoryPort`; no separate membership storage — `Group#parentGroupId` links the tree, `Membership`s ride on the `User` aggregate
- `ManualControlLink` (docs/RC-CONTROL-PHASE1-PLAN.md §1, RC-CONTROL Phase 1 R1) — opaque, adapter-owned handle to one engaged `ManualControlPort` relay link; `boolean active()` — whether its sender thread is still running
- `ManualControlPort` (docs/RC-CONTROL-PHASE1-PLAN.md §1, RC-CONTROL Phase 1 R1): driven port for a streaming, ack-less RC-channel-override relay — the fire-and-forget, continuous-stream opposite of `FlightCommandPort`'s request→ack one-shots. `boolean supports(Device)`; `ManualControlLink engage(Device)` — opens a link and starts its fixed-rate sender thread; throws `IllegalArgumentException` if the device is unsupported or not currently reachable ("cannot command what you cannot hear" — no live source address heard from it yet); `void send(ManualControlLink, RcChannels)` — hands the sender thread the newest frame via a single-slot, latest-wins mailbox; non-blocking, must never itself write to the wire (only the link's own sender thread may), no-op once released; `void release(ManualControlLink)` — sends a short release-sentinel burst (`RcChannels#released`) then stops the sender thread; idempotent. No real implementation yet — `adapters/adapter-mavlink`'s `MavlinkManualControlSender` is docs/RC-CONTROL-PHASE1-PLAN.md's next wave, R3
- `LiveUpdatePublisherPort` (docs/REALTIME-PLAN.md §4): `void publishFleetChanged()` — no payload, a driving adapter re-derives its own snapshot; `void publishTelemetryAppended(AssetId, Telemetry)`; `void publishDetections(AssetId, DetectionResult)` — attributed to the stream's *owning asset*, not the stream itself; `void publishEvent(Event)` — must not throw on ordinary delivery failure and must return quickly, same contract as `EventPublisherPort`, since it's called from the same hot stream-pipeline/telemetry paths (once per completed inference, once per appended sample); `void publishDetectionEvent(DetectionEvent)` (backend follow-up batch, extending the R-c channel for the events UI) — a debounced `DetectionEvent` opened/advanced/closed, carrying the event itself (unlike `publishFleetChanged`'s no-payload shape, since there's no cheaper way for a driving adapter to re-derive "which event, which state"); `void publishMapEvent(MapEvent)` (docs/MAP-REWORK-PLAN.md §2.3, Wave A) — **replaces** the former `default void publishMarkCreated(Mark)`/`publishMarkUpdated(Mark)`/`publishMarkCleared(Mark)` trio (docs/TACTICAL-MARKS-PLAN.md §5, deleted by this wave): one abstract method now covers marks, drawings, *and* layers. Unlike every other method on this port (and unlike the three it replaces, which were `default`-bodied no-ops precisely so pre-existing implementors kept compiling), `publishMapEvent` is **deliberately not `default`** — scoped per-connection SSE delivery (docs/MAP-REWORK-PLAN.md §4.3) is the point of the rework, so an implementor must engage with it rather than silently no-op. This is an intentional breaking change: `vision-api`'s `LiveUpdateRegistry` and `vision-app`'s `NoopLiveUpdatePublisher` **do not compile** until Wave C implements the new method (see Status below)
- `FeedTransmitterPort`: `boolean supports(FeedSpec)`; `StreamDescriptor start(FeedId, FeedSpec)` — blocking setup then background transmit, returns the descriptor the RX side (`VideoSourcePort`) can ingest from; `void stop(FeedId)` — idempotent. **TX (transmit) half** of the RX/TX doctrine (`docs/CYCLES-PLAN.md` §0): pushes a local media source over a real wire protocol so the platform's own RX adapters ingest it like real hardware — simulation infrastructure, unrelated to `StreamPublisherPort` (viewer-facing egress). Unrecoverable transmit failure just stops the feed, best-effort, no error channel (KISS)
- `FlightCommandPort` (docs/DRONE-INFRA-PLAN.md I-e — guarded command TX): `boolean supports(Device)`; four blocking command methods that each send one MAVLink command and wait briefly for the aircraft's own acknowledgement — `CommandResult setMode(Device, String modeName)`, `CommandResult arm(Device, boolean force)`, `CommandResult disarm(Device, boolean force)`, `CommandResult returnToHome(Device)` — plus one non-blocking, never-throwing `FlightCapability capabilities(Device)`. The one deliberate place the platform's RX-only doctrine (`docs/CYCLES-PLAN.md` §0) is broken: Stage 1 opened it for `returnToHome`; **Stage 2 (docs/DRONE-INFRA-PLAN.md I-e Stage 2)** added `setMode`/`arm`/`disarm`/`capabilities`. `returnToHome` is kept for Stage-1 compatibility and is equivalent to `setMode(device, "RTL")`. `arm`/`disarm`'s `force` uses the MAVLink `21196` magic value (bypasses the autopilot's pre-arm/disarm checks). The four command methods throw `IllegalArgumentException` for an unsupported device, a non-commandable firmware (unrecognized/never-heard, or a firmware whose RC link is known not to process the command, e.g. Betaflight), or (setMode) an unknown mode name; throw `IllegalStateException` when the aircraft's own acknowledgement explicitly refuses (denied/unsupported/failed). A lost or missing acknowledgement is neither — see `CommandResult#NO_ACK`. `capabilities` never throws: it returns `FlightCapability.notCommandable()` for an unheard/unsupported/Betaflight vehicle, and reports commandable (INAV included — its commands honestly `NO_ACK` if its RX ignores them). There is still no fully generic command surface (arbitrary opcodes, mission/fence upload) — later stages. One real implementation today: `adapters/adapter-mavlink`'s `MavlinkFlightCommander` — see that module's MODULE.md
- `ModelRegistryPort`: `List<ModelRef> models()`; `void promote(ModelRef)` — must apply atomically (no partial-promotion reads)
- `OverlayPort`: `VideoFrame render(AnnotatedFrame)` — synchronous, input frame never mutated, returns a distinct instance
- `PasswordHasherPort` (docs/U-AUTH-PLAN.md, wave 2): `String hash(String rawPassword)` — opaque hash string, exactly what `User#passwordHash()` stores; `boolean verify(String rawPassword, String hash)` — constant-time-ish check, `true` iff `rawPassword` produced `hash`; implemented outside the domain/application (BCrypt in production, `vision-app`, wave 3)
- `RecordingPort`: `void streamStarted(StreamId, Device)`; `void publish(StreamId, VideoFrame)` — must be cheap, no blocking I/O; `void streamEnded(StreamId)` — idempotent; method shape mirrors StreamPublisherPort but is a distinct port (durable storage vs live egress)
- `ReplayFrameExtractionPort` (docs/CV-TRAINING-V2-PLAN.md §3, Wave W1) — pull one decoded frame out of a stream's durable recording at a specific instant, the "capture a training frame from replay" counterpart to live capture's `StreamService#latestRawFrame`: `Optional<VideoFrame> frameAt(StreamId, Instant)` — `Optional.empty()` is **honest absence** (no recording configured, recording disabled on the media server, or nothing recorded at that instant), never an error, mirroring `StreamPublisherPort#playbackUrl`'s own posture toward a missing recording; implementations **must** stamp the returned `VideoFrame`'s `capturedAt` with the requested `at` and its `sequence` with `0`, so callers never have to reconcile two notions of "when". No implementation yet — `MediamtxReplayFrameExtractor` (`adapters/adapter-publish-hls`) is a later wave
- `StreamPublisherPort`: `void streamStarted(StreamId, Device)`; `void publish(StreamId, VideoFrame)` — called for every frame, adapter owns latest-wins drop policy; `void streamEnded(StreamId)` — idempotent; `default Optional<URI> viewUrl(StreamId)` — HLS viewer URL, empty unless overridden; `default Optional<URI> whepUrl(StreamId)` — WebRTC/WHEP viewer URL (docs/MVP2-PLAN.md §L), empty unless overridden; **unlike `viewUrl`, never proxied** — a WHEP session is a POST/SDP + ICE exchange, not a byte stream a reverse proxy can forward, so an implementation that has one returns the media server's own origin URL directly, not an app-relative path; `default Optional<URI> playbackUrl(StreamId, Instant, Duration)` (docs/OPS-CORE-PLAN.md §R) — recorded-clip playback/export URL for the `[start, start+duration)` window, empty unless overridden; **never proxied either**, same reasoning as `whepUrl` — see `adapters/adapter-publish-hls/MODULE.md` (once R-a lands) for the one real implementation (mediamtx's own `/get` playback endpoint)
- `TelemetryRepositoryPort`: `void save(UsageId, Telemetry)` — append-only; `List<Telemetry> findByUsage(UsageId, int limit)`
- `TelemetrySourcePort`: `boolean supports(Device)`; `Flow.Publisher<Telemetry> open(Device)` — per-open, hot/live, latest-wins backpressure; `void close(DeviceId)` — idempotent
- `TrainingPort` (docs/CV-TRAINING-PLAN.md §6/§7, Phase 2): `void startTraining(TrainingJobSpec spec, Consumer<TrainingProgress> onProgress)` — the Java side of `cv.proto`'s `Training.StartTraining` server-streaming RPC; optional, GPU-training-host-only in production (never GB4005 — see the plan's "constraint that shapes everything"). **Contract:** blocks, consuming the underlying gRPC stream and invoking `onProgress` for every `TrainingProgress` until the stream terminates — a terminal `SUCCEEDED`/`FAILED` progress, or the call returns/throws because the transport itself ended (an unrecoverable transport error is a thrown exception, never a synthesized terminal progress); cancellation ends the stream with no terminal message, so a caller cannot distinguish "cancelled" from "closed early" purely from this port. **Threading:** blocks for the job's lifetime — the caller (an application-layer service) runs it on its own executor, not the calling thread; `onProgress` is invoked synchronously from the consuming thread; implementations must be safe to call concurrently for different jobs. Deliberately mirrors a callback shape rather than returning a `Stream`/`Iterator`, keeping this module free of any streaming/reactive framework type. No implementation yet — an adapter (`adapters/adapter-cv-grpc`) wrapping the gRPC stream is a later wave; `vision-application`'s `TrainingJobService` running it off-thread and holding job state is another
- `UserRepositoryPort` (docs/U-AUTH-PLAN.md, wave 1): `Optional<User> findByUsername(String)` — matches case-insensitively against the normalized (lower-cased) username every `User` already stores; `Optional<User> findById(UserId)`; `User save(User)` — upsert by `UserId`, saves the full aggregate including `memberships`; `List<User> findAll()` — snapshot. Implementations enforce actual username uniqueness; this port only declares the lookup contract
- `VideoSourcePort`: `boolean supports(StreamDescriptor)`; `Flow.Publisher<VideoFrame> open(StreamId, StreamDescriptor)` — per-open, hot/live, latest-wins backpressure, unrecoverable failure → `onError`; `void close(StreamId)` — idempotent

## Conventions
- **Validation:** every record validates in its compact constructor with manual `if (…) throw new IllegalArgumentException(…)` per field (no Bean Validation, no `Objects.requireNonNull` — that idiom is application-layer only).
- **Defensive copies:** every `List`/`Set`/`Map` component is reassigned in the compact ctor via `List.copyOf`/`Set.copyOf`/`Map.copyOf`; `VideoFrame` reassigns `data` to `data.asReadOnlyBuffer()`.
- **UUID id pattern:** `AssetId`/`DeviceId`/`DetectionEventId`/`GroupId`/`StreamId`/`UsageId`/`UserId`/`ZoneId` all wrap `UUID` with the same pair of factories — `random()` (`UUID.randomUUID()`) and `of(String)` (`UUID.fromString`, rethrows as `IllegalArgumentException`). `CategoryId` is the exception: a validated kebab-case `String` slug, not a UUID — categories are reference data with human-authored keys, not generated ids. `Event.id` is the other exception: a plain `String` (not a typed id), since `Event` is a fire-and-forget notification never looked up by id, unlike `DetectionEvent`, which is stored/upserted/queried by its own typed id.
- `with*`/`closed` copy methods (`Asset.withDevices/withAttributes`, `AssetUsage.closed/withPositions/withSampleCount`) return new record instances — records have no setters.
- Enums (`Capability`, `EventType`, `PixelFormat`) are pure marker sets, no behavior.
- Every port interface's javadoc documents **Contract** and **Threading**; ports with backpressure concerns (`VideoSourcePort`, `TelemetrySourcePort`) add a **Backpressure** section describing the latest-wins drop policy.

## Gotchas
- **`GeofenceZone.contains` is a planar approximation, not spherical geometry** (docs/OPS-CORE-PLAN.md §G) — it treats longitude/latitude as plain Cartesian x/y and runs the standard ray-casting even-odd point-in-polygon test over them. This is accurate at the scale a geofence actually operates at (tens of meters to a few kilometers — the curvature error over such a small span is negligible), but it is **not** valid near the poles (meridian convergence badly distorts the planar assumption) or across the antimeridian (±180°, where longitude wraps to a discontinuity a planar test cannot see — a zone meant to span 179°→-179° would instead read as spanning nearly the whole globe the other way). No attempt is made to detect or reject a pathological polygon that spans either; zones are expected to be authored well away from both.
- `VideoFrame.data()` never returns the buffer stored on the record — every call returns a fresh `data.duplicate()`, so two readers (or two calls from the same reader) never share position/limit/mark state, and writes always throw `ReadOnlyBufferException`.
- `Event.streamId` is nullable — device-level events (`DEVICE_ONLINE`/`DEVICE_OFFLINE`) have no stream.
- `StreamDescriptor.protocol` must already be lower-case; the compact ctor throws `IllegalArgumentException` if it isn't — callers cannot rely on normalization happening for them.
- `CategoryId.slug` must match `[a-z0-9]+(-[a-z0-9]+)*` (lower-case-kebab), enforced here rather than left to callers, because slugs double as stable, human-readable reference-data keys.
- `Asset.devices` must be non-empty — an asset with zero devices cannot be constructed at all.
- `RecordingPort`'s method shape is identical to `StreamPublisherPort` (`streamStarted`/`publish`/`streamEnded`) but the two are deliberately separate interfaces/adapters (durable recording vs live viewer egress), enabled/disabled independently per stream.
- `DetectionPort` itself applies no concurrency limit or queuing — `PipelineConfig.maxInFlightInferences` is enforced entirely by the caller (`vision-application`'s `StreamPipeline`), not this port.
- `ScanRequest.methods()` (now in `vision-application`) empty means "all registered mechanisms", not "none".
- `FeedSpec` has no null-defaulting convenience constructor for `options` (unlike some other records with a defaults ctor) — `StreamDescriptor` is the closest existing precedent for a `(protocol, URI, Map<String,String>)`-shaped record, and it validates (throws on `null` options) rather than defaulting, so `FeedSpec` follows that instead.
- **`Event` and `DetectionEvent` are unrelated types despite the name overlap** (docs/MVP2-PLAN.md §E, E-a) — `Event` is `EventPublisherPort`'s fire-and-forget, write-only notification (device online/offline, stream started/stopped, pipeline errors, raw per-frame detections), never persisted or queried back by anything today; `DetectionEvent` is a debounced, stored/upserted/queried "label X was seen for a while" record with a completely different shape (no `label`/`confidence`/`position` on `Event`, no `type`/`message`/`attributes` on `DetectionEvent`). There is no cheap way to serve `Event`s (e.g. `PIPELINE_ERROR`) through `DetectionEventRepositoryPort`/`GET /api/events` — see `vision-application`/`vision-api`'s MODULE.mds for the full reasoning and what a real fix needs.
- **`ControlBinding#toMicros` ignores `centerMicros` entirely for `Source.BUTTON`** (docs/RC-CONTROL-PHASE1-PLAN.md §1/§5) — a button's mapping is a straight 2-point line from `minMicros` (normalized 0, unpressed) to `maxMicros` (normalized 1, pressed); there is no third point to interpolate through, matching the plan's §5 table listing a literal "—" for button center/deadband. `ChannelMap.defaultMap()`'s aux bindings still set `centerMicros=minMicros` — that value exists only to satisfy `ControlBinding`'s `minMicros<=centerMicros<=maxMicros` invariant, not because it participates in the button's own math. A caller building a custom `BUTTON` binding can pick any valid `centerMicros`; it is inert.
- **`ChannelMap#apply` never throws on missing input** — an unreported/`null`/short `axes` or `buttons` list simply reads as `0.0` (center for `AXIS`, unpressed for `BUTTON`) for whichever bindings' `sourceIndex` falls outside it; this is deliberate (a browser gamepad frame that doesn't yet report every axis/button must not crash the relay) and `apply` sizes its returned `RcChannels` to the highest `rcChannel` any binding targets (`Math.max(1, …)` if `bindings` is empty), `IGNORE`-filling every channel no binding covers.

## Status

docs/MAP-REWORK-PLAN.md **Wave A done** (map as Common Operational Picture, domain half — modeled
on DELTA's Monitor/layers/verify-promote ideas): nine new `com.drones.vision.domain.model` types —
`Affiliation` (APP-6 friend/enemy enum), `LayerId`/`DrawingId` (standard UUID id idiom, mirror
`MarkId`/`ZoneId`), `LayerKind` (`COP|TEAM|PERSONAL`), `AccessLevel` (`VIEW|CONTRIBUTE|MANAGE`,
declared least→most permissive — ordinal comparison is part of the contract, `MapAccessPolicy`
Wave B relies on it), `LayerGrant` (nested `SubjectType{USER,GROUP}`), `MapLayer` (`withName`,
`withGrants`), `Verification` (nested `VerificationState{UNVERIFIED,CONFIRMED,REJECTED}`, static
`unverified()`, CONFIRMED/REJECTED require `verifiedBy`+`verifiedAt`), `DrawKind`
(`LINE|POLYGON|ARROW|TEXT`), `Drawing` (geometry invariants keyed off `DrawKind`, `withGeometry`,
`withDetails`) — plus `MapEvent` (nested `EntityType{MARK,DRAWING,LAYER}`/`Action{CREATED,UPDATED,
CLEARED,DELETED}`, the live-update payload). Two new `com.drones.vision.domain.port.out`
interfaces mirroring `MarkRepositoryPort`'s minimal shape — `MapLayerRepositoryPort`,
`DrawingRepositoryPort` — plus one breaking change: `LiveUpdatePublisherPort`'s three `default`
no-op `publishMarkCreated`/`publishMarkUpdated`/`publishMarkCleared` methods (docs/
TACTICAL-MARKS-PLAN.md §5) are **deleted**, replaced by one non-`default` `void
publishMapEvent(MapEvent event)`. `Mark` itself is reworked in place — three new components
(`layerId`, `affiliation`, `verification`), five wither methods (`withPosition` split out of the
old `withDetails`, whose signature narrowed to `(String label, String note, MarkKind kind,
Affiliation affiliation)`; `withStatus` unchanged; new `withVerification`, `withLayer` for
promotion) — and `MarkKind` changes shape: `FRIENDLY` deleted (a friendly mark is now any kind with
`Affiliation.FRIENDLY`), `UNIT`/`EQUIPMENT` added, giving `{UNIT,EQUIPMENT,HAZARD,POI,TARGET}`; the
old-kind→(new kind, default affiliation) migration/seed mapping is on `MarkKind`'s own javadoc.
`MarkStatus`/`MarkSource`/`MarkRepositoryPort` are unchanged. `./mvnw -B -pl vision-domain test`:
**478/478 green**, up from 391 (+87: 6 `LayerIdTest`, 6 `DrawingIdTest` — mirror `ZoneIdTest`; 4
`LayerGrantTest`, 9 `VerificationTest` (including `unverified()` and the CONFIRMED/REJECTED
reviewer-required matrix), 14 `MapLayerTest` (name length + defensive-copy + both withers), 29
`DrawingTest` (the full per-`DrawKind` point-count matrix, TEXT's non-blank-label requirement,
non-TEXT's blank-normalizes-to-null + length cap, `colorToken`'s kebab-case + length validation,
defensive point copying, both withers), 11 `MapEventTest` (null-component matrix, the
payload-type-must-match-entity invariant for all three entity kinds, `CLEARED`-is-mark-only);
`MarkTest` grew from 14 to 22 (+8: `layerId`/`affiliation`/`verification` null checks,
`withPosition`, `withVerification`, `withLayer`, `withDetails`'s narrowed signature).
`Affiliation`/`LayerKind`/`DrawKind` have no dedicated test, consistent with this module's
pure-marker-enum convention (`Capability`/`EventType`/`Role`); `AccessLevel` likewise has none
despite its ordinal contract being load-bearing — same precedent as `Role`, which documents the
identical "declaration order is meaningful" contract with no dedicated test either;
`MapLayerRepositoryPort`/`DrawingRepositoryPort` have no dedicated test (plain interfaces, no
nested record), consistent with `MarkRepositoryPort`/`GeofenceRepositoryPort`/every other port
here. **Design decisions**: `MapEvent`'s compact ctor validates two cross-field invariants beyond
the plan's literal code sketch — `payload`'s runtime type must match `entity`, and `Action.CLEARED`
is only valid for `entity == MARK` — both real "manual `if(...)  throw`" checks in the same spirit
as `Verification`'s own CONFIRMED/REJECTED rule, not just documentation, since a wiring bug here
would otherwise surface as a `ClassCastException` deep in the API layer's DTO mapping instead of at
construction; `MapLayer.ownership` is the pre-existing `Ownership` record reused verbatim per the
plan's own frozen code sketch (not widened to a nullable-`groupId` variant for `COP`/`PERSONAL`
layers) — how a `COP`/`PERSONAL` layer gets a well-formed `Ownership` (bootstrap principal, a
personal layer's own creator) is Wave B/C's bootstrapping concern, not this record's; `Drawing`'s
`colorToken` reuses `CategoryId`'s exact kebab-case regex rather than inventing a second pattern,
since both are "short lower-case-kebab slug" shapes. **Cross-module impact — expected and
deliberate, not a regression**: this wave's scope is `vision-domain/**` only, per
docs/MAP-REWORK-PLAN.md §6's wave table; every other module is now red until its own wave lands.
Concretely: (1) `LiveUpdatePublisherPort`'s new non-`default` `publishMapEvent` method breaks
`vision-api`'s `LiveUpdateRegistry` and `vision-app`'s `NoopLiveUpdatePublisher`, both of which
implement this interface and must add the method — Wave C's job. (2) `Mark`'s reworked constructor
shape (12 components instead of 9, reordered, new required `layerId`/`affiliation`/`verification`)
and `MarkKind.FRIENDLY`'s removal break every existing call site — `vision-application`'s
`DefaultMarkService`/`MarkSpec`/`MarkPatch` (Wave B), `adapters/adapter-persistence`'s
`MarkEntity`/`JpaMarkRepository` and `vision-app`'s `InMemoryMarkRepository`/devsupport seed data
(Wave C), and `vision-api`'s `MarkController`/`MarkResponse`/`CreateMarkRequest` (Wave C, though
per docs/MAP-REWORK-PLAN.md §4 the whole `/api/marks` surface is replaced by `/api/map/marks`
anyway). (3) `MapLayerRepositoryPort`/`DrawingRepositoryPort` are brand-new interfaces with zero
implementations yet — nothing existing referenced them, so nothing is broken by their addition, but
Wave B's `MapLayerService`/`DrawingService` and Wave C's JPA/in-memory implementations now have a
frozen contract to build against. None of `vision-application`, any `adapters/*`, `vision-api`,
`vision-app`, nor `vision-web` was modified in this wave — their own compile breaks are Wave B/C's
job to resolve, not a defect in this one.

docs/NAV-IA-REDESIGN-PLAN.md **Wave 4, F8 done** (replay library, domain half): one additive method
on `AssetUsageRepositoryPort` (`com.drones.vision.domain.port.out`), `findRecent(int limit)` —
`findRecentByAsset(AssetId, int)`'s fleet-wide counterpart (see its own entry above), backing the
Monitor → Replay library page's list of finished flights across every asset (docs/design/
10-replay.md). No new model type — the read model that resolves each row's asset name and scopes
visibility lives in `vision-application` (`UsageService`/`UsageSummary`, a new service area rather
than a method on `ReplayService` — see that module's MODULE.md for why). A port interface has no
behavior of its own to unit-test (same convention as every other port here), so `./mvnw -B -pl
vision-domain test` stays **391/391 green, unchanged count**. Two pre-existing implementations
picked up the new method: `adapters/adapter-persistence`'s `JpaAssetUsageRepository` and
`vision-app`'s `InMemoryAssetUsageRepository` (see their own MODULE.mds) — plus one hand-fake test
double, `DefaultLabelingServiceTest.FakeAssetUsageRepositoryPort` (`vision-application`), which
needed the same mechanical `not exercised by this suite` stub the rest of its unused methods already
carry.

docs/CV-TRAINING-V2-PLAN.md **Wave W1 done** (one-button training + replay capture delta, domain half): a **delta** on top of CV-TRAINING-PLAN's Wave T1 (below) — deletes the manual-export surface and adds the two ports its replacement needs. Removed: `port/out/DatasetExportPort.java` (+ `DatasetExportPortTest.java`), `model/DatasetExport.java` (+ `DatasetExportTest.java`), and the `DatasetStatus.EXPORTING` enum constant (nothing in this module, or anywhere in the reactor, ever set it — see Cross-module impact). Added: `model/DatasetUpload.java` (`datasetId`/`uploadedAt`/`sampleCount`/`sizeBytes` — `datasetId`/`uploadedAt` non-null, counts non-negative; the delivery-receipt replacement for `DatasetExport`'s filesystem manifest), `port/out/DatasetUploadPort.java` (`upload(DatasetId, String dataYaml, List<ExportEntry> entries)`, replacing any prior upload for the same `datasetId`; nested `ExportEntry` record carries `DatasetExportPort.ExportEntry`'s three compact-ctor checks over **verbatim**, byte-for-byte — non-blank `imageName`, non-empty `imageBytes`, non-null `labelFileText`), `port/out/ReplayFrameExtractionPort.java` (`frameAt(StreamId, Instant) → Optional<VideoFrame>`, `Optional.empty()` for honest absence, never a thrown error — see their own entries above for full detail). Framework-free throughout, same compact-constructor `if (…) throw new IllegalArgumentException(…)` idiom as every other record here. `./mvnw -B -pl vision-domain test`: **391/391 green**, down from 393 net (-13 removed: 8 `DatasetExportTest` + 5 `DatasetExportPortTest`; +11 added: 6 `DatasetUploadTest` — full compact-ctor validation matrix (null `datasetId`/`uploadedAt`, negative `sampleCount`/`sizeBytes`, zero-is-valid, well-formed round-trip) + 5 `DatasetUploadPortTest` — `ExportEntry`'s validation matrix, same five cases `DatasetExportPortTest` had, carried over verbatim onto the new port; `ReplayFrameExtractionPort` has no dedicated test, consistent with this module's "ports have no dedicated test" convention (its nested-record exception doesn't apply — it has no nested record)). **Design decisions**: `ExportEntry`'s three checks and their exact messages were copied byte-for-byte from the deleted `DatasetExportPort.ExportEntry` (read before deletion) rather than re-derived, per the plan's explicit "carried over verbatim" instruction; `DatasetUpload` deliberately drops `DatasetExport`'s `exportId`/`classes`/`location` fields — `exportId` had no caller once export stopped being a separate, resumable, downloadable artifact, `classes` is redundant with the `dataYaml` string the upload call already carries, and `location` (a filesystem/object-storage handle) has no meaning for a transport-only delivery that never lands on the platform's own disk (docs/CV-TRAINING-V2-PLAN.md §A); `ReplayFrameExtractionPort` reuses `VideoFrame` rather than inventing a new frame type, matching every other frame-shaped port in this module. **`EXPORTING` grep, whole repo**: besides this module's own `DatasetStatus.java` (now removed) and `DatasetExportPort`'s deleted javadoc, two references remain **outside vision-domain** that this wave deliberately did not touch (out of scope — `vision-domain/**` only): `adapters/adapter-persistence/src/test/java/.../PostgresDockerIntegrationTest.java:1116` (`DatasetStatus.EXPORTING` in a persistence round-trip test — now a compile error, W6's job per the plan) and `vision-api/src/main/java/.../dto/DatasetResponse.java:18` (a javadoc comment naming `EXPORTING`, harmless but stale — also W6). `vision-web/src/app/core/api/models.ts:1368`'s `DatasetStatus` TS union still lists `'EXPORTING'` too — that one is W7's job per the plan's §8. None of these three block `vision-domain` compiling/testing standalone. **Cross-module impact**: this wave deletes two types (`DatasetExportPort`, `DatasetExport`) and one enum constant that four other modules reference today (found via repo-wide grep: `vision-application` — `YoloDatasetWriter`, `LabelingService`, `DefaultLabelingService`, `TrainingStores`, and three test files; `adapters/adapter-persistence` — `FilesystemDatasetExport` + its test; `vision-app` — `TrainingWiringConfiguration`, `VisionTrainingProperties`, two wiring tests; `vision-api` — `LabelingController`, `DatasetExportResponse`, a controller test). **All four modules will not compile until their own waves (W5 application, W6 REST+wiring+persistence) land** — this is expected and named by the plan's own wave sequencing (`W1 → W2‖W3‖W4‖W5 → W6 → W7`), not a regression this task introduced; `vision-domain` itself builds and tests green in isolation per CLAUDE.md's per-module build discipline. `DatasetUploadPort`/`ReplayFrameExtractionPort` are brand-new interfaces with zero implementations yet, same "nothing broken by addition, but now a frozen contract to build against" pattern as every other new port in this module's history — `GrpcDatasetUploadPort` (adapter-cv-grpc, W3) and `MediamtxReplayFrameExtractor` (adapter-publish-hls, W4) are the next waves that implement them, both depending only on this one.

docs/CV-TRAINING-PLAN.md **Phase 2 §6/§7 training-job seam, domain half done** (Java side of cv-service's now-implemented `Training.StartTraining` server-streaming fine-tune): three new `com.drones.vision.domain.model` types — `TrainingJobSpec` (`baseModel`/`datasetId`/`epochs`, mirrors `cv.proto`'s `TrainingJobSpec` request message field-for-field), `TrainingProgress` (`jobId`/`epoch`/`totalEpochs`/`loss`/`map50`/`state`/`message`, mirrors `cv.proto`'s `TrainingProgress` response message), `JobState` (`RUNNING`/`SUCCEEDED`/`FAILED` — proto's `JobState` minus `JOB_STATE_UNSPECIFIED`, mapped at the adapter boundary) — plus one new `com.drones.vision.domain.port.out` interface, `TrainingPort` (`startTraining(TrainingJobSpec, Consumer<TrainingProgress>)`, a blocking callback-shaped streaming verb — see its own entry above for the full Contract/Threading). `./mvnw -B -pl vision-domain test`: **393/393 green**, up from 381 (+12: 4 `TrainingJobSpecTest`, 8 `TrainingProgressTest` — full compact-ctor validation matrices plus a couple of shape-sanity tests (zero-epoch terminal message, empty message on `RUNNING`); `JobState` has no dedicated test, same convention as every other pure marker enum here; `TrainingPort` has no dedicated test, same convention as every other port interface here). **Design decisions**: both `TrainingJobSpec.datasetId` and `TrainingProgress.jobId` are plain `String`, not typed domain ids (`DatasetId`/a hypothetical `TrainingJobId`) — these records cross a gRPC boundary to an external training host that has no notion of the platform's id types, and the task's frozen contract mirrors the wire messages field-for-field rather than the higher-level `TrainingJob`/`ModelTrainingPort` abstraction the plan's own §7 sketches for a later wave; `jobId` is validated non-blank (not merely non-null) for consistency with every other String field in this module (`DatasetExport#exportId`, `DatasetExportPort.ExportEntry#imageName`, etc.) even though a blank value was never explicitly called out; `loss`/`map50` carry no range validation (unlike `Detection#confidence`'s [0,1] clamp) since the task scoped validation explicitly to `jobId`/`state`/`epoch`/`totalEpochs` and these two metrics' valid range/meaning is entirely cv-service's to define, not this module's to guess at. **Cross-module impact**: `TrainingPort` is a brand-new interface with zero implementations — nothing existing referenced it before this wave, so nothing else in the reactor is broken by its addition — but it now has a frozen contract for two later waves to build against: an adapter (`adapters/adapter-cv-grpc`) implementing it over the gRPC stream, and `vision-application`'s `TrainingJobService` running it off its own executor (per the port's Threading contract) and holding per-job state for a REST/UI layer to poll. Neither `vision-application`, any adapter, `vision-api`, `vision-app`, nor `vision-web` was modified in this wave.

docs/CV-TRAINING-PLAN.md **Wave T1 done** (CV model-improvement loop, domain half — capture/label/dataset value models + ports): nine new `com.drones.vision.domain.model` types — `DatasetId`, `TrainingSampleId` (UUID ids, `random()`/`of(String)`, same pattern as every other typed id); `AnnotationSource` (`MODEL`/`OPERATOR`, pure provenance marker); `Annotation` (ground-truth box+label, reuses `BoundingBox`, deliberately **no confidence** unlike `Detection`, free-form `label` matching `Detection#label`'s open-vocab reasoning rather than a `CategoryId`); `DatasetStatus` (`OPEN`/`EXPORTING`/`ARCHIVED`); `Dataset` (name + optional `targetCategory` + ordered `classes` YOLO vocabulary + `Ownership` + status + `createdAt`); `SampleStatus` (`PENDING`/`LABELED`/`DISCARDED`); `TrainingSample` (captured frame metadata + evolving `List<Annotation>`, image bytes deliberately not carried here); `SampleImage` (mirrors `AssetImage` bit-for-bit — clone-in via compact ctor, clone-out on every `data()` call); `DatasetExport` (an export run's manifest) — plus four new `com.drones.vision.domain.port.out` interfaces, `DatasetRepositoryPort`, `TrainingSampleRepositoryPort`, `SampleImageStorePort` (the `AssetImageRepositoryPort` shape, verbatim), `DatasetExportPort` (with a nested, compact-ctor-validated `ExportEntry` record) — and two new `AuditTargetType` constants, `DATASET`/`MODEL` (see their own entries above for full detail). No change to any pre-existing type's shape; every new type lives entirely in new files. `./mvnw -B -pl vision-domain test`: **381/381 green**, up from 322 (+59: 6 `DatasetIdTest`, 6 `TrainingSampleIdTest` — mirror `ZoneIdTest`; 5 `AnnotationTest`, 9 `DatasetTest`, 12 `TrainingSampleTest` — full compact-ctor validation matrices (blank/null required fields, non-positive `width`/`height`, nullable-field acceptance for `targetCategory`/`assetId`/`labeledBy`/`labeledAt`) plus defensive-copy assertions on `classes`/`annotations`; 8 `SampleImageTest` — the `AssetImageTest` clone-in/clone-out suite verbatim, retargeted; 8 `DatasetExportTest`; 5 `DatasetExportPortTest` (new `port/out` test package — `ExportEntry`'s own compact-ctor validation, the one nested-record exception to this module's "ports have no dedicated test" convention). `AnnotationSource`/`DatasetStatus`/`SampleStatus` have no dedicated tests, consistent with `Capability`/`EventType`/every other pure marker enum here; the four new port interfaces likewise have none, consistent with `MarkRepositoryPort`/`GeofenceRepositoryPort`/every other port. **Design decisions**: `Dataset.classes`/`DatasetExport.classes` allow empty (no minimum-size requirement stated by the plan, matching `EventRuleConfig.labels`'s "empty is valid" precedent — unlike `GeofenceZone.polygon`'s explicit ≥3 minimum); `DatasetExportPort.ExportEntry` got real compact-ctor validation (non-blank `imageName`, non-empty `imageBytes`, non-null-but-possibly-empty `labelFileText`) even though the plan's own code sketch elides constructor bodies everywhere (`{ }`), matching the project's blanket "every record validates in its compact constructor" convention (CLAUDE.md) rather than reading the elided body as a literal "no validation" instruction; `ExportEntry`'s `imageBytes` is **not** defensively cloned (unlike `SampleImage`/`AssetImage`) since it is a one-shot, single-read handoff from the pure `YoloDatasetWriter` (Wave T2) straight into `DatasetExportPort.write`, not stored/re-read state — cloning would only add cost with no aliasing risk to guard against in that flow. **Cross-module impact for T2 (`vision-application`) and T3 (`adapter-persistence`+`vision-app`), the next waves, both depending only on T1**: all four new ports are brand-new interfaces with zero implementations yet — nothing existing referenced them before this wave, so nothing else in the reactor is broken by their addition — but T2's `DatasetService`/`LabelingService`/`YoloDatasetWriter` and T3's JPA entities/`InMemory*` fallbacks/`FilesystemDatasetExport` now have a frozen contract to compile against. Two things T2 will need to resolve itself, not pinned by this wave: (1) label-vs-`Dataset.classes` membership checking and PENDING→LABELED/DISCARDED transition rules are explicitly *not* domain invariants (per docs/CV-TRAINING-PLAN.md §1's own comment, "enforced in the application layer, not here") — `TrainingSample`'s compact ctor accepts any status/annotation combination its component-level checks allow; (2) `StreamPipeline.latestRawFrame()` / `StreamService.latestRawFrame(StreamId)` (docs/CV-TRAINING-PLAN.md §2/§D) are `vision-application`-only additions this wave does not touch. Neither `vision-application`, `adapters/adapter-persistence`, `vision-api`, `vision-app`, nor `vision-web` was modified in this wave.

docs/TACTICAL-MARKS-PLAN.md **Wave M1 done** (geolocated tactical marks, domain half): five new `com.drones.vision.domain.model` types — `Mark`, `MarkId`, `MarkKind`, `MarkStatus`, `MarkSource` (see their own entries above) — a new `com.drones.vision.domain.port.out` interface `MarkRepositoryPort` (mirrors `GeofenceRepositoryPort`'s shape exactly), and the two genuinely-new pieces: `GeoProjection` (pure geo-math: `project` drone-pose→estimated-ground-point, `bearingDistance` haversine+initial-bearing) plus its `BearingDistance` output record. No existing bearing/distance/haversine/destination-point helper existed anywhere in the codebase before this wave (surveyed `DetectionExtrapolator` — pixel-space, not geo; `GeofenceZone#contains` — planar point-in-polygon, not a distance/bearing calc) — `GeoProjection`'s trig is genuinely new math, not a delegation to a pre-existing helper. `Mark`/`Ownership`/`GeoPosition` reused verbatim, no new geo or owner type invented. `LiveUpdatePublisherPort` gained three `default`-bodied no-op methods, `publishMarkCreated`/`publishMarkUpdated`/`publishMarkCleared` (see its own entry above) — purely additive, so its two existing implementors (`vision-api`'s `LiveUpdateRegistry`, `vision-app`'s `NoopLiveUpdatePublisher`) compile unchanged and inherit the no-op until a later wave (M4) overrides them. `./mvnw -B -pl vision-domain test`: **322/322 green**, up from 266 (+56: 6 `MarkIdTest` — mirrors `ZoneIdTest`; 14 `MarkTest` — full compact-ctor validation matrix, blank-note-normalizes-to-null, `createdBy()` derivation, `withDetails`/`withStatus` copy-preserves-the-rest; 31 `GeoProjectionTest` — nadir (depression=90 and altitude=0 both collapse to the drone's own ground point, independent of heading), the four cardinal headings at the default 45° depression with a `bearingDistance` round-trip assertion, heading normalization (360/negative/>360 all equivalent), ground-range-vs-depression (`altitude/tan(depression)`, monotonic in depression), a golden due-north value derived independently from the small-angle meridian identity, antimeridian-wrap and near-pole sanity (no `NaN`, results stay in `GeoPosition`'s valid ranges), the full validation matrix (`null` drone, negative/non-finite altitude, depression outside `(0,90]`, non-finite heading), `bearingDistance` zero/cardinal/known-1°-of-latitude-baseline/symmetric/antimeridian cases, and null-argument validation; 5 `BearingDistanceTest` — bearing `[0,360)` boundary validation, non-negative distance). **Cross-module impact for M2/M3 (next waves, run in parallel per the plan, both depend only on M1)**: `MarkRepositoryPort` is a brand-new interface with zero implementations yet — nothing existing referenced it before this wave, so nothing is broken — but `adapters/adapter-persistence`+`vision-app`'s M2 (`MarkEntity`/`JpaMarkRepository`/`InMemoryMarkRepository`/wiring) and `vision-application`'s M3 (`MarkService`/`DefaultMarkService`) both now have a contract to compile against. For M2: `Mark`'s columns map straight across per docs/TACTICAL-MARKS-PLAN.md §3 (`position` splits into `latitude`/`longitude`/nullable `altitude_meters`, `ownership` splits into `owner_id`/`group_id`, `kind`/`status`/`source` as `VARCHAR` enum-name columns) — no jsonb, no polygon array, unlike `GeofenceZoneEntity`. For M3: `GeoProjection.project` takes a fully-resolved `GeoPosition` + heading + altitude + depression — `MarkService.geolocate` is responsible for reading `UsageTracker.latestTelemetry(AssetId)`, checking `latitude`/`longitude`/`headingDegrees` are present and `altitudeMeters` is present and `>0` *before* constructing the input `GeoPosition` and calling `project` (this wave's `GeoProjection` does **not** itself accept nullable telemetry fields — by design, since `GeoPosition`'s lat/lon are non-nullable primitives). `vision-api`/`vision-app`'s `LiveUpdateRegistry`/`NoopLiveUpdatePublisher` were not touched (M4's job) but do not need to be — the three new `publishMark*` methods are `default`-bodied. Neither `adapter-persistence`, `vision-application`, `vision-api`, `vision-app`, nor `vision-web` was modified in this wave.

docs/RC-CONTROL-PHASE1-PLAN.md **Wave R1 done** (RC relay, domain half): three new `com.drones.vision.domain.model` records — `RcChannels`, `ControlBinding` (with nested `enum Source { AXIS, BUTTON }`), `ChannelMap` — plus a new `com.drones.vision.domain.port.out` pair, `ManualControlPort`/`ManualControlLink` (see their own entries above for the full contract). `ChannelMap.defaultMap()` reproduces the plan's frozen §5 table verbatim: gamepad axes 0–3 → RC channels 1–4 (centered 1500µs, `[1000,2000]` full travel, no deadband, not reversed), gamepad buttons 0–3 → RC channels 5–8 (`[1000,2000]`, not reversed); channels 9–18 are out of scope for v1 (ambiguous extension release sentinel, plan Open Questions §4 — see `RcChannels`'s own entry). `toMicros`/`apply`'s exact semantics — including the "`BUTTON` ignores `centerMicros`" modeling decision and `apply`'s missing-input/unbound-channel behavior — are called out in Gotchas above. No dependency on any framework or on `VisibilityScope` (kept application-only per the plan, matching `FlightCommandPort`'s own precedent). `./mvnw -B -pl vision-domain test`: **266/266 green**, up from 222 (+44: 12 `RcChannelsTest` — length/sentinel/range validation, defensive copy, `channel()`, `released()`; 21 `ControlBindingTest` — the full compact-ctor validation matrix plus `toMicros` center/endpoints/reversed/deadband-boundary(inclusive)/clamp for both `AXIS` and `BUTTON`; 11 `ChannelMapTest` — `defaultMap()` shape, `apply()` against the plan's own example vector (`axes=[0.0,-0.12,1.0,0.0]`, `buttons=[0.0,1.0]`), empty/`null`/short-list safety, and unbound-channel `IGNORE`). **Cross-module impact for R2/R3 (next waves, same plan, run in parallel per the plan)**: `ManualControlPort`/`ManualControlLink` are brand-new interfaces with zero implementations yet — nothing existing referenced them before this wave, so nothing is broken — but `vision-application`'s R2 (`DefaultManualControlService`) and `adapter-mavlink`'s R3 (`MavlinkManualControlSender`) both now have a contract to compile against. Neither module was touched in this wave.

docs/CV-CONTROL-PLAN.md **Wave B done** (live per-stream CV control, domain half): `PipelineConfig` gained a 9th field, `detectionEnabled` (see its own entry above), plus `DEFAULT_DETECTION_ENABLED = true`. The previous 8-arg canonical ctor is now a convenience ctor defaulting `detectionEnabled=true` (unchanged behavior for every existing caller — `mergeOntoDefaults()` in `vision-api`'s `StartStreamRequest`, plus every test using the 6-/7-/8-arg forms); the 9-arg ctor is now canonical and does the validation. `defaults()` also fixes the model id from the dead `ModelRef("yolo","latest")` (matched no real checkpoint) to `ModelRef("yolo26n.pt","latest")` (the id cv-service already falls back to) — a behavior-preserving correctness fix, called out with a comment in the test. `ModelRegistryPort` untouched (stays dormant, per plan §D). `./mvnw -B -pl vision-domain test`: **222/222 green**, up from 221 (+1 net: one new `nineArgCanonicalConstructorRoundTripsAnExplicitDetectionEnabled` test; three existing ctor-chain tests extended in place to also assert `detectionEnabled()` defaults true, `defaultsMatchSpec`/`rejectsNullEventRule` updated for the new field/arg count). **Cross-module impact for later waves**: one production 8-arg canonical call site, `vision-api/src/main/java/com/drones/vision/api/dto/StartStreamRequest.java:46` (`mergeOntoDefaults()`), compiles unchanged (now resolves to the convenience ctor) — Wave D should extend it to thread `labelFilter`/`detectionEnabled` per the frozen DTO contract. One test assertion will now **fail at runtime** (not a compile break) because it hardcodes the old default model id: `vision-app/src/test/java/com/drones/vision/app/CvDetectionEndpointE2ETest.java:125` asserts `modelId == "yolo"` on a detection that round-trips through `PipelineConfig.defaults()` → `GrpcDetectionPort` → a test gRPC servicer that echoes the request's model id back — this now needs updating to `"yolo26n.pt"`, left for whichever wave next touches `vision-app`. No other repo-wide `PipelineConfig.defaults()`/`ModelRef("yolo",...)` usage was found to hardcode-assert the old id (checked `vision-application`, `vision-api`, `vision-app`, all `adapters/*`) — the rest either compare `defaults()` to itself/relatively, or construct their own unrelated `ModelRef("yolo", ...)` test fixtures independent of `defaults()`.

docs/U-SCOPE-PLAN.md **U-e slice 2, wave 1 (domain additions) done**: two changes to `com.drones.vision.domain.port.out` — a new `AssignmentRepositoryPort` (the pilot→asset join, see its own entry above) and one added method on `AuditTrailPort`, `findByActor(UserId, int)` (the only change to that file). No new model records, no change to any existing type's shape — the visibility model itself (`VisibilityScope`/`ScopeResolver`) lives in `vision-application`, not here, since it's derived application logic over these ports, not a domain value. `./mvnw -B -pl vision-domain test` stays **217/217 green, unchanged count** — a new port interface and an added interface method have no behavior of their own to unit-test, the same convention as `GroupRepositoryPort`/`PasswordHasherPort`/every other port here. JPA + in-memory implementations of `AssignmentRepositoryPort` and the `findByActor` addition to the two existing `AuditTrailPort` implementations (vision-app's `LiveUpdateAuditTrail` decorator + devsupport `InMemoryAuditTrail`) are waves 2–3. See `vision-application`'s MODULE.md for `VisibilityScope`/`DefaultScopeResolver`/`DefaultAssignmentService`/`DefaultActivityService` and the scoped read/command methods.

docs/U-AUTH-PLAN.md **wave 1 done** (identity domain model): four new `com.drones.vision.domain.model` types — `Role` (enum, `PILOT`/`MANAGER`/`ADMIN`, declared least→most privileged, ordinal ordering meaningful), `Group` (`GroupId`/`name`/nullable `parentGroupId` org-chart node), `Membership` (`GroupId`+`Role`, a user's role in one group), `User` (the identity aggregate — see its own entry above for the username-normalization/email-shape-check/passwordHash-opacity/`topRole()`/redacted-`toString()` details) — plus two new `com.drones.vision.domain.port.out` interfaces, `UserRepositoryPort` and `GroupRepositoryPort` (both minimal CRUD, same shape as `AssetRepositoryPort`/`GeofenceRepositoryPort`). `UserId`/`GroupId` already existed (used, not recreated) — only `Ownership`'s doc comments still describe them as "the constant dev principal until the identity phase"; that phase is this one, but updating `Ownership`'s javadoc/wiring is out of this wave's file scope (`vision-domain/**` only touches new types) and is left for whichever later wave rewires `CurrentUser`. No change to any pre-existing type's shape. `./mvnw -B -pl vision-domain test`: **217/217 green**, up from 188 (+29: 5 `GroupTest`, 3 `MembershipTest`, 21 `UserTest` covering the full validation matrix, username normalization, memberships defensive copy, `topRole()` empty/single/multiple-with-insertion-order-independence, and `toString()` redaction — `Role`/`UserRepositoryPort`/`GroupRepositoryPort` have no dedicated tests, consistent with this module's convention that pure enums and port interfaces carry no behavior of their own to unit-test). See docs/U-AUTH-PLAN.md waves 2–4 for `AuthService`/`UserService`/`GroupService` (vision-application), Spring Security/`AuthController`/JPA persistence/seeding (vision-app/vision-api/adapter-persistence), and the login UI (vision-web).

docs/U-AUTH-PLAN.md **wave 2** (application, in progress) added one further `com.drones.vision.domain.port.out` interface to this module — `PasswordHasherPort` (`hash(String)`/`verify(String,String)`, see its own entry above) — the password-hashing seam `AuthService`/`UserService` (vision-application) hash and verify through; BCrypt implements it in `vision-app`, wave 3. A pure port interface, no dedicated test, same convention as `UserRepositoryPort`/`GroupRepositoryPort`. `./mvnw -B -pl vision-domain test` stays **217/217 green**, unchanged count.

docs/DRONE-INFRA-PLAN.md **I-e Stage 2 done** (guarded command TX, domain half — arm/disarm + mode select): new `FlightCapability` record (`com.drones.vision.domain.model`, see its own entry above) plus four new methods on `FlightCommandPort` — `setMode(Device, String)`, `arm(Device, boolean)`, `disarm(Device, boolean)`, `capabilities(Device)` (`returnToHome` kept unchanged for Stage-1 compatibility, now equivalent to `setMode(device, "RTL")`). Same doctrine as Stage 1: capability-gated per firmware, `IllegalArgumentException`/`IllegalStateException`/`NO_ACK` failure contract on the command methods, and `capabilities` never throws. `./mvnw -B -pl vision-domain test`: **221/221 green**, up from 217 (+4 `FlightCapabilityTest`: `notCommandable()`, field/mode retention, defensive-copy + unmodifiable list, null-list rejection — `FlightCommandPort`'s new methods have no behavior of their own to unit-test, same convention as every other port). One real implementation: `adapters/adapter-mavlink`'s `MavlinkFlightCommander` (see that module's MODULE.md for the DO_SET_MODE/COMPONENT_ARM_DISARM wire paths and the `FlightModes.selectableModes` mode-name source). `vision-application`/`vision-api`/`vision-app`/`vision-web` are untouched by Wave A — the service/API/wiring/UI are Waves B/C.

docs/DRONE-INFRA-PLAN.md **I-e Stage 1 done** (guarded command TX, domain half — "bring it home"): new `CommandResult` enum (`com.drones.vision.domain.model`, `ACCEPTED`/`NO_ACK`) plus `FlightCommandPort` (a new `com.drones.vision.domain.port.out` interface, `supports(Device)`/`returnToHome(Device)`) — see their own entries above for the full contract/exceptions. This is the one deliberate place the RX-only doctrine (`docs/CYCLES-PLAN.md` §0) is broken, and only this far: a single command, framework-free, no generic command surface. One real implementation: `adapters/adapter-mavlink`'s `MavlinkFlightCommander` (see that module's MODULE.md for the TX wire path, the hub's new last-source-address tracking, and the ack-listener seam). No change to any pre-existing type's shape. `./mvnw -B -pl vision-domain test`: **188/188 green, unchanged count** — `CommandResult` is a pure marker enum and `FlightCommandPort` a plain interface, neither has behavior of its own to unit-test, same convention as `EventType`/`GeofenceRepositoryPort`/every other pure enum or port interface in this module (see the G-a paragraph below for the precedent). `vision-application`/`vision-api`/`adapters/adapter-persistence`/`adapter-simulation`/`vision-app`/`vision-web` are untouched by this phase — the application service/API/wiring/UI around this port are later waves.

docs/OPS-CORE-PLAN.md **G-a done** (geofencing, domain half): `ZoneId` (standard UUID id), `ZoneKind` (`KEEP_IN`/`KEEP_OUT`), `GeofenceZone` (polygon ≥3 vertices, `contains(GeoPosition)` ray-casting, `withEnabled`/`withDetails` copies — see their own entries above), `GeofenceRepositoryPort` (`com.drones.vision.domain.port.out`, upsert/findById/findAll/deleteById, same shape as `CategoryRepositoryPort`), `EventType#GEOFENCE_BREACH`, and `StreamPublisherPort#playbackUrl` (for the parallel R-a recording work — see that method's own entry above). No changes to any pre-existing type's shape. 188 tests, all passing (`./mvnw -B -pl vision-domain test`; up from 164 — G-a added 24: 6 `ZoneIdTest`, 18 `GeofenceZoneTest` covering polygon validation, `contains()` — inside/outside/vertex-adjacent/concave — copy methods, and defensive copying; `EventType`/`GeofenceRepositoryPort`/`StreamPublisherPort#playbackUrl` have no dedicated tests, consistent with this module's convention that pure enums and port interfaces carry no behavior of their own to unit-test). See `vision-application`'s own MODULE.md for `GeofenceMonitor`/`GeofenceService` (the breach-evaluation and CRUD halves) and `UsageTracker`'s new hook; `vision-api`/`adapter-persistence`/`vision-app`'s own MODULE.mds (once G-b lands) for the REST surface, JPA/jsonb storage, and wiring.

Fully implemented, including `docs/CYCLES-PLAN.md` §3's `FeedId`/`FeedSpec`/`FeedTransmitterPort` (C3, domain half) and §12 CP-c's `PipelineConfig.defaults()` inference-rate bump (5fps→10fps, paired with `vision-application`'s new `DetectionExtrapolator` for box smoothness at the higher sample rate). 164 tests, all passing (`./mvnw -B -pl vision-domain test`; up from 150 — docs/FC-INTEGRATIONS-PLAN.md F-a added `FlightState` (12 new `FlightStateTest` cases) and `Telemetry`'s 9th `flightState` component (8-arg convenience ctor default, 9-arg explicit ctor — 2 new `TelemetryTest` cases); up from 142 at the asset-image feature — docs/UX-REWORK-PLAN.md §U-d item 3 added `AssetImage`/`AssetImageRepositoryPort`, 8 new `AssetImageTest` cases; up from 122 at E-a — docs/MVP2-PLAN.md E-a added `DetectionEvent`/`DetectionEventId`/`DetectionEventState`/`EventRuleConfig`/`DetectionEventRepositoryPort` plus `PipelineConfig#eventRule`, 19 new tests total: 6 `DetectionEventIdTest`, 5 `EventRuleConfigTest`, 5 `DetectionEventTest`, 3 new `PipelineConfigTest` cases for the 6-arg convenience ctor default/explicit 7-arg ctor/null-rejection; docs/MVP2-PLAN.md V-e then added `PipelineConfig#overlayBurnIn`, +1 net test — see the V-e paragraph below).

docs/FC-INTEGRATIONS-PLAN.md **F-a done** (flight-controller-aware telemetry, domain half): `FlightState` (a new `com.drones.vision.domain.model` record) plus `Telemetry`'s new nullable 9th component `flightState` — no interface, no port change, since `FlightState` is a plain value that rides the existing `TelemetrySourcePort`→`UsageTracker`→persistence/SSE pipe (rejected alternatives: booleans-as-doubles in `extra`, a sibling telemetry channel — see the plan's "Design decision" section). `Telemetry`'s previously-canonical 8-arg constructor is now a convenience ctor defaulting `flightState=null`, so every one of the ~20 existing `new Telemetry(...)` call sites across `vision-application`/adapters/`vision-api` compiles unchanged. See `adapters/adapter-mavlink/MODULE.md` for the MAVLink decoder that actually populates it; `vision-application`/`vision-api`/`adapters/adapter-persistence`/`adapters/adapter-simulation`'s own MODULE.mds (once their own phases land — F-b/F-c) for how it flows to the API/persistence/simulation.

docs/UX-REWORK-PLAN.md **§U-d item 3 done** (asset image, domain half — CONTRACT 2's backend): `AssetImage` (a new `com.drones.vision.domain.model` record) plus `AssetImageRepositoryPort` (a new `com.drones.vision.domain.port.out` interface) — two genuinely substitutable implementations exist (`adapter-persistence`'s JPA/bytea one, `vision-app` devsupport's in-memory one), earning the interface per `.claude/skills/java-clean-code/SKILL.md` §1. No change to any existing type. See vision-application/vision-api/adapters/adapter-persistence/vision-app's own MODULE.mds for the rest of the feature (the probe endpoint, the image controller, the JPA/bytea storage, and the wiring).

**Backend follow-ups batch (R-c channel extension, domain half)**: `LiveUpdatePublisherPort` gained a fifth method, `publishDetectionEvent(DetectionEvent)`, backing the new `detection-events` live topic (`vision-api`) — no new model records, expressed entirely in terms of the pre-existing `DetectionEvent`. `./mvnw -B -pl vision-domain test` stays **142/142 green**, unchanged count (an interface method has no behavior of its own to unit-test here, same as every other `LiveUpdatePublisherPort` method before it). See `vision-application`/`vision-api`/`vision-app`'s MODULE.mds for the seam this is announced from (`DetectionEventRepositoryPort` decorator), the SSE topic itself, and the wiring.

docs/REALTIME-PLAN.md **§4 done** (server-push data plane, domain half — backend of Phase R-c): added `LiveUpdatePublisherPort` (`com.drones.vision.domain.port.out`) — a single new interface, no new model records (it's expressed entirely in terms of pre-existing `AssetId`/`Telemetry`/`DetectionResult`/`Event`). No test changes here (an interface has no behavior of its own to unit-test); `./mvnw -B -pl vision-domain test` stays **142/142 green**, unchanged count. See `vision-application`/`vision-api`/`vision-app`'s MODULE.mds for who implements/calls it, the SSE registry, and the feature-flag wiring.

docs/MVP2-PLAN.md **V-e done** (optional overlay burn-in skip, domain half) — `PipelineConfig` gained an 8th `overlayBurnIn: boolean` field (default `true` via the new `DEFAULT_OVERLAY_BURN_IN` constant, unchanged behavior), following E-a's own two-deep "N-1-arg convenience ctor" chain: the 7-arg ctor (previously canonical) is now a convenience ctor defaulting `overlayBurnIn=true`, and the pre-existing 6-arg ctor still chains through it (now also inheriting the burn-in default). `PipelineConfigTest` grew from 6 to 7 test methods net (two existing tests renamed/extended to also assert the new field's default, one new `eightArgConstructorAcceptsAnExplicitOverlayBurnIn` test, `rejectsNullEventRule`'s call site updated to the 8-arg shape) — `./mvnw -B -pl vision-domain test`: **142/142 green**. See `vision-application`/`vision-api`/`vision-app`'s MODULE.mds for how the flag reaches `StreamPipeline` and the API.

docs/MVP2-PLAN.md **E-a** (detection events) added `DetectionEvent`/`DetectionEventId`/`DetectionEventState`/`EventRuleConfig` (all in `com.drones.vision.domain.model`) and `DetectionEventRepositoryPort` (`com.drones.vision.domain.port.out`), plus a new `eventRule: EventRuleConfig` field on `PipelineConfig` (7th canonical component; a 6-arg convenience ctor keeps every pre-existing call site compiling, defaulting `eventRule` to `EventRuleConfig.defaults()`). See `vision-application`/`vision-api`/`vision-app`'s MODULE.mds for the debounce rule engine, API surface, and wiring.

docs/MVP2-PLAN.md **R-a2** (usage→stream link, domain half) added `AssetUsage#streamId` — see the field's own entry above and `vision-application`/`adapters/adapter-persistence`/`vision-app`'s MODULE.mds for how it's recorded, persisted, and consumed by flight replay.

docs/MVP2-PLAN.md **L-a** (WebRTC/WHEP viewing URL beside HLS) added `StreamPublisherPort#whepUrl` as a second `default Optional<URI>`-returning method alongside the existing `viewUrl` — no other domain change; this port already anticipated more than one egress transport via `default`/empty methods, so the ripple was additive only. See `adapters/adapter-publish-hls/MODULE.md` for the one real implementation (`MediamtxStreamPublisher`) and `vision-api`/`vision-app`'s MODULE.mds for how it reaches `/api/streams` etc.
